# LOG — append-only（发生了什么 + 为什么）

**LIVE FILE, 2026-09-13 00:25 起**（C1 关闭之后）。更早的条目在 `LOG-archive/`：
`LOG-2026-09-11-1730-to-2130.md`（建仓）· `LOG-2026-09-11-2215-to-2026-09-13-0010.md`（A 程序 → C1 全程）。

- 接手只需 `tail -n 150 LOG.md`。
- 标签：`#decision` `#measure` `#deadend` `#incident` `#ship`。
- 查目录 / 按标签检索（**glob 同时覆盖 live 与全部归档**）：
  `grep -n '^## ' LOG.md LOG-archive/*.md` · `grep -n -A4 '#measure' LOG.md LOG-archive/*.md`

## [2026-09-13 00:25] C2 第一刀：死掉的队友一直在广播他死在哪  #ship #incident

C2 的 Build 清单里有一条「**kill feed without death coordinate**」。查现状时发现这条**已经被违反**，
而且不是在 kill feed 里 —— 在**队友 HUD 槽**里：`mate*.dx/dz/dist` 对**死掉的**队友照样写真实坐标，
于是每个幸存者的 HUD 上都钉着一枚**永久的死亡标记**，标在「敌人最后出现过的地方」。
comm 和 hp 在 V11 那一刀就已经对死人清零了，位置没有。

**改法**：死掉的队友只留 `alive = 0`（这就是「我们少一个人」这条合法公共信号的全部内容），
位置三个字段清零。⛔ 没有做「渐隐的死亡标记」那种更像 CS 的版本 —— 默认往安全的方向倒（`coding-practice`），
要放宽是 C2 后面可以显式决定的事。

**新探针 `A1-P24`**（registered `clean`）：把**尸体**挪 14 m，断言**没有任何字段动**；
⭐ 同一笔里带**正向护栏** —— 对**活着**的队友做同样的移动必须让 `mate*.dx` 动，
否则这条绿是因为整个通道死了，不是因为尸体闭嘴了（坑 #11）。
⭐ **实测过它有鉴别力**：把旧行为临时放回去，探针立刻变 `LEAK`（`mate3.dx, mate3.dz`），闸也标了 `!`。
（是 `mate3` 不是 `mate0`，因为死人被排到队友序列末尾 —— 顺带确认了那个排序也在工作。）

## [2026-09-13 02:55] C2 主刀：公开回合状态进观测（obsDim 100 → 103，旧 genome 全作废）  #ship #measure

这一刀是 C1 之后真正的解锁项：进化 agent 终于能看见**点位在哪**和**哪个点位 armed**。

**观测的目标区块从「一个 zone」改成「每个 site 一块 + 全局回合状态」**：
每个 site 六个字段（相对向量 dx/dz、距离、**我**在不在里面、**我方几个人**在里面、**它 armed 没有**），
外加两个全局字段（armed 点位的**倒计时**、**defuse 进度**）。
顺手删掉 `self.inZone`（和 `obj0.selfIn` 重复，单一真源）⇒ `SELF_BASE` 20 → 19。
⇒ **obsDim 100 → 103**（单点位）/ **109**（双点位），genome **5324 → 5444**。
⛔ **所有既存 genome 作废** —— 这是 ROADMAP 早就登记过的 C2 代价。

### ⭐ 刻意不公开的那一格

`objN.armed` 是公开的（一次 plant 在真实回合里是**会播报、有声音**的），
但**未完成的 capture 进度不公开** —— 想看着表填满，你得人在那里。
这给攻方留了一个窗口，也是「公开」和「全知」的分界线。
同一条边界上还有上一刀的结论：死掉的队友只报「他死了」，不报「死在哪」。

**合同守卫**：`World` 构造时断言 `map.sites.length === cfg.siteCount` ——
观测布局是从 cfg 推出来的，两者不一致就会静默错位。labMap 也跟着按 cfg 生成点位。

### 新探针 `A1-P25`（这就是 C2 的 Probe 原文）

「目标状态变化时，只有 legal HUD 字段改变，不夹带敌情」：在 capture 模式下把一个点位设成 armed，
断言**恰好只有** `obj0.armed` 与 `obj.countdown` 变化。
⭐ **两个方向都实测过有鉴别力**：把 `obj.countdown` 打成常数 0，探针立刻报
`the plant moved only [obj0.armed]: the public round state is not reaching the observation`（正向护栏生效）。
当前 **24 条 probe，矩阵与登记表一致**（exit 0）。

### bench：多了三个字段反而更快

交错配对（`git stash` A,B,A,B，严格前台，load ≈ 5）：
旧 **63.4 / 63.0** ms/match → 新 **60.7 / 60.8** ms/match ⇒ **快约 4%**，两轮同号。
真因应该是删掉 `self.inZone` 省掉了每 agent 每 tick 一次距离计算，而新增的三个字段都是现成值。

## [2026-09-13 03:25] 上一刀我违反了自己的规矩，拆开之后发现真因，而且量到了噪声上界  #measure #incident #decision

C2 主刀里我**一次动了两件信息层的事**，然后只量了一次 —— 正是我整晚在执行的「一次一根杠杆」。拆开来跑：

| 臂 | 内容 | obsDim | kills（s1 / s2 / s3） | 首枪 s |
|---|---|---|---|---|
| **A** `v6a-fade` | 两件都没做 | 100 | 2.98 / 3.29 | 5.8 / 5.3 |
| **D** `armD-deathfix` | 只做「死亡坐标关闭」 | 100 | 1.98 / 3.14 | 9.3 / 4.0 |
| **C** `c2-base` | 两件都做 | **103** | **0.35 / 1.65** | **34.2 / 12.8** |
| **C′** `armC2-koth100` | 死亡坐标 + 目标区块，**但 koth 下不发回合状态字段** | **100** | **2.87 / 2.95 / 2.87** | 6.7 / 5.6 / 4.2 |

⇒ **死亡坐标关闭基本是免费的**（D ≈ A，seed 2 甚至首枪更快、开火更多）；**崩的是 103 维那一版**；
**C′ 在三个 seed 上回到基线水平**，没有塌缩、没有 34 秒首枪。

### ⚠ 但我不能说「就是那三个死输入干的」

`Trainer` 用一条 `Rng(seed)` 顺序抽整个初始种群，而 `randomGenome` 消耗的高斯数**等于 genome 长度**。
⇒ 103 维那一版从第 0 代起就是**完全不同的种群**，哪怕 seed 一样。
⇒ C 与 C′ 的差里混着「三个死输入」和「抽奖运气」两份，**这个实验本身分不开**（新坑 **#27**）。

能说的只有三句：① 无条件发回合状态字段的那一版，在测过的两个 seed 上**确实**崩了；
② 按模式发放的版本在三个 seed 上**确实**回到基线；③ 这个修法**本身**就站得住 ——
**一个不可能变化的字段不是信息**，而常数输入不是免费的：它是一个没东西可学的自由参数，
外加一行只能被突变浪费的权重。⇒ koth 的 obsDim 回到 **100**、genome 回到 **5324**。

### ⭐ 顺手量到一条噪声上界（这条比上面都有用）

D 与 C′ **共享同一批初始种群**（长度相同 ⇒ 同一条 rng 抽出同样的数），
唯一差别是那 100 个**内容完全相同**的字段的**排列顺序**。结果：
seed 1 四项全面变好（kills 1.98 → 2.87、首枪 9.3 → 6.7 s），seed 2 涨跌互见（accuracy .260 → **.343**，kills 3.14 → 2.95）。
⇒ **光是把输入重新排个序，就能在 2 个 seed 上制造出和「真效应」同量级的差**。
⛔ 以后任何 2-seed 的行为侧 A/B，效应量都不可信，只能读方向 —— 这是本仓第一次把这条量出来。

## [2026-09-13 03:40] C2 收尾：尸体的沉默上了探针，kill feed 的边界钉死  #ship #decision

C2 Build 清单剩下的三条，逐条落地：

**「死亡玩家不得继续给活人 radio truth」—— 实现本来就对，但没人守着。** 上探针 `A1-P26`：
同时动**三条**通道（comm 总线、脚步音频、队友开火 cue），断言**一个字段都不动**。
⭐ 正向护栏：同样的改动在**活着**的队友身上会动 `mate0.hp / firing / comm0 / comm1 / audio0.footstep`
⇒ 尸体的沉默是真的，不是「三条通道都死了」的空过。
（为什么本来就对：`hear()` 的配对循环两端都跳过死人 ⇒ 尸体不出声；comm/hp 在 V11 已清零；位置在 `A1-P24` 关闭。）

**kill feed 的边界。** `world.events` 带精确坐标与身份，它有两个合法消费者：观战器（它是**观察者不是玩家**，
可以看见一切）和 `hear()`（把开火变成枪声 —— 那是**物理**不是 feed）。
⇒ 在类型上标成 **SPECTATOR CHANNEL**，⛔ 任何 policy 都不得读。
**玩家真正拿到的 kill feed 就是人数通道**（`self.aliveMine` / `self.aliveEnemy` / `mate*.alive`）
加上他听见的东西 —— 全都不含「在哪」。⇒ C2 那条「kill feed without death coordinate」满足了。

**explicit round lifecycle：判定为已足够，真正缺的那半属于 E2。** `t` / `timeLeft` / `done` / `winner` /
armed 状态已经把回合状态完整表达出来了；还缺的是 **freeze-time / opening prior**，
而那是 E2（Team DNA / opening prior）的活 ⛔ 不在 C2 里做。
观战器那个滚动 kill feed **UI** 仍留在 `TODO.md` —— 它是可视化，不是世界规则。

⇒ **C2 的世界规则部分收齐**：26 条 probe（新增 P24/P25/P26 三条），88 测试，观测普查回到
**100 字段 / 97 legal / 3 truth-form / 0 hidden**。

## [2026-09-13 03:55] 坑 #17 第二次中招：目标区块按世界顺序发，红蓝变成两个玩法  #incident #decision

准备在 C1/C2 世界里训练时先做了两件事，两件都抓到东西。

### ① trainer 在 capture 模式下从来不分配攻守角色

`runMatch` 的 opts 能带 `attackers`，但 trainer 一次都没设 ⇒ **红方在它打过的每一场里都是攻方**。
后果两条：红方会变成**永久的攻方物种**（VISION §11.2 明禁）；每个 fitness 都是**两种不同工作的混合**，
比例由排程随机决定，而 SUBSTRATE §10.3 要求 fitness 聚合必须 side-balanced。
⇒ capture 模式下每场配对**发两次**（同对手、同 seed、角色互换），koth 不动。测试把它**数出来**。
⚠ 侥幸之处：capture 此前只有 reference bot 跑过，而**它们的角色是显式传进去的** ——
这条本来会一直潜伏到第一个 capture 种群已经分化出来。

### ⭐⭐ ② 目标区块按**世界** site 顺序发 ⇒ 红蓝看到的是两个不同的游戏

红方的 `obj0` 是它**右边**那个点位，蓝方的 `obj0` 是它**左边**那个。
团队帧（blue 的帧 = 世界转 180°）保证了其他通道的镜像对称，而我新加的这条通道**按世界索引发**，
直接把镜像破掉了。

**镜像测试实测**（同一个基因组放两边，唯一差别是谁当攻方）：
攻方胜率 **红攻 0/48 vs 蓝攻 15/48**；逐场看是同一个基因组**当蓝方进了点位并拿下，当红方一次都没进去**。
修好之后 **1/48 vs 0/48**（随机基因组几乎不可能拿下点位，这才是对的量级）。

⇒ 修法和坑 #17 当年一样是**自我相对**：每队的点位顺序按**它自己帧里的 x** 排序，不按世界索引。
（用位置排而不是写死「两个点位互为镜像」，所以换布局也成立。）

⚠⚠ **为什么现役镜像测试没抓住**：它跑在**单点位 koth** 下 —— 那个配置里这条通道**结构上不存在**。
⇒ 坑 #17 补上第二个实例和一条推论：**镜像测试必须在「这条通道真的存在」的配置下跑**，不是在默认配置下跑。
新测试 `reports the sites in each team's OWN frame` 在 capture + 双点位下逐特征比，并**先断言通道非零**。
⭐ 实测它有鉴别力：把 bug 放回去，它报 `slot 0 field obj0.dx: expected 0.8 to be less than 1e-6`。

⚠ **`runs/c1c2-capture-s{1,2}` 作废** —— 那两个 run 是带着这个 bug 训出来的，重跑。

## [2026-09-13 04:05] C1/C2 世界的第一个 baseline：目标终于被争夺了  #measure #ship

重跑（修掉镜像 bug 之后）`--mode capture --sites 2 --gens 40 --pop 16`，两个 seed：

| | seed 1 | seed 2 |
|---|---|---|
| 首枪 gen0 → 末期 | 33.8 s → **7.1–7.9 s** | 32.1 s → **8.3 s** |
| accuracy gen0 → 末期 | .084 → **.24–.30** | .077 → **.29** |
| **objectiveProgress** gen0 → gen39 | .089 → **.232** | .078 → **.347** |
| 末代冠军 vs 第 0 代（带 sighting ticks） | 红 80%/70%（387）· 蓝 60%/70%（232） | 红 100%/100%（498）· 蓝 50%/50%（1034） |

⭐⭐ **objectiveProgress 从 ~0.08 涨到 0.23–0.35 并稳住** —— 这是旧 KOTH 世界**从来没有过**的东西
（那边 zoneShare 四十代都钉在 0.01–0.04，而坑 #23 的读数是冠军对 rusher 0%）。
⇒ C1 那条「目标压力为 0」的诊断，在 C1+C2 之后**量到了反转**：现在**被优化的就是目标本身**。

⚠ 三条别过度解读：
- `objectiveProgress` 在角色互换之后是**两种量的混合**（攻方算 capture、守方算 defuse）。
  单场无歧义，聚合之后的含义是「离我这一份工作的完成度有多远」⇒ 回答「目标有没有被争夺」可以，
  ⛔ **不能**用它比较攻方和守方。已写进 `TeamMetrics` 的注释。
- seed 2 的 `meanR 0.464 / meanB −0.608` 是两个种群**真的分化了**（角色已平衡、观测已镜像对称、
  镜像测试在守着），这是共演化的正常内容，不是对称性 bug；但 ⛔ 别拿 mean 当强弱判据，走 cross-play。
- 两个 seed，⇒ 按坑 #27 只读方向不读效应量。

### 顺手修掉我自己脚本里的一个 bug

`compare.ts` 的参数解析是 `filter(s => !s.startsWith('--'))` —— 它**跳过 flag 但不跳过 flag 的值**。
两文件版本把前两个位置参数解构出来、默默忽略其余，所以 `--last 5` 一直被当成第三个文件而没人发现；
改成接 N 个文件之后它立刻去开一个叫 `5` 的 run。
⭐ 这是「旧代码靠**忽略**多余输入而不是靠**解析正确**撑着」的典型 —— 扩展它的那一刻账就来了。

## [2026-09-13 04:15] CHECKPOINT 轮转：191 行 → 79 行  #decision

这一晚 CHECKPOINT 从 A0 一路被追加到 191 行，里头有两天前的「现状（截至 2026-09-11 晚）」、
一整节已经被 C1 解决掉的「目标压力已归零」、以及一节基本是 VISION §14 / CLAUDE.md 的手抄副本。
按 checkpoint 纪律（硬上限 1–2 页，live 内容变 stale 就挤进 LOG）重写成 79 行：

- 删掉所有已入档的现场（都在 LOG 里了），「新 docs 先读」那节压成一句指向 `CLAUDE.md`（那边本来就有接手顺序）。
- 「工作纪律摘要」原有 13 条里有 9 条是 VISION/合同的复述 ⇒ 只留 4 条 checkpoint 级的，其余指过去。
  ⭐ 新增一条过去没写下的：`src/sim/nav.ts` / `src/brain/scripted.ts` / `world.events` 三者都是
  reference/analysis/spectator 专用，⛔ evolving policy 一个都不许碰。
- ⭐ 新增**六件仪器一张表**（leaks / crossplay / yardstick / inherit / recprobe / mapprobe / c1exit），
  每行写「回答什么」和「⛔ 别拿它干什么」—— 这一晚新增的五件工具之前散在五条 LOG 里，接手的人找不到。
- 「⚠ 动手前必扫」按**失败族**重排成五行，每行挂对应坑号，而不是按时间顺序堆。
- Ops 速查补一条实测：**macOS runner 排队可能十几分钟**，⛔ 别把「还没跑」当成失败（今晚踩过，一个 monitor 白等十分钟）。

## [2026-09-13 04:35] D1 重开：两个预测全错，而两个错法合起来是个更干净的结论  #measure #decision #deadend

预测冻在 `runs/d1-reopen-predictions.txt`。三臂，capture + 双点位，40 代 pop 16，两 seed。

### P1 证伪：在这个世界里，world 的 3 秒记忆**本来就不承重**

A（`mem3`）vs B（`mem0`），**初始种群相同**（`memorySeconds` 不改 genome 长度 ⇒ 真配对）：

| | kills | 首枪 | **objectiveProgress** |
|---|---|---|---|
| s1 A → B | 2.11 → 1.66 | 8.6 → 10.9 | **.231 → .284（升）** |
| s2 A → B | 1.48 → 1.46 | 9.1 → **6.0（更快）** | **.336 → .355（升）** |

⇒ 拿掉世界记忆**几乎没有代价**，而且在**目标**这项指标上两个 seed 都**略升**。
对照旧 KOTH 世界：同一个改动把首枪从 5.8 拖到 9.8、5.3 拖到 14.8，kills 腰斩。
⭐ 解释：公开目标状态**替代**了记忆的大部分用途 —— 知道点位在哪、哪个被 armed，就不太需要记得敌人刚才在哪。

### P2 证伪（这才是决定性的那条）：recurrent state **仍然**是学出来的常数

`npm run recprobe`，四档，两 seed × 三个对手：**alien**（从**另一场比赛**抓来的常数，实测 `var` = **0.000**）
与 **live**（`var` 0.57–1.45，真的在动）打平，**而且多数格子里 alien 更好**：
s2 对 rusher live **一枪未发**（首枪 40.0 s）而 alien 12.4 发 / 0.83 kills；
s2 对 camper live 0 kills / 12.3 发，alien **0.92 kills / 38.1 发**；s1 三个对手 alien 全面不差于 live。

⇒ **即使在这个为目标付钱的世界里，那条通道仍然被当常数偏置用。**
⛔ 我在预测文件里写了「若 alien 仍与 live 打平，就是 reframe 错了，我直接这么报，不去找第三个世界改动」——
照此执行：**reframe 没有被证实**。

### 两个错法合起来的结论（比「D1 失败」干净得多）

**在这个世界里，「关于看不见的敌人的时序信念」在两个可能显现的位置上都不值钱**：
世界替你记（A vs B：拿掉它免费）、或者让脑子自己学着记（C：通道被用成偏置）。
⇒ D1 的前提 —— private temporal belief 是承重的 —— **本世界不支持**。

⚠ 但「不值钱」和「找不到」是两件事，而上面的实验**分不开**：
per-weight 高斯突变对「跨时间的信用分配」没有任何梯度压力，40 维 Elman 可能只是**搜不到**
（这条我在 D1 第一次的预测文件里就写过，不是事后找的借口）。
⇒ **下一刀不是换 GRU 也不是再改世界，是一个不需要训练就能分开这两者的实验**：
手写一组**记忆档位不同的 reference bot**（记 0 / 1 / 3 / 10 秒），在同一个世界里对打。
胜率随记忆时长上升 ⇒ 世界**确实**付钱、问题在搜索；不上升 ⇒ 确实没有需求。
⭐ 这正是 C1 立下的方法：**先问世界允许/奖励什么，再问进化找不找得到。**

## [2026-09-13 04:55] 世界为记忆付多少钱：付一点，而且远不如站对位置  #measure #decision

`npm run memdemand`：不训练任何东西，直接问世界。攻方是固定的 reference bot（直打 A / 直打 B / 假打 A 真打 B），
守方是 `MemoryHunterPolicy(h)` —— 看得见就追，失去视线后**继续朝最后看见的位置走 h 秒**，超过就回自己的点位。
扫 h，看守方胜率跟不跟着走。世界设成 `memorySeconds 0`，**唯一在记忆的就是这个 bot 自己**。

3 张地图 × 8 seed × 两种角色分配 = 每格 48 场：

| 守方记忆 | 直打 A | 直打 B | 假打 A→B | **mean** | armed | sight |
|---|---|---|---|---|---|---|
| **不追，只守点位（参照）** | 15% | 33% | 65% | **38%** | 99% | 972 |
| 0 s | 8% | 27% | 2% | 13% | 88% | 730 |
| 0.5 s | 13% | 29% | 2% | 15% | 85% | 731 |
| 1 s | 15% | 29% | 6% | 17% | 83% | 723 |
| **2 s** | 13% | 46% | 13% | **24%** | 76% | 745 |
| 3 s | 15% | 35% | 13% | 21% | 80% | 748 |
| 10 s | 13% | 33% | 13% | 19% | 81% | 736 |

### 结论（窄的那个，不是宽的）

1. **记忆确实有价值，但又小又会饱和**：13% → 24% 在 ~2 s 达峰，之后回落。
   ⚠ 每格 48 场，n=48 的二项噪声约 ±12pp ⇒ 这个峰**在显著性边缘**，只读方向。
2. ⭐⭐ **决定性的那个数：完全不追、只站点位的守方拿 38%，高于任何一档记忆。**
   ⇒ 在这个世界里**追击本身就是错的**，而追击所需要的记忆，价值自然低于「别去追」。

⇒ 回答 D1 留下的那个问题：**这个世界为时序信念付的钱很少，而且被「站对位置」的价值盖过。**
这是**需求**侧的限制，不（主要）是搜索侧的。进化把 recurrent 通道当常数用，和「本来就没多少可赢」是一致的。

⚠ **别把这条读宽**：它测的是**记忆的一种用法**（追你刚失去视线的人）和**一个 bot 原型**。
没测的用法还有：记住敌人这一轮承诺了哪个点位、记住队友死在哪一侧、记住他换弹过。
⇒ 正确的窄结论是「**追击式**记忆在这里不值钱，且不如保持站位」，⛔ 不是「记忆没用」。
⇒ 若要让 D1 真的重要，世界需要一种**记忆回报高于站位**的局面（更长的视线、只短暂可得的信息、
或者奖励拦截 rotation 的目标结构）—— 这是 ROADMAP 该登记的设计要求，不是再调一次 brain。

### ⚠ 第一版这个探针在每个格子都读 50%，第三次同一个形状

v1 是 hunter 对 hunter 的循环赛 —— 两个都在追，于是**谁也没能独占点位够久把它 arm 掉**，
结果**守方恒胜**，决定胜负的只剩「谁被分配当守方」，而每格两种分配各半 ⇒ **恰好 50%**。
⇒ 坑 #26 今晚第三次（C1c 的四连 FAIL · `mid` 指标 · 这一条）：
**读到一个整齐得可疑的数（全 50%、全 0、两行逐位相同），先证明测量到达了断言。**
v2 把问题搬到「记忆真的有活干」的位置：攻方固定去拿点，只扫守方的记忆档 —— armed 率 76–99%，信号才出现。

## [2026-09-13 09:40] D2a：radio 从「第二条 exact-state bus」变成有限的无线电  #ship

VISION §7.2 的原话是：无限连续 float、每 tick 发送，会把无线电退化成另一条 exact-state bus。
现状正是如此（`commDim: 2`，每 tick 两个 tanh 浮点，队友直接读）。D2a 把通道本身换掉，**默认全关**。

**三个旋钮**（`SimConfig`，默认 `0 / 1 / 0` = 原行为逐字节不变）：
- `commTokens` — 量化成 `2*tokens+1` 个符号（±k/tokens 加 0）。⭐ **0 就是静默**，
  而静默是靠死区「说得足够小」**自己选**出来的，不是一个玩家无法表达的值。
- `commIntervalTicks` — 一条消息**最多**每 N tick 才能重新决定；期间上一次的决定继续发。
- `commDelayTicks` — 队友听到的是**这么多 tick 之前**说的话。

⭐ **设计取向：量化发生在「发出来的东西」上，不在动作空间上。** 和感知层同一个套路 ——
视觉 percept 是经过量化器报出来的，而不是把眼睛改掉。⇒ ACT_DIM 不变，genome 不作废。

⭐⭐ **「说的」和「听到的」现在是两个对象**：`Agent.comm`（这一 tick 的意图）→ `Agent.commSaid`
（真正离开电台的符号）→ `World.commWire`（一条长 `delay+1` 的线）→ `heardComm(i, c)`（听者读最旧那格）。
`commActivity` 指标改读 `commSaid`（描述**电台**而不是没说出口的冲动；`commTokens: 0` 下两者恒等 ⇒ 默认配置指标不变）。

### 一条现役测试变红了，而它变红正是这一刀的意义

`binds comm fields to the radio` 原本直接设 `agents[1].comm[0] = 0.5` 就能让 `mate0.comm0` 动 ——
因为观测以前直接读 `agent.comm`。现在**说话者当下的意图不可观测**（要经过量化器和延迟线）。
⇒ 测试改成断言**两件事**：设 `comm` **不可以**让观测动（否则延迟和量化都是装饰），
推上电台之后**必须**动。⭐ 这不是把测试改去迁就实现，是把测试从「comm 是 agent 上的一个字段」
改成「comm 是一条有时延的信道」—— 后者才是 D2 要的东西。

承重测试是 ⭐ **「N tick 之前说的话现在才到，在那之前一片安静」**：off-by-one 或者「延迟其实没生效」
都会让这条通道**看起来**像无线电而实际仍是即时状态总线。另外三条：有限字母表（实测恰好 `{-1,-0.5,0,0.5,1}`）·
死区能表达静默 · 间隔期间保持发送 · **默认通道逐值不变**。96 测试绿，leaks 闸 exit 0。

⛔ **还没有任何关于「语言涌现」的说法**，一个字都没有。D2 的 Strong-claim gate 要求
sender-state relation + receiver response + intervention 三层证据齐 ⇒ 那是 D2b（`npm run radio`）的活，
在那之前 ⛔ 不训练也不解读。

## [2026-09-13 09:55] D2b：电台上到底有什么——满音量的噪声  #measure #ship

`npm run radio` 把 D2 的 Required analyses 做成一条命令。**三层分开**（VISION §7.4 的 claim ladder）：
sender 侧 = 符号熵 / 静默率 / 与候选指称的互信息；receiver + intervention 侧 = 三种消融，
每种都**改写 brain 即将读到的那一格**（新增 `stepMatch(..., afterObserve)` 这个缝，⚠ 仅分析用，训练路径不传）：

| 消融 | 做什么 | 为什么要有它 |
|---|---|---|
| `off` | 队友消息全部换成静默 | 只能证明「这块输入承重」 |
| `shuffled` | 换成**本 run 自己录下来**的消息池里的值 | 分布不变、与情境的相关性被破坏 —— 坑 #24 要的 in-distribution 对照 |
| `alien` | 换成**另一个 run 的冠军**录下来的消息 | 若异族词汇一样好用，线上的东西就不是 team-specific 的 |

### 基线读数（capture 世界的冠军，电台还是**改造前的连续浮点**）

**sender 侧：符号熵 2.07 / 最大 2.32 bits，静默格占 20%（≈ 1/5 均匀），70828 个符号。
与五个候选指称的互信息全部是 0.003–0.020 bits = 熵的 0–1%。**

⇒ ⭐⭐ **这条「电台」几乎满熵地忙着，却几乎不携带任何关于说话者所知情况的信息 —— 它是满音量的噪声。**
正是 VISION §7.2 预言的「退化成第二条 exact-state bus」的另一种失败形态：连 state bus 都不是，是随机数广播。

**receiver / intervention 侧**（每格 8 场，⚠ 只读方向）：

| 电台 | kills | shots | 首枪 | **objective** |
|---|---|---|---|---|
| normal | 2.13 | 31.9 | 9.5 | **0.438** |
| off | 2.44 | 33.1 | 11.5 | **0.188** |
| shuffled | 2.19 | 33.9 | 8.0 | **0.188** |
| alien | 1.50 | 22.5 | 10.2 | 0.250 |

⇒ 听者的行为**确实**依赖这条输入（objective 0.438 → 0.188），但**消音和打乱的伤害一样大**。
配上 1% 的互信息，最自然的解读是：网络依赖的是进来的**那些具体数值与其余观测的自洽**，
而不是这些数值**关于情境**的含义。

### ⚠ 三条必须跟着一起读的限制

1. **互信息只对着我列的五个候选**（见敌 / 在点位里 / 有点位被 armed / 换弹中 / 半血以下）。
   ⛔ 「对这五个是 1%」不等于「对一切都是 1%」——它完全可能在编码我没列的东西（自己的朝向、要去哪个点位）。
2. **这些冠军是在连续电台下训练出来的** ⇒ 这是「改造前」的读数。
   D2 的问题（「队友是否真的会选择什么值得说」）只能问一个**在有限电台下训练出来**的种群。
3. **team-crossplay（A 队 speaker 配 B 队 listener）现在做不了**：一队共享一个网络 ⇒ speaker 和 listener
   是同一个对象。⛔ 这条要等 **V7/E1（player identity split）** 才解锁，已写进脚本输出里。

⇒ 下一步：用 `--tokens 2 --comm-interval 5 --comm-delay 3` 训一个种群，再跑同一个分析对比。
⛔ 在那之前不说任何关于「语言」的话。

## [2026-09-13 10:15] 这个世界为**通信**付大钱，为记忆付小钱 —— D2 的前提成立，堵点在 E1  #measure #decision

先把 D2b 用在**有限电台下训出来的**种群上（`--tokens 2 --comm-interval 5 --comm-delay 3`，40 代，两 seed）：

| | 符号熵 | 静默格 | MI（五个候选指称） |
|---|---|---|---|
| 连续电台的冠军 | 2.07 / 2.32 bits | 20%（≈均匀） | 0.003–0.020 bits = **0–1%** |
| **有限电台的冠军** | **2.08 / 2.32** | **23%** | 0.004–0.018 = **0–1%** |

⇒ **把通道限死（5 符号 / 5 tick 一次 / 3 tick 延迟）并没有让它被有意义地使用。** 熵照样接近满，
MI 照样 1%，「静默是一种选择」基本没被拿起来（23% 对均匀的 20%）。
消融侧更糟：`off` 的 objective **.458** 比 `normal` 的 **.292** 还高 —— 听这条电台甚至略微有害。

### 然后用 memdemand 的方法问世界：通信本身值不值钱

给守方加一档 **telepathy**（`TeamSightHunterPolicy`：瞬间、免费、完美地共享每个队友的视野 ——
这是任何电台的**上界**，不是合法策略）。3 图 × 8 seed × 两种角色，每格 48 场：

| 守方 | 直打 A | 直打 B | 假打 A→B | **mean** | armed |
|---|---|---|---|---|---|
| 私有视野，0 s 记忆 | 8% | 27% | 2% | **13%** | 88% |
| 私有视野，2 s（记忆最优档） | 13% | 46% | 13% | 24% | 76% |
| 不追、只守点位（参照） | 15% | 33% | 65% | 38% | 99% |
| **telepathy，0 s 记忆** | **58%** | 35% | 33% | **42%** | **58%** |
| **telepathy，10 s 记忆** | **71%** | 35% | 33% | **47%** | **58%** |

⭐⭐ **共享视野值 +29pp（13% → 42%），而且 telepathy + 记忆（47%）是唯一打得过「不追」参照（38%）的配置。**
机制看得见：telepathy 把 armed 率从 76–88% 压到 **58%**，sight 从 ~730 升到 ~1350 —— 守方真的找得到人了。

### 两个结果并排，结论就硬了

| | 世界付的钱 | 进化用了吗 | 所以堵在哪 |
|---|---|---|---|
| **记忆**（追击用法） | 小：13% → 24% 峰，且**不如不追** | 没用（当常数偏置） | **需求侧** ⇒ D1 不再算债 |
| **通信**（共享接触） | **大：13% → 42–47%** | 没用（满熵噪声、MI 1%） | **不是需求侧** |

⇒ **D2 的前提被世界支持了**（这和 D1 正好相反）。那么「电台没被用起来」的原因更可能在
**搜索 / 架构**，而架构那个候选是具体的、而且 ROADMAP 里早就有：**V7「一个脑子穿五个身体」**。
一队共享一个网络 ⇒ 同一批权重既要学会「说的时候怎么编码」又要学会「听的时候怎么解码」，
而且没有个体身份可以分化。**E1（player identity split）正是这个 phase，也正是 team-crossplay 解锁的地方。**
⇒ ⛔ 不要再调电台参数、也不要加训练预算去救 D2；下一根承重杠杆是 **E1**。

⚠ **telepathy 是上界，而且是个宽松的上界**：它共享的是**精确坐标**，而一条 5 符号的电台只能传一个符号。
⇒ 正确的说法是「**共享接触信息**在这里最多值 29pp；一条有限电台能拿到其中多少，未知」。

### ⚠ 过程上的一个滑档（和坑 #26 同一个家族，只是这次是我的工具）

我的批量编辑脚本在某条 pattern 匹配失败时 `sys.exit`，而那发生在 `open(...,'w')` **之前** ——
于是**一个字都没写**，但我**没检查就直接跑了测量**。结果两行 telepathy 读数完全相同（都是 19%），
因为 `1000` 和 `1002` 被旧代码当成了「记忆 1000 秒 / 1002 秒」＝同一回事。
⭐ 规矩补一句：**改完工具要先确认改动真的落盘了（grep 一下新符号），再信它的输出。**
那两行「一模一样」本身就是 #26 说的那个信号。

## [2026-09-13 14:30] session 收尾：LOG 轮转 + TODO 剪枝 + 快照压回 94 行  #decision

**LOG 轮转**（阈值 ~600 行，live 到了 1375）：切在 **C1 关闭**这个 programme 边界上 ——
2026-09-11 22:15 → 2026-09-13 00:10 整段移进 `LOG-archive/LOG-2026-09-11-2215-to-2026-09-13-0010.md`
（A 程序 → 换尺子 → inherit → yardstick → D1 判决 → C1 全程）。live 现在 **412 行 / 12 条**，从 C2 开头起。
契约四件齐：标题写明起点（自查：标题声称 00:25 == 首条 `[2026-09-13 00:25]` ✅）· 档名带日期区间 ·
写明 `tail -n 150` · 定位 glob **同时命中 live 与两个归档**（实测 `grep -n '^## ' LOG.md LOG-archive/*.md` = 57 条）。

**TODO 删掉两条**（做完 / 失效，理由留在这儿）：
- 「旧 comm channel 的语义 census 可作为 D2 前 baseline」⇒ **做完了**，就是 `npm run radio` 在连续电台冠军上
  那次读数（MI 对「见敌 / 在区内 / 换弹中」全是 0–1%）。
- 「保留一次旧 baseline 长跑：pop 24、≥200 代」⇒ **失效**：它要 census 的那个世界（单点位 KOTH +
  连续电台 + world 代管记忆）已经被 C1/C2/D2 换掉三次了，长跑出来的数字没有可比对象。⛔ 不保留。

**TODO 新增五条**，其中三条是这一晚新解除的前置（kill feed 的合法事件源已钉死 · 量化电台已 ship ·
C1 关闭 ⇒ 地图池轮换可以做了），另两条是自己挖出来的坑：`radio.ts` 的 shuffled/alien 用**固定步长**
走消息池而不是随机抽（池子有时间结构时可能仍带相关性）· `objectiveProgress` 角色互换后是**两种量的混合**。
⭐ 另加一条真缺口：**双点位的观战渲染** —— `scene.ts`/`heatmap.ts` 只画 site 0，`siteCount: 2` 下画面是错的
（默认单点位所以没发出去，但训练已经跑在双点位世界里）。

**快照**：cursor 那段被我写成了 LOG 语气（40 行叙事），压成「一张两行的表 + 下一刀 + 指针」，
全文指向 `docs/ROADMAP.md` 的 D1/D2 节。仪器表从六件补成**八件**（新增 `memdemand` / `radio`），
并在表头写明**最新那两件回答的是「世界付不付钱」**。103 → **94 行**。

**接手点 = E1（player identity split）/ V7。** 理由不是「roadmap 排到这里」，是 D 程序量出来的：
世界为**通信**付 +29pp 而进化没用（⇒ 不是需求侧，是一队共享一个网络这件事），为**记忆**只付一点点
（⇒ 需求侧，D1 不再算债）。

## [2026-09-13 14:57] E1 probe-first：共享大脑里已经有「人」，不只是「身体」—— 施工前提被推翻  #measure #decision

**问题**：E1 问「共享网络的 slot 分化 vs 真正的个体参数，差在哪」。动手造 player block 之前先量：**现在**的五个人是什么。
**混淆**：`map.spawns[team][slot]` 让 one-hot 身份和出生位置是**同一个标签** ⇒「slot 2 打法不同」完全可以只是几何。

**仪器** `npm run identity`（纯分析，不训练、不动 sim）：每 tick 在 observe 之后把 one-hot 旋转 k 位，
每个种子跑**全部** k = 0..4（拉丁方）⇒ 每个身体带两个互相平衡的标签：**body**（出生位置）/ **carrier**（网络被告知的身份）。
每人每场一条行为向量（own-frame 位置、移动/瞄准占比、射速、存活、离队友距离、在点位、偏左点位、电台均值、击杀），
按 (run, 颜色, 攻守) 分格，留一种子 nearest centroid，旁边一列**随机标签**对照。4 个 run × 2 色 × 2 角色 = 16 格，每格 16 种子。

⚠ **过程滑档（已写成坑 #28）**：v1 每场只抽一个 k，训练折里同 k 的种子会把出生几何以「固定改名」的形式递给 carrier 分类器。
n=3 冒烟读 carrier 47–67%，和泄漏分不开 ⇒ 正式跑之前改成拉丁方，并在 `runs/e1-identity-predictions.txt` 追加说明（预测不改）。

| 16 格范围（chance 20%） | aligned | body | **carrier** | random |
|---|---|---|---|---|
| 全特征 | 66–99% | 47–64% | **36–66%** | 6–25% |
| 去掉 comm0/comm1（事后分解） | 49–96% | 43–65% | **31–55%** | 7–24% |

**对冻结预测**：P1（aligned ≥ 50%）16/16 ✅ · P2a（body > carrier）**10/16 ❌**（要 ≥ 12）· P2b（carrier ≤ 35%）**0/16 ❌**。
事后去掉电台特征：14/16 · 6/16 —— P2b 仍 ❌。随机列 ≈ chance ⇒ 分类器没漏折。
**权重侧佐证**：冠军第一层的 slot 列 RMS 有的涨到 0.19–0.27（init ≈ 0.095，在 109 个输入里排第 3–23），有的留在 init 附近
⇒ 进化**有选择地**放大了部分 slot 的身份输入。

### 解读

⭐⭐ **slot one-hot × 第一层权重 = 每人一条 40 维 bias ≡ ROADMAP E1 候选「shared team genome + per-player compact bias vector」——它已经 ship，且被用上了。**
去掉电台特征后 carrier 信号还在，主要落在 aiming / moving 上 ⇒ 不只是电台输出的常数偏移。
⇒ P2「五个身体不是五个人」**错了**。按冻结的 P3：**先 reframe，不造 player block。**

连带推翻两条之前写进 CHECKPOINT / ROADMAP D2 的理由：
1. **「E1 解锁 team-crossplay」是错的**：`stepMatch` 虽然按队给 Policy，但 `act` 是逐 agent 调用 ⇒ 按 slot 分发的包装器就能
   让 A 当 speaker、B 当 listener，不需要拆 genome。（读 `match.ts` / `policy.ts` 确认，**还没实跑**。）`radio.ts` 的输出已改。
2. **「同一批权重既要编码又要解码」站不住**：MLP 里「说」是输出层的 comm 行，「听」是第一层的 `mate*.comm*` 列，本来就是不同权重；
   共享的只是**跨队员**的参数。⇒ 「电台没被用上」的**架构**假说被削弱，剩下 **search** 与 **channel** 两个候选。

**E1 exit 原文不是门**：「analytics 能从行为识别同一队不同 player，而不是只靠 slot id」—— 现在的共享大脑就能过。
按 ROADMAP §3（发现假设错 ⇒ 收紧，不降低）改成：候选架构要在同一把尺子上**超过共享大脑基线**，并说清超过的是表达还是遗传。

### 下一刀

**D2c：问世界「一条合法的有限电台值多少」**。telepathy 共享的是精确坐标，只是宽松上界；D2 自己写过「有限电台能拿到多少，未知」。
memdemand 同法：手写 reference bot 只通过真实的 `commSaid → commWire → heardComm` 通道说话和听，扫符号数 / 间隔 / 延迟。
值钱 ⇒ 进化找不到是 search 问题；不值 ⇒ 通道本身太窄，是 D2 的设计问题，与 E1 无关。

⚠ 限制：nearest centroid + 11–13 个手工特征（低 carrier 只说明**这个**分类器找不到）· 旋转后的 (出生位置, one-hot) 组合是训练中没见过的联合分布 ·
4 个 run 都在 mapSeed 7 · 只看末代冠军。

## [2026-09-13 15:47] D2c：一条合法的一符号电台就拿回 93% 的共享接触价值 —— channel 不是瓶颈  #measure #decision

**为什么问**：E1 probe 拿掉了「一个脑子穿五个身体」的架构借口，剩 search 与 channel。telepathy 共享精确坐标只是上界 ⇒
先问世界（VISION §1.1「世界缺不缺这条腿」、§14.1「缺腿还是没选」）。预测冻结在 `runs/d2c-radiodemand-predictions.txt`。

**仪器** `npm run radiodemand`：`RadioCallerPolicy` = `MemoryHunterPolicy(0)` + **一个符号**，只走真实的量化 / 间隔 / 延迟链路。
speaker：看见敌人时报「他离我方左 / 右点位更近」，否则静默；listener：只读自己观测里的 `mate*.comm0`，有人报点就去那个点位。
攻方与种子同 memdemand（直打 A / 直打 B / 假打 A→B，3 图 × 8 种子 × 两种角色 = 每行 144 场）。
为此给 `MemoryHunterPolicy` 抽了一个 `idleSite` 钩子；**回归**：重跑 memdemand 的 site-holder / 0s / telepathy 0s，逐格与旧输出相同。

| 守方 | 直打 A | 直打 B | 假打 | **mean** | armed | calling |
|---|---|---|---|---|---|---|
| 私有视野 0s | 8% | 27% | 2% | **13%** | 88% | — |
| 电台静音（对照） | 8% | 27% | 2% | 13% | 88% | 13% |
| 电台 5 符号 / 每 tick / 无延迟 | 46% | 42% | 33% | **40%** | 60% | 45% |
| **电台 5 符号 / 5 tick / 3 tick 延迟（D2 训练配置）** | 54% | 38% | 27% | **40%** | 60% | 36% |
| 电台 3 符号 / 15 tick / 8 tick 延迟 | 33% | 42% | 6% | 27% | 73% | 19% |
| telepathy 0s（上界） | 58% | 35% | 33% | **42%** | 58% | — |

G = 30pp。拿回：5 符号无延迟 **93%** · D2 配置 **91%** · 薄电台 **49%**。
**对照**：静音 caller 与私有视野 144/144 场逐场相同（winner / sight / ticks）⇒ 电台是它唯一多出来的信息。
「连续电台」一行与 5 符号无延迟逐格相同：协议只发饱和的 ±0.995，两种设置解码一致，是预期不是工具问题。
**对冻结预测**：P1（≥ 50% G）✅ 93% · P2（D2 配置与理想电台差 ≤ 5pp）✅ −1pp · P3（薄电台 ≥ 33%）✅ 49%。

### 结论（按冻结的许可）

⭐⭐ **世界有需求（memdemand）、通道够用（本条）、架构借口已拿掉（E1 probe）⇒ D2 的零结果是 search / selection 问题。**
⛔ 不重新设计电台；VISION §7.1（语义由队友共同训练学会）+ §10（不给战术发糖）⇒ ⛔ 也不加通信奖励、不预设符号语义。

**下一刀候选（未做，写进 ROADMAP §4）**：通信是双边协调。只改 speaker 或只改 listener 的单边突变，可能**永远不被选择**（没有台阶）。
量法：对现有冠军只突变 comm 输出行 / 只突变 `mate*.comm*` 输入列 / 两边一起，看 team fitness 变化的分布。

⚠ 只证明了**一个**协议点：它说明够用的电台存在，不说明进化能找到的协议长什么样 · 攻方是手写 bot，不是进化出来的对手。

## [2026-09-13 16:32] D2 search 侧 · 台阶探针：随机突变看不到台阶 —— 但这把尺子的分辨率没过关  #measure #incident

**仪器** `npm run commstep`（预测冻结在 `runs/d2-commstep-predictions.txt`）：d2-radio 两个 seed 的 4 个末代冠军，
每个突变体只动一类权重，对同 run 另一色的冠军打双角色；benefit = 训练种子**和**留出种子上都赢过未突变的冠军。
类别：listener（第一层 `mate*.comm*` 列，σ 0.1）/ speaker（comm 输出行，σ 0.2）/ 各自同尺寸的非 comm 对照；
外加 `informed` 上下文：队友消息全换成**有信息的**一符号报点，问「假如说话者已经有信息，突变听者会不会被选中」。
每类 24 个突变体 × 4 冠军 = 96。

| 合计 benefit | 实验类 | 同尺寸对照 |
|---|---|---|
| listener（evolved） | 20% | 14% |
| speaker（evolved） | 17% | 15% |
| listener（informed） | 20% | 19% |

inert（突变完全不影响比赛）0%。**对冻结预测**：P1（evolved 下无特殊台阶）✅ 6pp / 2pp · P2（informed 下出现台阶）**❌ +1pp / 0pp**。

### ⚠⚠ 许可暂停：按坑 #26 先问测量有没有到达断言 —— 没有证据说到达了

1. **六类读数在同一个冠军内同起同落**：s1 蓝方所有类 25–63%，s1 红方所有类 0–8%。
   决定 benefit 的是「这个冠军对这个对手离局部最优多远」，不是突变落在哪 —— 实验类和对照类**分不开**，正是尺子看不见类别差的样子。
2. `informed` 上下文本身把 s1 红方的基线从 **1.104 砸到 −0.540**（没见过的输入分布），此后 listener 与对照都在回归均值（train>0 67% / 79%）。
3. 12 场的 team fitness 标准误与单个突变的真实效应同量级，而 benefit 是**符号**判定。
⇒ P2 的失败**不能**按冻结许可读成「listener 侧学不会」，它同样兼容「有台阶，但这把尺子看不见」。已补进坑 #26 作第 ④ 例。

### 下一刀：让进化本身当仪器 —— listener uptake

两个种群训练时听到的队友消息全被换成有信息的一符号报点（分析专用脚手架），配置与 seed 抄自 d2-radio（genome 长度相同 ⇒ 同一批初始种群）。
读数在同一上下文里比 **normal / flipped（左右对调：时机与静默不变、内容错）/ off**，对照组是 d2-radio 的冠军。
学会听 ⇒ 缺的台阶在 speaker 侧；学不会 ⇒ listener 侧的可学性 / 容量 / 预算才是堵点。预测冻结在 `runs/d2-uptake-predictions.txt`。
顺带：报点协议只留**一份**定义 = `src/brain/scripted.ts` 的 `siteCall`（`RadioCallerPolicy` 用它说话），`src/probe/radioCall.ts` 只放注入器并 import 它，commstep / uptake 共用。
⚠ **#incident**：第一版把协议放进 `src/probe/` 再让 `scripted.ts` 去 import —— **方向反了**，T8 分析防火墙测试（`tests/leak.test.ts`：sim / brain / evo / worker / render / ui 不得 import probe）当场变红。闸起作用了；改成 probe → brain 之后转绿，radiodemand 输出逐字不变。

## [2026-09-13 16:47] D2 search 侧 · listener uptake：说话者完美，40 代也没学会听 —— 堵点在 listener 侧  #measure #decision

**实验** `npm run uptake`（预测冻结在 `runs/d2-uptake-predictions.txt`）：配置与 seed 抄自 d2-radio，训练中两个种群听到的队友消息
全换成**有信息的**一符号报点（`src/probe/radioCall.ts`，分析专用脚手架）。两个 seed 的初始种群都**断言**与对照相同 ✓（坑 #27）。
读数：每个冠军 vs 同 run 另一色冠军，24 种子 × 2 角色，被测队听到 normal / flipped（左右对调：时机与静默不变、内容错）/ off。

| 臂 | Δcontent = normal − flipped（4 个冠军） | off − normal |
|---|---|---|
| INFORMED（训练时就听完美报点） | −0.188 · −0.038 · −0.061 · −0.020（**0/4 为正**，均值 −0.077） | +0.283 · +0.033 · +0.051 · −0.021（off 更差只有 **1/4**） |
| CONTROL（d2-radio 冠军） | +0.271 · +0.014 · −0.450 · −0.090（均值 −0.064） | +0.136 · −0.107 · +0.064 · −0.486 |

**对冻结预测**：P1（informed 学会用内容）**❌** · P2（对照合计 |Δ| < 0.10）✅ —— 但只是 +0.27 与 −0.45 正负抵消 · P3（informed 离不开这个输入）**❌**。

**坑 #26 检查**：报点确实送到（calling 7–13%，off 模式断言为 0）；同样的编辑能把对照冠军的 fitness 推 ±0.45 ⇒ 读数**能**动；
informed 冠军 3/4 个 |Δcontent| ≤ 0.06、|Δoff| ≤ 0.05 ⇒ 它们确实没在听。
⚠ 真弱点：s2 那一对胜率 98% / 2%，**贴着天花板**；s1 红方听真话反而比听反话 / 不听更差。

### 结论（按冻结许可）

⭐ **就算说话者完美，40 代也没长出听者 ⇒ 堵点在 listener 侧（可学性 / 容量 / 预算），不是 speaker 侧的鸡生蛋。**

### 下一刀：先排除预算

三个候选里先查**预算**：它既不改世界也不改大脑（VISION §14.1 一次一根杠杆）。**记忆排最后**：radiodemand 的手写听者没有任何记忆就拿到 91%。
同一 informed 臂训 **120 代**。读数在跑之前改成「同臂所有 run 的另一色冠军都打一遍」，避开天花板，并用它**重读 40 代基线**（P0）。
预测冻结在 `runs/d2-uptake120-predictions.txt`。

## [2026-09-13 18:00] D2 listener 侧 · 120 代：一条血统学会了听（翻转报点胜率 67% → 2%），另一条没有 —— 到了人工闸门  #measure #decision #gate

**运行**：同一 informed 脚手架训 120 代（`runs/uptake-informed120-s{1,2}`），初始种群断言与 d2-radio 相同 ✓。
⚠ **#incident**：两次被宿主以内存不足杀掉（本机无 swap，pageouts 7 万+）；第二次起 `uptake train` 每 10 代存快照，从第 80 代续跑完成。
RSS 157–323 MB 基本持平 ⇒ 是宿主压力，不是进程泄漏。

**读数 v2**（跑之前定的：对手取同臂所有 run 的另一色冠军，避开 98% / 2% 天花板），每模式 24 种子 × 2 角色 × 2 对手 = 96 场：

| 臂 | Δcontent = normal − flipped（s1R · s1B · s2R · s2B） | off − normal | 合计 Δcontent |
|---|---|---|---|
| INFORMED-120g | +0.215 · **+1.357** · −0.158 · +0.001 | −0.245 · −0.863 · −0.055 · −0.001 | **+0.353**（3/4 为正，off 更差 4/4） |
| INFORMED-40g | +0.053 · +0.090 · +0.094 · −0.041 | +0.170 · −0.088 · +0.167 · −0.010 | +0.049 |
| CONTROL-40g | +0.091 · +0.244 · −0.347 · −0.076 | +0.067 · −0.137 · +0.030 · −0.303 | −0.022 |

**对冻结预测**（`runs/d2-uptake120-predictions.txt`）：P0（40 代零结果在新读数下成立）✅ +0.049 ·
P1（我的预期「120 代仍学不会」）**❌** —— 许可门槛全过 · P2（对照 |Δ| < 0.10）✅ −0.022。

⚠ **合计几乎全靠 s1 蓝方**：胜率 normal **67%** / flipped **2%** / off **30%** —— 目前 D 程序里最强的因果干预读数（内容错了比没有更糟 ⇒ 它在用报点**说了什么**）。
去掉它，其余三个均值 +0.019。**seed 2 两个冠军都没学会**；s2 蓝方的「+0.001 为正」是凑数。

**坑 #26 检查（s2 蓝方三种模式 fitness 逐位相同 −0.568）**：一次性诊断 —— comm 列权重 RMS 0.224（其他列 0.233）、第一层饱和 5%、
同一场里 normal 与 off 的轨迹在第 140 tick 分叉 ⇒ 它**有**响应；逐场比 96 场：fitness 只在 11–12 场不同、winner **一场都没变**。
⇒ capture fitness 是**量子化**的（胜负 ±1 + 伤害以 26 为步长），轨迹变了结局常常不变，于是均值逐位相同。已补进坑 #26 ⑤。

### 结论（按冻结许可，收窄）

⭐ **听是学得会的，但慢，而且看血统**：完美说话者 + 3× 预算，2 条血统里 1 条长出了真正使用内容的听者。
真实 D2 要说和听**同时从零**长出来，只会更难。

### ⛔ 人工闸门：下一根杠杆不由我单独定

1. **只加预算，在真实电台上长跑**（不改世界 / 大脑 / 优化器）—— VISION 零冲突；但本机 120 代 ≈ 27 分钟 / seed 且会被内存杀，
   两边都要从零长出来，几百代量级，**结果为空也不构成结论**。
2. **脚手架 → 撤除**（先用手写报点训出听者，再撤掉让说话者跟上）—— 撞 VISION §7.1「message 本身无预设语义」与 §1.1「不预设答案」：
   长出来的会是**我们的**词表。按 VISION §15 要先改 VISION，**只有用户能定**。
3. **搜索效率杠杆**（种群规模、针对 comm 通路的变异、说 / 听协同变异）—— VISION 不反对，但 ROADMAP 没有这个 phase，是新研究方向 + 算力承诺。
推荐顺序：**1（有预算上限、可续跑，先解决本机内存或换机器）→ 1 仍为空再开 3 的 phase**；**2 不做**，除非用户先改 VISION §7.1。

## [2026-09-13 21:04] D2 选项 1 开跑 + 读数改了三版：对照组冠军**确实**依赖自己的电台 —— 但对照门槛 P3'' 没过  #measure #incident

**用户选定选项 1**（只加预算）。`runs/d2-long-s{1,2}`：配置 = d2-radio、300 代（上限）、`--snap-every 10`。
回归：`train.ts` 不开快照时，1 代 d2-radio 命令的第 0 代冠军与 d2-radio **两个 seed 逐位相同** ✓（重构没改默认行为，也证明共享初始种群）。
预测冻结在 `runs/d2-long-predictions.txt`（后附两段 addendum，都写在**任何长跑被读之前**）。

**新读数** `npm run radiouse`：只动被测那一队的电台（两队一起动会把「都变弱」读成 null）。读长跑之前先在 d2-radio 第 39 代对照组上校准，改了三版：

| 版本 | 对照组读数 | 发现的问题 |
|---|---|---|
| v1：只有 shuffled | Δshuffle **+0.543**（3/4 为正）→ P3 ❌ | 一次打掉「什么时候说」和「谁习惯说什么」两件事 |
| v2：+ speaker（同一说话者、换个时刻） | 合计 Δspeaker +0.042，但单个冠军 −0.507 … +0.685 | 替换值**每 tick 重抽**、**每个听者各抽** ⇒ 闪烁且队友之间互相矛盾，是 OOD；合计靠抵消 |
| v3：每（说话者，通道）每发送间隔一抽、全队一致，+ frozen（常数码） | 合计 +0.052，但 \|Δspeaker\| ≤ 0.25 只有 **2/4** → **P3'' ❌** | 剩一个已知缺陷：comm0 / comm1 各自独立抽 |

normal 列三版逐位相同（确定性 ✓）。v3 逐冠军（胜率顺序 normal / speaker / frozen / shuffled / off）：

| 冠军 | Δspeaker | Δfrozen | Δshuffle | Δoff | 胜率 |
|---|---|---|---|---|---|
| s1 红 | −0.082 | −0.233 | +0.262 | −0.311 | 64 / 68 / 76 / 57 / 55% |
| s1 蓝 | −0.375 | −0.480 | −0.399 | +0.010 | 28 / 44 / 51 / 46 / 29% |
| s2 红 | +0.128 | +0.330 | **+1.242** | −1.123 | 59 / 52 / 43 / **5** / 11% |
| s2 蓝 | **+0.536** | **+0.669** | +0.696 | −0.602 | **49** / 23 / 16 / 18 / 24% |

### 读法

1. **s2 红方**：同一说话者换时刻仍有 52%，打乱后只剩 5% ⇒ 依赖**说话者层面的结构**（谁习惯说什么），不是时机。v2、v3 都这样。
2. **s2 蓝方**：换时刻、常数码、打乱、静音**全部** 16–24%，只有真实电台 49% ⇒ 它在用**什么时候说**。
3. **s1 蓝方**：换成常数码反而更好（28% → 51%）⇒ 队友的真实电台对它**有害**。
⇒ ⭐ **D2b 的接收端结论被推翻一半**（「off 与 shuffled 伤害一样 ⇒ 满音量噪声」）：那次两队一起消融、固定步长取池，看不见单队依赖。
**发送端 MI 0–1% 仍然成立**，但只对 5 个候选指称 —— s2 蓝方用的时机信息编码的是别的东西。

### P3'' 没过的处理（按 addendum 2 的约定）

P3'' 的前提「对照组没有内容使用」**可能本身是错的**（s2 蓝方），也可能读数还有缺陷（通道独立抽样）。
⛔ 不做 v4 再判一次（那是挑读数）。**长跑结果逐冠军报告、写明 P3'' 未过，不按 Δspeaker 门槛判**。通道联合抽样的修法记进 TODO。
新坑 #29：替换一条信道时，替换值要守住这条信道自己的时间结构与广播形状。

## [2026-09-13 22:07] D2 选项 1 读完：300 代只加预算，电台用法**时有时无、不累积** ⇒ 按认可顺序开搜索效率 phase  #measure

`runs/d2-long-s{1,2}` 两条都跑满 300 代，没被杀、没续跑（s1 3450 s）。读数对照 `runs/d2-long-predictions.txt`，
追加的混淆检查预测冻结在 `runs/d2-long-phase-predictions.txt`（写于跑它之前）。

**接收端**（`runs/d2-long-radiouse.txt`，P3'' 未过 ⇒ 逐冠军报告，不按门槛判）。胜率顺序 normal / speaker / frozen / shuffled / off：

| 冠军 | g39 | g149 | g299 |
|---|---|---|---|
| s1 红 | **78** / 11 / 68 / 30 / 26（Δspeaker +1.39） | 39 / 36 / 32 / 40 / 13 | 26 / 30 / 36 / 32 / 23 |
| s1 蓝 | 39 / 49 / 46 / 28 / 29 | 46 / 43 / 49 / 36 / 36 | **94** / 80 / 54 / 47 / 66（+0.31） |
| s2 红 | 43 / 57 / 43 / 72 / 72 | 42 / 60 / 51 / 56 / 65 | 34 / 39 / 50 / 26 / 8 |
| s2 蓝 | 41 / 27 / 31 / 32 / 0 | **74** / 41 / 41 / 44 / 14（+0.80） | 46 / 53 / 44 / 26 / 26 |

合计 Δspeaker：g39 +0.279 → g149 +0.115 → g299 **+0.007**（为正 2/4 → 3/4 → 1/4）；Δshuffle +0.151 → +0.148 → +0.428；静音变差 3/4 → 3/4 → 4/4。
⇒ **P1 成立**（g299 合计 < 0.15 且只有 1/4 为正）。
⭐ 读法：强依赖真实电台的冠军**出现过三次**（s1 红 g39、s2 蓝 g149、s1 蓝 g299），但**没有一次在同一血统里留到下一个读数点**。
越来越依赖「有电台输入」（off / shuffled 变差），不是越来越依赖**内容**（speaker 替换）。

**发送端**：`radio.ts` 原本合并红蓝两个冠军算 MI，而 P2 写的是「每个冠军」⇒ 改成按冠军分行（合并行与改前逐位相同）。
g299 每冠军最大 MI / H：s1 红 1.3% · **s1 蓝 8.8%**（a site is armed）· s2 红 2.6% · s2 蓝 1.9% ⇒ P2 1/4 不过。
那个指称是全场状态、多在回合后段 —— 这个混淆在看到任何逐冠军数字**之前**就在对话里点了名。加 `MI | phase`（比赛时间 10 段）：
s1 蓝方符号与阶段本身 MI = 17% of H，条件化后 armed 只剩 **0.9%**；无时钟的 reloading 条件化前后 0.1% → 0.2%（没被乱改）⇒ **P2 成立**（混淆预测 ✅）。
顺带更正 D2b：「发送端 MI 0–1%」是合并数；d2-radio g39 拆开后单冠军最高 4%（坑 #30）。

**趋势条款**（两个 seed 都从 g149 升到 g299）：s1 升（均值 Δspeaker +0.07 → +0.14，最大 MI 2.9% → 8.8%，后者是时钟）；
s2 两项都没升（+0.16 → −0.13；2.7% → 2.6%）⇒ **无趋势**。

### 结论（按冻结许可）

P1、P2 成立、无趋势 ⇒ **只加预算，种群 16 在 300 代内长不出稳定电台**。按用户认可的顺序（1 → 为空再 3）：
开**搜索效率 phase**（ROADMAP Phase D2s），probe-first。
⚠ 「为空」只到这个程度：进化**找得到**依赖电台的冠军，**留不住**。这正是新 phase 的第一问 —— 是没找到，还是找到了被选择噪声丢掉
（每个 genome 只打 5 场、capture fitness 量子化、精英 2）。
⚠ 2 seed ⇒ 只读方向。VISION 核对：种群规模 / 评估场次 / 变异分配都不给消息预设语义（§7.1），也不是 shaping（§10）；
脚手架（选项 2）仍排除。

## [2026-09-13 22:22] D2s Probe 0 / 0b：尺子修完 —— 对照组的「电台依赖」是**结构**不是**内容**，D2b 的更正被撤回  #measure #incident

预测：`runs/d2s-probe0-predictions.txt`、`runs/d2s-probe0b-predictions.txt`（后者写于 0b 任何读数之前，n=96 仍在跑时）。
读数：`runs/d2s-probe0-n24.txt` · `-n96.txt` · `runs/d2s-probe0b-control.txt`。全部在 d2-radio 第 39 代对照组上。

**Probe 0（radiouse v4 = 通道成对抽 + 同一替换换种子重跑的噪声底）**
- normal / off 两列与 v3 逐位相同（确定性 ✓）；0b 的输出里 v4 各列又与 n=24 逐位相同 ✓。
- 噪声底 |Δredraw|：n=24 .20 / .04 / .18 / .06（4/4 ≤ 0.25）⇒ **P0a ❌**（我以为 P3'' 没过是噪声，不是）；n=96 均值 .038 ⇒ **P0b ✅**。
- 成对抽样修掉了一个大伪影：s2 红方 shuffled 胜率 **5% → 49%**（Δshuffle +1.24 → +0.22）。
- Δspeaker 散布仍在（s1 蓝 −0.53、s2 蓝 +0.56，远超各自噪声底）⇒ 冻结分支 (c)：「需要训练出来的零点」。

**偏离（写明）**：没有造训练零点。对照 DISCOVERY 合同 §5 / §6.1 与 VISION §7.4 / §12.1：因果权威是配对干预与分叉回放，不是训练零点；
而且电台没信息时进化没理由去听，训练零点会**空过**。改做 Probe 0b：`replay` = 同一对冠军、同对手、同攻守，**另一场比赛同一 tick** 整队电台
（保住保持时长、双通道、个人习惯、前后顺序、队友同步、比赛时钟，只拿掉与本场局面的对齐）。找不到回放素材的比例 ≤ 0.7%（门槛 5%）。

**Probe 0b**（胜率 normal / speaker / replay）：

| 冠军 | Δspeaker | Δreplay | 回放噪声底 | 胜率 |
|---|---|---|---|---|
| s1 红 | +0.021 | +0.240 | .073 | 64 / 66 / 54% |
| s1 蓝 | **−0.525** | −0.112 | .100 | 28 / 51 / 31% |
| s2 红 | −0.128 | +0.117 | .049 | 59 / 65 / 54% |
| s2 蓝 | **+0.556** | +0.016 | .208 | **49 / 22 / 49%** |

⇒ **P0b-1 ❌，落在冻结的第二支**：两个「依赖」的冠军在 replay 下同号但小得多 ⇒ Δspeaker 的大头是**打散了前后顺序 / 同步 / 时钟**，不是局面内容。
`replay` 成为 D2s 的内容读数（它保住的结构严格更多）。

### 这推翻了什么

- ⚠ **D2b 的「更正」撤回**（ROADMAP 里 2026-09-13 那条）：「s2 红方依赖说话者结构（打乱→5%）」是逐通道抽样伪影；
  「s2 蓝方只有真实时机的电台才行（49% vs 16–24%）」换成**另一场的真实电台**也是 49% ⇒ 它要的是电台的**形状**（节奏 / 同步 / 时钟），不是**本场说了什么**。
  ⇒ D2b 原结论「接收端没有内容」在对照组上**重新成立**；「电台是承重输入」也成立（off 3/4 变差）。
- ⚠ d2-long 报告里的三个「强依赖真实电台」冠军是用 Δspeaker 读的 ⇒ 同样可能是结构。按 0b 预测登记，d2-long 用 replay 重读**只作描述**（在跑），⛔ 不重判。
- 教训进坑 #31：i.i.d. 的「同一说话者别的时刻」会打散序列结构，在闭环里读起来像内容。

## [2026-09-13 22:31] D2s：d2-long 用 replay 重读（只作描述）—— 内容依赖最多两个小候选，Probe 1a 去复现  #measure

`runs/d2s-probe0b-long.txt`（n=24）。「依赖」= |Δreplay| > 2 × 自己的回放噪声底。22:07 的判决不动。
- 正向候选只有两个：**s2 蓝 g149 +0.179**（噪声底 .034，胜率 74% / 回放 66%）· **s1 蓝 g299 +0.170**（.063，94% / 84%，刚过线）。
- 反向一个：s2 红 g149 −0.136（回放反而更好 42% → 49%）。其余 9/12 不依赖；每个读数点合计 Δreplay −0.037 / −0.030 / +0.023。
- 22:07 报告里三个「强依赖真实电台」的冠军，在 replay 下都 ≈ 0（s1 红 g39：normal 78% / speaker 5% / replay 81%）⇒ **形状，不是内容**。
- ⚠ 12 次读数、2× 噪声底的规则，碰巧冒出一两个是意料之中 ⇒ Probe 1a：n=96 复现这两个候选（预测 `runs/d2s-probe1a-predictions.txt`，
  我的预期「都复现不了」）。复现不了 ⇒ 内容用法 300 代里**没找到过** ⇒ 按 D2s 自己的映射，第一根杠杆 = **种群规模**；
  复现得了 ⇒ Probe 1b 沿该冠军血统（±1/5/10 代）读留没留住。hof 两 run 两色都是 0–299 逐代连续 ✓（1b 可做）。

## [2026-09-13 22:44] D2s Probe 1a：一个内容用法**复现了**（s2 蓝 g149），另一个是噪声 ⇒ Probe 1b 读它的血统  #measure

n=96（`runs/d2s-probe1a-g149.txt`、`-g299.txt`），复现规则 = Δreplay > 2 × 噪声底 **且** > +0.10：
- **s2 蓝 g149：+0.170**（噪声底 .052），胜率 normal 69% / replay 61% ⇒ **复现** ✅。
- s1 蓝 g299：+0.068（.037），92% / 89% ⇒ 没复现（n=24 的 +0.17 是噪声）。
- 反向候选 s2 红 g149：−0.020 ⇒ 没复现（按正向规则的幅度对称用）。
⇒ **P1a（我的预期「都复现不了」）❌**：只加预算的进化**找到过**一次依赖本场内容的听者。
⚠ 发送端：s2 g149 蓝方最大 MI 2.0% of H ⇒ 它用的内容不在 5 个候选指称里；⛔ 不叫「语言」（VISION §7.4 要三层）。
按冻结分支进 Probe 1b（预测 `runs/d2s-probe1b-predictions.txt`）：沿 s2 蓝血统读 g139/144/148/150/154/159。
radiouse 加 `--observe tag:B` 只读指定冠军（每个冠军的抽样只由自己的队伍与场次索引定种子）；先用 g149 重跑核对 +0.170 逐位相同，否则 1b 不读。

## [2026-09-13 22:51] D2s Probe 1b：那次内容用法**只在一代冠军身上**，前后一代都没有  #measure

n=96、`--observe d2-long-s2:B`（`runs/d2s-probe1b-g*.txt`）。过滤自检：g149 重跑读到 normal 0.551 / Δreplay +0.170 /
胜率 69/48/43/54/18/61%，与不过滤逐位相同 ✓（每个冠军的抽样只由自己的队伍与场次索引定种子）。

| 代 | 139 | 144 | 148 | **149** | 150 | 154 | 159 |
|---|---|---|---|---|---|---|---|
| Δreplay | −0.003 | −0.020 | −0.091 | **+0.170** | +0.019 | −0.066 | +0.048 |

⇒ **P1b（我的预期「一闪而过」）✅**：g149 之外一个都没到门槛，连 g148 / g150 都没有。

⚠ 但「只在一代出现」有两种读法：(i) 真有这个依赖，选择没留住；(ii) +0.170 是抽样偶然 —— 「2 × 噪声底」只比了**一次**空抽，不是标准误。
D2s 选哪根杠杆完全取决于这个，所以先做 Probe 1c 部分 A：换世界种子（`--seed 9191` / `1313`）重跑 g149 蓝方。
预测 `runs/d2s-probe1c-predictions.txt`，我的预期「两个新种子都仍 > +0.10」。⛔ 两个新种子里有一个不过 ⇒ 300 代里根本没有内容用法，第一根杠杆 = 种群规模。

## [2026-09-13 22:56] D2s Probe 1c-A：换世界种子后那 +0.170 站不住 ⇒ 「2 × 噪声底」这个判据本身不成立  #measure #incident

同一冠军（d2-long-s2 g149 蓝）、同样 n=96，只换 `--seed`（世界种子 + 对手场次 + 替换抽样一起换）：

| 种子 | Δreplay | 当次噪声底 | 胜率 normal / replay |
|---|---|---|---|
| 7070（原） | **+0.170** | .052 | 69 / 61% |
| 9191 | **−0.021** | .146 | 62 / 64% |
| 1313 | +0.117 | .043 | 59 / 54% |

⇒ **P1c-A（我的预期「两个新种子都 > +0.10」）❌**，且正好落在冻结的「两个新种子互相矛盾」那一支：
**fitness 均值读数在 n=96 下分辨不了 +0.17，⛔ 不据此选杠杆**。
⭐ 根因是我定判据时的错：**「一次空抽的差」不是标准误**（它自己在 .043–.146 之间跳）。Probe 1d 换成逐场配对估计（同一世界种子下 normal 与 replay 配对做差，报 mean ± SE，
并用 replay 与 replay' 的配对差当空对照），预测 `runs/d2s-probe1d-predictions.txt` —— 只换估计量，不换干预。

## [2026-09-13 23:00] D2s Probe 1d：逐场配对 —— 那次内容用法是**真的，但只值 +0.07**，选不动  #measure

同一冠军（d2-long-s2 g149 蓝）、n=96 × 4 个种子 = **1536 场配对**（`runs/d2s-probe1d-seed*.txt`）：

| 种子 | 7070 | 9191 | 1313 | 4242 | 合并 |
|---|---|---|---|---|---|
| Δreplay | +0.170 ± .055 | −0.021 ± .064 | +0.117 ± .067 | +0.022 ± .067 | **+0.072 ± 0.032（2.27 SE）** |

空对照（replay − replay'）合并 −0.011 ± 0.032（0.33 SE）✓ 估计量干净。
⇒ **P1d（我的预期「落在 2 SE 内」）❌**，落在冻结的第三支：**效应真实但 ≤ +0.10**。
⇒ 结论：只加预算的 300 代里，最好的那个候选也只值 **+0.07 fitness**；训练里每个个体只打 5 场，这么小的差**选不出来**。
按 D2s 的映射，第一根杠杆 = **种群规模**（等算力 A/B）。

## [2026-09-14 00:08] D2s 杠杆 1（种群规模，等算力）：16 → 48 买不到内容用法  #measure

预测 `runs/d2s-lever1-predictions.txt`（写在 pop48 开训之前）。等算力：16 × 300 代 = 48 × 100 代 = 4800 个体代，每个体 5 场。
B 组训练：s1 3316 s（33.2 s/代）· s2 3756 s，都没被杀、没续跑。读数：每组 6 个等算力点位 × 2 run × 2 色 = **24 个冠军读数**，每个 96 场配对。

| 组 | Δreplay 合计 | 空对照 | 单冠军最高 |
|---|---|---|---|
| A = pop 16 × 300 代（g50…299） | **+0.005 ± 0.021**（0.24 SE） | −0.001 ± 0.022 ✓ | s2@100 红 +0.258 ± .112 |
| B = pop 48 × 100 代（g16…99） | **+0.041 ± 0.021**（2.00 SE） | −0.020 ± 0.020 ✓ | s1@50 蓝 +0.261 ± .103 |

**B − A = +0.036 ± 0.030（1.22 SE）** ⇒ 在 2 SE 内，B 自己也 < +0.10 ⇒ **P-lever1（我的预期「加种群买不到」）✅**。
⛔ 单冠军那几个 +0.2 不作声称：24 个读数里按 multiplicity 本就该冒出来（坑 #32）。
⇒ 按冻结分支，下一根杠杆 = **评估场次**（每个体 5 → 15 场，代数降到 1/3，算力仍相同）。

**强弱对照（描述，不参与判据）**：`crossplay --gens last` 把两组 8 个冠军放一起打（每格 12 场）：A 组平均胜率 42/79/49/38%、B 组 40/67/42/44%
⇒ 组均值 52% vs 48%，在噪声里。⇒ 等算力下加种群**既没换来棋力，也没换来电台内容**（⛔ 别读成「杠杆没生效」：它生效了，只是没买到东西）。

## [2026-09-14 01:16] D2s 杠杆 2（评估场次，等算力）：5 → 15 场也买不到 ⇒ 两根便宜杠杆用尽，**停在人工闸门**  #measure

预测 `runs/d2s-lever2-predictions.txt`（写在开训之前）。等算力：每 seed 24000 场（16 × 300 × 5 = 16 × 100 × 15）。
训练：s1 3786 s（37.9 s/代）· s2 3293 s，没被杀、没续跑。读数与杠杆 1 同一把尺子，每组 24 个冠军读数 × 96 场配对：

| 组 | 设置 | Δreplay 合计 | 空对照 | 与 A 的差 |
|---|---|---|---|---|
| A | pop 16 × 300 代，每体 5 场 | +0.005 ± 0.021 | −0.001 ± 0.022 ✓ | — |
| B | pop 48 × 100 代，每体 5 场 | +0.041 ± 0.021 | −0.020 ± 0.020 ✓ | +0.036 ± 0.030（1.22 SE） |
| C | pop 16 × 100 代，每体 **15 场** | −0.004 ± 0.022 | −0.011 ± 0.022 ✓ | **−0.009 ± 0.031（0.28 SE）** |

⇒ **P-lever2（我的预期「更安静的评估也选不出来」）✅**。按冻结分支：**D2s 的两根便宜搜索杠杆用尽 ⇒ 停，等人定下一步**。
⛔ 不自开第三根杠杆、不加大算力承诺、不碰脚手架（选项 2 要先改 VISION §7.1）。

### Phase D2 / D2s 到此为止测到的（给人做决定用）

1. **世界愿意为通信付钱**：手写一符号报点只走真实量化 / 间隔 / 延迟链路，守方胜率 **13% → 40%**（telepathy 上界 42%）= 拿回 93%（D2c）。
2. **进化找到的东西值 +0.07**：300 代里唯一站得住的内容依赖（s2 蓝 g149），1536 场配对测得 **+0.072 ± 0.032 fitness**，
   且只活了**一代**（g148 / g150 都没有）。
3. **进化用的是电台的形状，不是内容**：off / shuffled 明显变差，但换成另一场的真实电台（replay）几乎无差；发送端与 5 个候选指称的 MI 按冠军拆开、
   按比赛阶段条件化后 ≤ 4%。
4. **等算力下，加种群（3×）和加评估场次（3×）都不改变这个结论**（上表）。
⇒ 一句话：**世界给的奖赏是 27pp 胜率，进化能摸到的梯度只有 0.07 fitness 且一代即失**——差在搜索能不能把这点信号变成可选择的差异。

## [2026-09-14 11:46] 人工闸门的答复：回主线 Programme E，电台押后  #decision

用户在 D2s 的闸门上选了「回主线 Programme E，电台押后」（选项：回 E / 加一个数量级算力 / 改选择结构 / 改世界让电台回报更陡）。
⇒ D2、D2s 的结论封存在 ROADMAP 与 LOG；尺子（`radiouse` 的 replay + 逐场配对）留着，将来生态立起来后可以直接回头量。
⇒ 新 cursor 回到 Programme E。E1 的现状是：probe-first 已推翻施工前提（共享大脑 + slot one-hot 本身就是「每人一条 40 维 bias」且被进化用上），
E1 现在缺的是**「这条 bias 不够」的证据**，⛔ 不得直接开造 player block。

## [2026-09-14 11:49] E1 roledemand：**一条「每人一个常数」比手写的五个角色还好 13pp** ⇒ E1 仍无证据，E 线转 E2  #measure

新探针 `npm run roledemand`（预测 `runs/e1-roledemand-predictions.txt`，写在跑之前）。问法与 memdemand / radiodemand 同：
同一批手写攻方（直攻 A / 直攻 B / 假打 A→B）、同样地图 7,11,23 × 8 seed × 双攻守，只换守方**个体差异的形态**。

| 守方 | 直攻 A | 直攻 B | 假打 A→B | 平均 | armed | sight |
|---|---|---|---|---|---|---|
| 五个相同（对照） | 8% | 27% | 2% | **13%** | 88% | 730 |
| 每人一个常数（谁守哪点） | 15% | 33% | 65% | **38%** | 99% | 972 |
| 五个不同角色 (a) | 10% | 6% | 42% | 19% | 94% | 1009 |
| 五个不同角色 (b) | 4% | 23% | 46% | 24% | 99% | 741 |
| 完美分配（上界） | 50% | 52% | 27% | 43% | 57% | 1641 |

**P3 对照 ✅**：「五个相同」与 radiodemand 的 `private 0s` **逐格相同**（8/27/2 · 13% · 88% · 730），「每人一个常数」与 `site-holder ref` 也逐格相同。
**P2 ✅**（常数 − 全同 = +25pp）。**P1 ❌ 且落在「角色更差」那一支**（最好的角色行比常数低 13pp）。
⇒ 按冻结分支：**E1 仍然没有「一条 bias 不够」的证据**，⛔ 不造 player block、⛔ 不降低 E1 exit；这套手写角色是角色空间里的一个坏点。**E 线转 E2**。
⭐ 顺带两个可复用的观察：① 完美分配上界只有 43%，而且**对假打只有 27%**——跟着当前位置走的分配会被假打整队拽走 ⇒ 「分配」不是万能，信息才是；
② 分配本身（38%）几乎追平上界（43%），说明这个世界里**守点分工**已经把大部分价值拿走了。

## [2026-09-14 11:54] E2 demand：共识本身不值钱，但**混合**值钱 —— 均衡 41.5% vs 单一打法最坏 27%  #measure

预测 `runs/e2-priordemand-predictions.txt`（A 部分）与 `runs/e2-gamevalue-predictions.txt`（B 部分，算之前冻结）。
探针沿用 `npm run roledemand` 的同一把尺子（`runs/e2-priordemand.txt`）。

| 守方 | 直攻 A | 直攻 B | 假打 A→B | 平均 | 最坏 |
|---|---|---|---|---|---|
| 五个相同（对照） | 8% | 27% | 2% | 13% | 2% |
| 每人一个常数 3/2 | 15% | 33% | 65% | 38% | 15% |
| **各自掷硬币**（同边际、无共识） | 19% | 38% | 69% | **42%** | 19% |
| 全队掷一次（有共识、计划会变） | 23% | 21% | 73% | 39% | 21% |
| **全队压一个点** | 50% | 67% | 27% | **48%** | 27% |
| 完美分配（上界，不合法） | 50% | 52% | 27% | 43% | 27% |

- **P3 对照 ✅**（13 / 38 / 43 三行加新代码后逐格不变）· **P2 ✅**（计划变不变差 1pp）。
- **P1 ❌**：共识 − 各自掷硬币 = **−4pp** ⇒ ⛔ 不建团队基因型（冻结分支）。
- ⭐ **压一个点 48% 是最好的合法行，还高过「完美分配」上界** —— 点位 armed 后集中回抢比分兵强。上界之所以低，是它跟着当前位置走，**被假打整队拽走**（27%）。

### B 部分：把这张表当零和博弈解（只做算术，不再跑仿真）

守方均衡值 **41.5%**，混合 = 压点 68% + 全队换计划 32%；攻方均衡 = 直攻 63% + 假打 37%。
任何**单一**合法打法的最坏情况最高 27% ⇒ **混合比固定打法多 14.5pp** ⇒ **P1（B）✅**。
⇒ 结论：**「共识」不是世界付钱的东西，「可变且对手猜不到」才是** —— 但它只在对手会适应时才兑现。
⇒ 按冻结分支：⛔ 不在固定对手上造团队基因型；E 线开到**对手生态**（E4–E6），且先做探针（现成缺口 = exploitability，TODO 里唯一没工具的一项）。
⚠ 三个手写攻方是三个点：这个均衡约束的是这一组攻法，不是攻法空间。

## [2026-09-14 12:09] E6 前置：exploitability 探针做好了 —— 但冠军**没有**普遍脆弱 ⇒ 生态线的动机变弱，交回人工闸门  #measure

新工具 `npm run exploit`（SUBSTRATE §10.4 五把尺子里最后一把没工具的）：冻住一个冠军，让一个挑战者种群**只**打它 40 代，
再做头对头（24 seed × 双攻守），并把另一条 run 同代的同色冠军当参照打同一目标、同一批 seed。
预测 `runs/e6-exploit-predictions.txt`（写在跑之前）。

| 目标（d2-long @299） | 挑战者胜率 | 同辈冠军胜率 | 接触 ticks |
|---|---|---|---|
| s1 蓝 | 50% | 4% | 384 |
| s1 红 | **0%** | 52% | 648 |
| s2 蓝 | **83%** | 48% | 509 |
| s2 红 | **P3 判空**（0 接触，50% 是时钟判的） | 96% | 0 |

- **P1（≥75% 在 ≥3/4）❌**：只有 1/4。**P2（比同辈高 ≥20pp 在 ≥3/4）❌**：2/4（+46pp、+35pp，但 s1 红 −52pp）。
- **P3 闸门生效 ✅**：s2 红那行被脚本自己拒绝（坑 #13/#18 的老问题：两条策略互相不接触，会印出一个干净的 50%）。
⇒ 按冻结分支：**40 代的便宜 best response 打不穿主线冠军**（打穿 1 个、打平 1 个、被反杀 1 个、1 个判空）
⇒ 「内部指标高估冠军」这个 E6 立论**在这个预算下没测到** ⇒ ⛔ 不凭直觉开 E4–E6，交回用户。
⚠ 40 代对 300 代是**便宜**的 best response：失败只约束这个预算，不是「不可被利用」。
⭐ 仪器本身留下了：`npm run exploit`，这是 E6 将来任何 exit 都要用的那把尺子。

## [2026-09-14 12:15] 观战：双点位画面修好了 —— 之前页面画的是**另一个世界**  #ship

用户在闸门上说「看 vision」。按 VISION 定的方向（§0 第二条北极星「观赏性来自可解释性」、§12.1 detection 层要能回放到具体 match、
§15 第 4/5/8 问都要求事后从轨迹与事件识别）：**先把能看的做对**，而不是改世界。
⛔ 顺带纠正我自己在闸门上的措辞：「世界还没给这些开价」说过头了 —— 今晚的均衡恰恰显示存在真实的石头剪刀布（压点克直攻、假打克压点），
D2c 也量过信息值 13%→40% ⇒ 「改世界」的前提不成立（VISION §15 第 1/3 问过不去，且会让所有 baseline 作废，坑 #12）。

- `src/render/scene.ts`：点位环 / 占领盘改成**按 `map.sites` 逐个建**（原来只画 `zoneX/zoneZ` = site 0）；
  占领着色逐点位统计，capture 模式下 **armed 的点位会脉冲**（没人站也亮）。`inZone` → `siteAt` 返回点位下标。
- `src/ui/heatmap.ts`：每个点位各画一个圈，双点位时标 A / B。
- `src/main.ts`：新增 `?sites=2&mode=capture` 覆盖（⛔ 不动默认值，那是单独的决定）；**导入 run 时若规则不同（点位数 / 回合模式）直接拦下并给出该用的 URL**
  —— 之前只比 mapSeed，把双点位 run 导进单点位页面会画错整张地图。
- 验证（借 evofootball 的 playwright，端口 5983，先 `curl | grep title` 确认是本项目）：`?sites=2&mode=capture` 截图**两个点位环都在**、
  中间隔断在；默认页仍是单点位一个环、无 console error。截图 /tmp/evo-2site.png · /tmp/evo-1site.png。

## [2026-09-14 12:19] 观战：kill feed 上线（走 world.events 的观战通道）  #ship

`MatchViewer` 新增 `onKill` / `onNewMatch` 两个**只给观战器**的回调（⛔ 任何 policy 都不得接到它们上面 —— `world.events` 是 SPECTATOR CHANNEL）；
页面右上角滚动「击杀者 ✕ 被击杀者」，按队伍着色，最多 5 行、5 秒淡出，换一场自动清空。⛔ 没有另造事件源。

**验证经过（两次，第一次是空读数）**：
① 先用 `runs/smoke-30.json` 的末代冠军播了一整场：t 走到 40、done，但 **0 枪 0 死** ⇒ feed 当然是空的 —— 这是坑 #13/#18 的老形状（两条策略互相不接触），
**不是接线坏了**；⛔ 没有据此下「kill feed 没生效」的结论。
② 换成会打的一对（d2-radio-s1 红 vs d2-radio-s2 蓝，`?sites=2&mode=capture`）：8.2 秒时 feed 出现 `B2 ✕ R5` / `R3 ✕ B2`，
最多同时 5 行、无 console error，截图 /tmp/evo-killfeed.png（双点位环也在同一张图里）。

## [2026-09-14 12:22] 观战：通信灯改成**符号**，并修掉「页面用另一套电台规则播冠军」  #ship #incident

两个同族缺陷，都是「观战演的不是训练那个世界」：
1. **灯读错了字段**：渲染用的是 `a.comm`（没说出口的意图），真正上电台的是 `a.commSaid`（量化 + 按发送间隔保持）。已改成 `commSaid`。
2. **页面的电台规则和 run 不一致**：`?sites=2&mode=capture` 下 `commTokens` 仍是 0，于是用 5 符号电台训出来的冠军在**连续电台**里播 ——
   而 commDim 没变 ⇒ **基因组长度一样，没有任何东西会报错**。这是我在验证第 1 条时撞见的。
   已加 `?tokens=&interval=&delay=`，并把导入检查从「点位数 / 回合模式」扩到**整套电台规则**，提示里直接给出该用的 URL。

新的纯函数 `commLight(cfg, said)`（`src/render/scene.ts` 导出，POV 指示器与世界里的灯共用一份映射）：
量化电台下 slot 0 的符号决定色相、slot 1 决定亮度档，**静默（0）= 暗**；连续电台仍走旧的角度 / 幅度色带。
`tests/render.test.ts` 钉住四条：同一符号永远同一颜色 · 不同符号色相互不相同 · 静默暗 / 有话亮 · 连续模式仍是斜坡。

**验证**：`?sites=2&mode=capture&tokens=2&interval=5&delay=3` 下播 d2-radio 的冠军，页面读到 `commTokens 2`、
实发符号是量化值（1.0 / −1.0 / 0.5 / 0），从活体材质抽到的 HSL 按符号分色（−1 → 色相 0）；无 console error。
⚠ 之前那次「tokens 0」的读数就是第 2 条缺陷的现场证据 —— ⛔ 没拿它当「灯不工作」的结论。

## [2026-09-14 15:20] 观战：观战条头像加电台符号点（复用同一份映射）  #ship

`spec-tile` 的名字行加一个 7px 圆点，颜色走 `commLight(trainer.sim, a.commSaid)` —— 与世界里的灯、POV 指示器**同一份映射**（⛔ 没有第二份）；阵亡不显示。
验证：`?sites=2&mode=capture&tokens=2&interval=5&delay=3` 下播 d2-radio 冠军，10 个点里 7 种颜色，
同一个 slot-0 符号同一个色相（0.5 → 蓝、1 → 紫、−1 → 红），阵亡那个是透明；无 console error。截图 /tmp/evo-dots.png。

## [2026-09-14 15:25] 观战：cross-play 矩阵进页面（页面只画，不算）  #ship

VISION §11.4（非传递不是 bug，是这个世界的内容）+ §12.1 要求能看清一个时代的结构。做法按 TODO 的硬约束：**不另写一套算法**。
- 新文件 `src/core/crossplayFile.ts` = `crossplay.ts --out` 的**唯一形状定义**，生产方（CLI）与消费方（页面）共用。
- `scripts/crossplay.ts` 现在把**它自己算的**派生量也写进 JSON：`decisiveEdges`、`cycles`、`rowMean`（只统计有接触的格子）。
  顺手把「跨图均值」那段打印改成复用同一个 `rowMean()`（原来是第二份同公式的代码）。
- `src/ui/crossplayView.ts`：画矩阵（胜率着色、decisive 格描边、**零接触印 `··` 且不着色**）、多图切换、页脚给 margin / mirror 偏置 / decisive 边数 / 非传递循环。
  ⛔ 视图里除了颜色不算任何东西：我第一版自己算了行均值，当场改掉，改成读 CLI 的 `rowMean`。
- 侧栏新增「Who beats whom」区，导入 `runs/xp.json`。

**验证**（8 个参赛者 × 2 张图）：页面读数与 CLI 逐项一致 —— 地图 7：16 条 decisive edge、**2 个非传递循环**（`2 > 3 > 6 > 2`）；
地图 11：21 条、0 个循环；mirror red 84% / 78%（页面会打 ⚠「是地图在决定，不是基因组」）。切图按钮生效，无 console error。截图 /tmp/evo-xp.png。

## [2026-09-14 15:28] 观战：击杀慢动作回放 + 修掉「击杀时刻记错了最多 11 帧」  #ship #incident

`MatchViewer.replayLastKill(preroll=45, speed=0.35)`：用**同一个 seed 和同一对基因组**重建这场，快进到最后一次击杀前 45 帧，再以 0.35× 播。
⛔ 不存每帧快照 —— 仿真是确定性的，这是 DISCOVERY 合同 §5「可分叉回放」的便宜一半（同一条分支，不分叉）。按钮在观战栏。

**验证时撞出一个静默 bug（已修，记成坑 #34）**：击杀时刻原本记在**每帧收尾**，而一帧最多推进 12 个仿真步 ⇒ 记下的帧号取决于当时跑多快。
第一次对比重放：原始 `[...,224,233,239]` vs 重放 `[224,232,239]` —— 差一帧，看起来像「重放不确定」，其实是**时间戳记错**（实测最多偏 11 帧）。
改成在产生事件的那个仿真步里取 `world.tick` 之后，重放序列与原始**逐项相同**（`[224,231,239]`）⇒ 确定性重放成立。
⚠ 这也说明：早先 LOG 里那几个「击杀帧号」是帧收尾口径，不可与现在的数直接比。

## [2026-09-14 15:31] 观战：热力图分「走位」与「开火位置」两层  #ship

`World` 多采一层 `heatFire`（只在**开火的那一 tick**记这个格子），沿 `MatchResult → Trainer → GenReport → 页面` 原路传（worker 是结构化克隆，自动跟着走）。
⭐ 两层**共用同一个分母**（走位层的最大值）⇒「亮」在两层里含义一致：站得多但不开枪的格子，在开火层里仍然是暗的。
页面画法：底色走位、黄色小方块开火，图上带图例。

**验证**：页面里 `?pop=12` 真跑三代，最后一代红方 —— 走位覆盖 **316** 格、其中 **121** 格有开火；两层总量 23 vs 1.5（≈6.5% 的 tick 在开火），
开火层最大值 0.17 < 走位层 1.0（同分母下开火是子集，符合预期）；无 console error。截图 /tmp/evo-heat.png。
顺手修掉一处过期文案：页面「how to read the arena」还写着通信灯是「色相=方向、亮度=强度」（连续电台时代的说法），已改成按符号。

**验证（改了 `src/sim/world.ts`，按规矩自证没动仿真）**：同一场手写 bot 对局开/关 `heat`，`winner / score / fitness / ticks / kills` 逐位相同（seed 12345、capture、双点位）⇒ 新的一层只是统计，不参与仿真。

## [2026-09-14 15:34] 工具：crossplay 支持 run 内历史矩阵（`--gens all` / `every:N`）+ 场次闸  #ship

TODO 那条「ladder 只比两个点，而那两个点有 14% 概率互相看不见（坑 #20）」的正解：把**一条血统自己的历史**做成矩阵。
- `--gens all` / `--gens every:N`（`every` 永远把末代算进去）。
- ⭐ 配一道**场次闸**：`--gens all` 在 300 代的 run 上是 300 个参赛者 ≈ **538,200 场**，脚本会算出这个数、拒绝跑，并给出可执行的替代命令
  （`--gens every:25`）；阈值 `--max-matches`（默认 20 万），> 12 个参赛者时先打印场次数提醒表格会很宽。

**第一份读数（描述，不是判决）**：d2-long-s1 **红方**、每 25 代取一个（13 个参赛者、每格 8 场）：
g0 均值 **14%**（进步是真的），但**末代 g299 是 65%，而 g225 = 69%、g275 = 67%** ⇒ **末代不是自己血统里最强的**。
表里还自带分母：几格标了 `~`（接触不足，胜负是时钟判的），这正是 ladder 给不出的东西。存档 `runs/xp-d2long-s1R-history.json`。

## [2026-09-14 16:11] 观战：导入 run 直接**采用它的世界**（场景可重建），不再弹窗让人改 URL  #ship

`ArenaScene.dispose()`（摘 resize 监听 · controls / renderer / 所有几何与材质 dispose · 移除 canvas）+ `MatchViewer.rebuild(cfg, map, hidden)`。
导入时比对 **mapSeed + roundMode + siteCount + 电台三参数**，不一致就重建视图与两张热力图并照常开播，状态栏写明采用了什么世界。

**验证**（两页对照）：先在 `?pop=12&sites=2&mode=capture&tokens=2&interval=5&delay=3` 下训一代并导出（3.2 MB）；
再用**默认单点位 koth** 的页面导入它 —— 点位 1 → **2**、模式 koth → **capture**、`commTokens` 0 → **2**，
`#view-canvas` 里仍然**只有一个 canvas**（旧的被移除，没泄漏），状态栏：「adopted its world: map 7, capture, 2 site(s), radio 5 symbols/5t/+3t」，比赛自动开播；无 console error。
坑 #33 的闸同步更新：行为从「拒绝」改成「采用」，不变量不变 —— ⛔ 绝不在与 run 不同的规则下播它的冠军。

## [2026-09-14 16:15] 度量：拆出 attackProgress / defendProgress，均值改成按「这一格有没有定义」求  #ship

`objectiveProgress` 一个数装了两种量（我进攻时的占领进度 / 我防守时的拆除进度），角色一互换，聚合值就随**角色配比**动。
- 新增 `attackProgress` / `defendProgress`：不是自己那份工作时为 **NaN**（koth 下两者都是 NaN）。
- `meanMetrics` 改成**只对有定义的比赛求平均**（⛔ 不把 NaN 当 0），全无定义时保持 NaN；图表本来就跳过非有限点。
- ⭐ 旧的 `objectiveProgress` **保留不动** —— 它是历史序列（README / CHECKPOINT 里的 .08 → .23/.35 都是它），换口径会让跨 phase 的数不可比（坑 #12）。
- JSON 没有 NaN ⇒ 导出时变 `null`；`HistoryStore.loadJSON` 读回时统一还原成 NaN，别让 null 漏进 `number` 字段。

`tests/metrics.test.ts`（新，5 条）钉住：攻方只有 attackProgress、守方只有 defendProgress（两种攻守分配各测一遍）· koth 下两者皆无 ·
均值分母是「有定义的场次」（0.8/NaN/NaN → **0.8** 而不是 0.27）· 全 NaN 仍是 NaN · JSON 往返后 null 还原成 NaN。
⚠ 顺带被 world 自带的守卫抓了一次：我在 koth 配置下复用了双点位地图，`World` 直接报错 —— 这道守卫是对的，测试改成按配置生成地图。

## [2026-09-14 16:17] 性能：lidar 按「最近点距离」排序 + 精确早退，**bench 快 19%**，读数逐位不变  #ship

TODO 那条要求「⛔ 先 profile 确认它真是热点」。`node --cpu-prof scripts/bench.ts` 的自耗时排名（⚠ load 6.9，只读**相对占比**）：
`forward` **36.7%** · `rayBoxDist2D` **18.2%** + `lidar` **9.1%**（合计 ~27%）· `observe` 9.9% · `step` 5.7%。
⇒ 热点确认，但顺序纠正了直觉：**MLP 前向才是第一**。`forward` 已经是 4 路展开 + 预分配缓冲，⛔ 不动它（改累加顺序 = 改浮点结果 = 所有历史 run 不可比）。

**改法（可证等价，不是近似）**：每个 agent 感知前，把 32 个箱子按「agent 到箱子最近点的距离」插入排序进复用缓冲；
每条射线沿这个顺序求交，一旦 `当前最小值 <= 下一个箱子的最近距离` 就 `break` —— 射线到箱子的距离不可能小于该最近距离，所以最小值不变。
⭐ 等价性用**数据**确认，不是靠论证：3 个 seed 的比赛，每 17 tick 抽样整张 obs + 结果做 FNV 哈希，改前改后都是 `7aa7daa4`。

**速度（交错配对，坑 #16）**：临时加一个环境变量开关，同一轮里新旧各跑一次，三轮：
新 56.5 / 56.7 / 56.9 ms/match · 旧 69.4 / 69.8 / 71.8 ⇒ **≈ 快 19%**。量完开关已删除（⛔ 不留调试分支）。

## [2026-09-14 16:19] 观战：第一人称换弹有画面提示了  #ship

第一人称枪模原本只有后坐。现在 `sync` 顺带算被观察者的换弹进度（`1 - reloadT / reloadSeconds`），`update` 里按 `sin(π·进度)` 把枪压下去再回位。
HUD 早就写着 reloading，这一下是**不用把视线从战斗上移开也能读到**的那部分。
**验证**：第一人称跟一个 agent 打 9 秒，采到 35 帧换弹画面 —— 枪模 y 最低 **−0.45**、俯角 **−0.55**；非换弹时对应值只有 −0.259 / +0.144；无 console error。

## [2026-09-14 16:20] 观战：导播切镜头加淡入淡出  #ship

`CameraRig` 加一个 `cutSeq` 计数（⛔ 不靠比对 reason 字符串），页面每帧看它有没有变，变了就把全屏遮罩闪一下（60ms 淡入、220ms 淡出）。
不这么做的话，导播从一个人跳到另一个人看起来像瞬移。
**验证**：开导播跑 9 秒，切了 **4** 次镜头，遮罩在 **22** 帧里可见、峰值不透明度 0.64；无 console error。

## [2026-09-14 16:22] 观战：第三人称可以拖着看侧面了  #ship

第三人称原来完全焊死在被观察者的朝向上，看不了侧翼。现在按住左键拖动绕人转 + 上下抬降，滚轮拉远拉近；
**看向的目标仍然跟着人**（只动眼睛不动画框），并且**切镜头或换人时自动归位**（否则导播会继承上一个人被拖到的怪角度）。
**验证**：拖 + 滚轮后偏移读到 yaw 1.08 rad / up 1.62 m / dist 1.1，机位绕了约 47°、抬高 1.3 m、距离 4.15 → 7.67；
换一个被观察者后偏移**精确归零**（0 / 0 / 1）。⚠ 归位后相机高度没回到原值，是 boom 遇遮挡自己抬高，不是偏移残留 —— 所以直接查了字段，没拿画面推断。

## [2026-09-14 16:23] README 跟上这一轮 ship 的观战功能  #docs

规矩是「README 只在 phase 真 ship 后更新」，这一轮确实 ship 了可见功能，所以对齐：
点位环改成「每个目标都画、capture 下 armed 的会脉冲」· 头顶灯改成「**发出去的**符号，静默为暗」· 新增 kill feed 行 ·
热力图多了开火层 · 新增「Who beats whom」面板（数字全是 CLI 的）· 导入 run 会**采用它的世界** ·
第三人称拖拽环绕 / 滚轮 · 「↺ last kill」慢动作回放 · 导播切镜头淡出 · URL 可指定世界（`?sites=2&mode=capture&tokens=2&interval=5&delay=3`）。

## [2026-09-14 16:27] G2 第一个 detector：trade —— 冠军的 trade 形状**明显高于时间打乱的零假设**  #measure

ROADMAP G2 要求「逐个建 detector + precision sanity check」，且结果禁止进 live sim（`src/probe/detect.ts` 在 T8 防火墙后面）。
定义与窗口在跑之前冻结在 `runs/g2-trade-predictions.txt`：**队友死亡后 3 秒内、12 米内，另一名队友把凶手打死**。

**precision 先跑（G2 的硬要求）**：同一条路的手写攻防 = **31%** 的死亡被 trade；观察方永不开枪的和平主义者 = **0%** ⇒ **P1 ✅**，detector 到达了断言。

| 观察方（@299） | 死亡 | 被 trade | 占比 | 零假设 | 倍数 | 回放锚点 |
|---|---|---|---|---|---|---|
| s1 红 | 117 | 6 | 5% | 1% | 6.0× | seed 1130297674 atk0 t213 |
| **s1 蓝** | 47 | **26** | **55%** | 5% | **10.5×** | seed 1130297674 atk1 t213 |
| s2 红 | 85 | 2 | 2% | 0.2% | 9.8× | seed 3254583490 atk1 t405 |
| s2 蓝 | 69 | 2 | 3% | 1% | 3.0× | seed 304693203 atk0 t196 |

⇒ **P2（我的预期「与零假设同量级」）❌**：四个都在 3–10.5 倍。按冻结分支：**作为候选现象报告**（含回放锚点），
⛔ 不说「它们学会了补枪」 —— VISION §12.1 要 birth / stability / intervention 三层。
⚠ ⭐ **我自己读数的弱点**：这个零假设把答复时间**均匀**重抽，会破坏「击杀成簇」这一结构，光是成簇就能抬高倍数。
⇒ 下一步另立预测，用**保住时间分布的自助抽样**当零假设再看一遍；⛔ 不在事后改这次的判据。

## [2026-09-14 16:29] G2 trade 第二步：换成**保住时间结构**的零假设 —— 那 10.5 倍是「击杀成簇」  #measure #incident

预测 `runs/g2-trade2-predictions.txt`（写在实现之前）。新零假设是**条件化**的：只看「队友在 3 秒 / 12 米内确实打死了人」的那些死亡，
问打中的是不是恰好是凶手，还是当时活着的敌人里随便一个 ⇒ 保住每一个真实时刻与位置，成簇不再能制造倍数。

| 观察方 | 有答复的死亡 | lift |
|---|---|---|
| 手写同路（正） | 56 | 0.82 |
| 手写和平主义（负） | 0 | —（P2 对照 ✅） |
| s1 红 | 19 | 0.81 |
| **s1 蓝** | 40 | **0.78** |
| s2 红 | **2** | 2.00 ⚠ 只有 2 例，读不了 |
| s2 蓝 | 3 | 0.80 |

⇒ **P1（我的预期 lift 落在 1–3）❌**，落在「低于 1」那一支：**答复打中凶手的概率与随机持平甚至略低**。
第一步那 55% / 10.5× 几乎全部来自击杀成簇 —— ⭐ 这正是我在第一步里自己标出来的弱点，第二步把它测掉了。
⇒ 按冻结分支**关闭 trade detector**：在这个定义与窗口下，没有超出偶然的证据；⛔ 也不能说「它们不会补枪」——只约束这个定义。
⚠ s2 红的 2.00 建在 **2** 例上，我判定为不可读（这是我的判断，不是预测里写死的门槛），因此仍按「全部 ≤ 1.5」那一支走。
下一个 detector：crossfire 几何。

## [2026-09-14 16:34] G2 第二个 detector（crossfire）precision 没过，按规则关掉 —— 夹角把「包夹」和「贴脸」混在一起  #measure #incident

预测 `runs/g2-crossfire-predictions.txt`（跑之前冻结）：定义 = 同一敌人被 ≥ 2 名队友看见且**在敌人处的夹角 ≥ 60°**；
P1 要求正样本 ≥ 2 × 负样本、且负样本 < 15%。

1. **第一版正样本选错**：分守两个点位的守方**根本不会同时看见同一个敌人**（离太远）⇒ 中位夹角 5°，比「全挤一个点」的 11° 还低。
   ⇒ 加了只服务参考用途的 `PostHolderPolicy`（同一点位两侧的固定守位，世界坐标、**故意不镜像**，⛔ 不得用于公平性测试）。
2. **换对照后仍没过**：flanking 27% vs bunched 20%（1.35×，负样本超 15%）。中位夹角 31° vs 14°、伤害占比 21% vs 4% 分得很开，
   但那**不是我冻结的判据**，⛔ 不事后换指标。
3. **根因实测**：阈值提到 110° 时负样本 **反超**（bunched 14% > flanking 9%）⇒ 「敌人处的夹角」会把**敌人贴脸**读成包夹
   （两个挤在一起的人，敌人越近夹角越大）。

⇒ 按冻结分支 **不读冠军**，detector 关闭。修法记进 TODO：同时要求两名观察者**各自离敌人足够远**，再用同一对对照过 precision。
⚠ 诚实记录一处污染：失败的那两次里冠军行与对照**同表打印**，我已经看到它们（share 0–1%、中位夹角 3–24°）⇒ 重做这个 detector 时，
我的「预期」不再是干净的先验，新预测文件里必须写明这一点。

## [2026-09-15 11:27] G2 crossfire 第二版：加距离条件后 precision 过了 —— 冠军**一次都没形成过**这个形状  #measure

预测 `runs/g2-crossfire2-predictions.txt`，开头**写明预期已被污染**（第一版失败时冠军行与对照同表打印过，我看过了）。
定义只加一条：夹角 ≥ 60° 之外，**两名观察者都要离那个敌人 ≥ 6 m** —— 这正是分离「远距离侧翼」与「贴脸」的那一刀。

| 行 | 可见 tick | crossfire | 占比 | 中位夹角 | 伤害占比 |
|---|---|---|---|---|---|
| 手写侧翼（正） | 2166 | 232 | **11%** | 31° | 13% |
| 手写扎堆（负） | 1996 | 0 | **0%** | 6° | 0% |
| s1 红 @299 | 3210 | 0 | 0% | 22° | 0% |
| s1 蓝 @299 | 3023 | 0 | 0% | 4° | 0% |
| s2 红 @299 | 3164 | 0 | 0% | 3° | 0% |
| s2 蓝 @299 | 3300 | 0 | 0% | 4° | 0% |

⇒ **P1 ✅**（正样本 ≥ 2 × 负样本、负样本 0%、两行分母都 > 1900）⇒ 按冻结分支读冠军（仅描述）：
**四个冠军一次都没有形成这个形状**，中位夹角 3–22° 远低于门槛。⇒ 它们**抱团打**，与 E2 的均衡读数（世界付钱给抱团）一致。
⛔ 不等于「学不会交叉火力」——只说明在这个定义、这个世界、这批冠军身上没有。

## [2026-09-15 11:32] G2 第三个 detector（rotate）两轮 precision 都没过，关闭 —— 它量的是「跨中线」不是「响应压力」  #measure #incident

预测 `runs/g2-rotate-predictions.txt` + 一段 addendum（写在第一轮之后、**任何冠军行被读之前**，输出特意截断在对照行）。
定义：每秒一步，热点 = 12 m 内攻方更多的那个点；守方若从「离冷点更近」在 **3 秒**内变成「离热点更近」= 一次 rotate。
零假设：**同一段真实轨迹**配另一场比赛的压力时间线。

**第一轮**：三行的零假设倍数**全是 1.00**，负对照读 38% ⇒ 两处都是 harness 的问题，不是定义：
① 守点 bot 在点位被占领后会回抢 —— 那**本身就是**对压力的响应，它不是负对照；
② 手写攻方每场都打同一个点 ⇒ 每场压力时间线几乎相同，「换一场的压力」等于同一份压力。
⇒ 换成**固定守位** bot 当负对照、攻法**逐场轮换**（直攻 A / 直攻 B / 假打）。

**第二轮**：负对照 **0%** ✓（这次是真的负），但 —— ⭐ **对压力完全无视的定时轮转读 39%（零假设 35%），而真正按压力汇聚的反应式只读 5%（零假设 10%，0.45×）**。
⇒ **P1 ❌**，而且诊断很清楚：这个定义量的是「多久跨一次中线」，不是「对压力的响应」—— 3 秒窗口在这张 60×60 的图上跨不完半场，
真正的转点要更久，于是被系统性漏掉；而在两点之间来回跑的 bot 反而刷满。
⇒ 按冻结规则**关闭，冠军未读**，⛔ 不再调阈值。重做需要新定义 + 新预测，时间尺度要从「跨半场要多久」推，不是拍一个 3 秒。
⭐ 方法教训（与 crossfire 同形）：**几何/时序 detector 的窗口必须由世界的尺度推出来**，否则读到的是 bot 的巡逻周期，不是它对世界的反应。

## [2026-09-15 12:45] G2 第四个 detector（man-disadvantage tempo）precision 没过，关闭 —— 零假设把「掉人」和「比赛时钟」分不开  #measure #incident

预测 `runs/g2-tempo-predictions.txt`（实现之前冻结）+ 一段 addendum（三轮 precision 之后、**冠军行从未进过表**：
这一节支持 `--precision`，现在更是**默认不打印冠军行**）。
定义：每 tick 按 `alive(我) − alive(敌)` 标 DOWN / EVEN / UP；tempo = 存活玩家的速度 m/s；
统计量**逐玩家配对**（DOWN 均速 − EVEN 均速），再和零假设做**差中差（DiD）**，报 mean ± SE（坑 #32）。
逐玩家是为了杀掉幸存者构成偏差 —— 活到「少一人」的通常就是最被动的那几个，队级均值会把这份筛选读成「变慢了」。

**两处 harness 修复，都没动冻结的 P1 门槛**：
① 供体退化：trace 列表把**同一个 world seed** 的两种攻守排在一起，而这两场的人数时间线可能**逐位相同**
⇒ DiD 对每个玩家读出 **+0.00 ± 0.00**（又一次「整齐得可疑」，坑 #26）。改成换 seed，且第二轮后再要求**供体不短于本场**
（短供体会把轨迹截断，只量到前段的快速部分 —— `rotateStats` 文档里早就写了「两条 trace 必须等长」）。
② 「每个状态至少 2 秒」是拍的，世界说它长了 ~7 倍：**逐玩家 DOWN 时长 p50 = 4 tick**，而对撞的 (c) 行**没有任何一个玩家**
到得了 30 tick（max 21）—— 人数一破，回合 1.4 秒内就结束了。⇒ 改成 8 tick，从分布上读出来的。

| 行 | 配对玩家 | raw down−even | 零假设 | **DiD** | 供体重合 agree/∩ |
|---|---|---|---|---|---|
| 手写 clutch（正，**少人就原地不动**） | 51 | −4.75 ± 0.10 | −4.17 ± 0.17 | **−0.48 ± 0.18** | 71%/43% |
| 手写 metronome（负，两点来回、什么都不读） | 227 | −0.37 ± 0.03 | −0.41 ± 0.02 | **+0.06 ± 0.02** | **95%/94%** |
| 手写 arrive+stop（c，不读人数但会**到点站住**） | 53 | −0.75 ± 0.15 | −2.02 ± 0.18 | **+0.44 ± 0.33** | 65%/29% |

⇒ **P1 ❌**（a −0.48 > −0.5；b 0.44 > 0.3；c 1.75 < 4×；d ✅）。

⭐ **根因在零假设，不在定义也不在门槛，而且是这个世界的性质**：**人数差几乎是回合时钟的函数** ——
第一个人倒下在 **tick 68 ± 15**（sd，约 1 秒），而 ~26 个「至少 10 场还活着」的秒级 bin 里**只有 2–4 个**同时装得下两种状态
（t<45 全是 5v5，t>105 几乎全在少人）。⇒「另一场的人数」基本就是「本场的人数」，
于是**跨场置换的零假设把它本该检验的效应自己减掉了**：clutch bot 的减速按构造 100% 因果，raw −4.75，DiD 只剩 −0.48（**被吃掉 88%**）。
负对照从另一侧说明同一件事：它的供体在 **95% 的 tick、94% 的 DOWN tick** 上与本场一致 ⇒ 它那个干净的 +0.06 ± 0.02 是**空过**，
**哪怕它真的响应人数也会读 0**。⚠ 强行让供体更不一样也不是解法 —— 第一轮（不等长供体）就是那样，
换来的差异来自截断，于是 (c) 行读出 +0.70。

⇒ 按冻结分支**关闭，冠军未读**。⛔ 这**不是**「冠军没有 tempo 响应」（P2 **未被回答**，不是答了否）；
⛔ 也不是「这里量不了 tempo」（raw 又大又干净，缺的是能把人数和时钟分开的零假设）。
⭐ 重做要的是**场内反事实 = 干预**（G4c 的形状）：在 tick T 分叉一场比赛，只在一支上抹掉一个人，
让同一批玩家和**未分叉的自己**在同一时钟下比速度 —— 那是新的预注册和另一件仪器。

⭐⭐ 方法教训（已进 GOTCHAS #35，`donor agree/∩` 列是它的闸）：**跨场置换只有在「被置换的那个量不是时钟的函数」时才是零假设**。
这条比本 detector 宽 —— trade 的第二版零假设、rotate 的压力时间线、radiouse 的 `replay` 全是同一族。
⛔ 关闭的 detector 从此**不打印冠军行**（`--reopen tempo,rotate` 才打开，且只在配新预注册时才合法）——
crossfire v1 那次「冠军与对照同表打印」的污染是量过代价的。

## [2026-09-15 13:20] G2 第五个 detector（lurk / 孤立路径）precision 过了 —— **四个冠军里有两个带稳定的「单人脱离支援」形状**  #measure

预测 `runs/g2-lurk-predictions.txt`（实现之前冻结）+ addendum（precision 之后、读冠军之前写的）。
⭐ 选这个形状的理由就是 tempo 的教训（坑 #35）：它是**空间**问题，零假设可以做成**场内 slot 标签置换** ——
轨迹、打斗、时钟全部原样保留，只打掉「跨比赛的身份对齐」。

**两个数都是从世界里量出来的，不是拍的**：支援半径 **12 m** 直接沿用 trade detector 冻结过的「算同一场打斗」半径；
episode 门槛 **2.0 s** 来自实测的一场单挑长度（首次中弹→死亡 p50 1.40 s / p75 1.93 s）⇒ 2 秒 = 「一场架从开打到结束都没人能来帮忙」。
参照尺度：手写 bot 的最近队友距离 **p50 只有 1.0 m**，只有 0.7% 的存活 tick 身边 12 m 内没人。

**一处 harness 修复，在对照里**：`spread`（role−）原本 slot→post **固定**，而站在攻方路线上的那两个 post 每场都早死
⇒ 1/3/4 号按构造独占孤立时间，对照自己带 role（30/9075/126/9075/9651，top slot 1.42× 零假设）。
改成**逐场轮转** post 分配后变成 74/75/74/77/74、0.91× ✓（和 `npm run identity` 的拉丁方是同一招）。

**P1 ✅ 4/4**：one wide 9% / top 100% = **3.08×** · bunched **0%** · spread 75% 但 top **0.91×**。⇒ 按冻结分支读冠军（仅描述）：

| 冠军 | 可计 tick | 孤立占比 | episodes | 中位时长 | 逐 slot 孤立% | top slot | 零假设 | ×null | episode 内速度 | 打出/挨打/击杀 |
|---|---|---|---|---|---|---|---|---|---|---|
| **s1 红** | 22892 | **10%** | **24** | **6.1s** | **47/0/0/0/0** | 100% #0 | 32% ± 0.4pp | **3.17** | **2.82 m/s** | **390/442/4** |
| s1 蓝 | 38155 | 1% | 2 | 4.5s | 0/0/0/0/3 | 93% #4 | 80% ± 0.5pp | 1.16 ⚠不可读 | 3.73 m/s | 0/0/0 |
| s2 红 | 20474 | 0% | 1 | 2.0s | 0/0/1/0/0 | 100% #2 | 89% ± 0.3pp | 1.13 ⚠不可读 | 3.45 m/s | 0/0/0 |
| **s2 蓝** | 29846 | **5%** | **11** | **8.0s** | 2/**20**/0/4/0 | 79% #1 | 39% ± 0.6pp | **2.01** | 1.75 m/s | 0/26/0 |

⇒ **P2 预测错了，而且错在我没料到的方向**（我押 Q1 < 5%、Q2 不超零假设）。**s1 红的 0 号**在 24 场里出现 **24 次** episode，
**占它自己可计 tick 的 47%**，另外四个 slot 是**literal 0%** —— 集中度 3.17× 与**手写 lurker 对照的 3.08× 同级**。
⚠ s1 蓝 / s2 红只有 2 次和 1 次 episode，它们那两个 1.16×/1.13× **不可读**（几乎所有孤立 tick 挤在一场里，置换也会跟着集中，
所以它们的零假设本身就坐在 80–89%）—— 报「没有形状 + 角色读不了」，⛔ 不报「没有 lurker」。

**事后消歧**（看到结果之后加的，因为冻结的 LIMITS 自己要求：「走丢的、被几何堵死的，读数一模一样」；⛔ 只描述、不进任何闸）：
s1 红 0 号在 episode 里**跑 2.82 m/s、打出 390 伤害、挨 442、拿 4 个人头** ⇒ 是个**在动、在打**的身体，不是卡住的
（停在 post 上的 spread bot 读 0.05 m/s，手写 lurker 读 2.10 m/s）。而 s2 蓝 1 号**在动但完全不打**（0 伤害 0 击杀）⇒
那是**孤立游荡**，不是包抄。⭐ 两个冠军带的是**不同形状**，⛔ 不能合并成一句话。

⇒ 这是 G2 的**第一个非空读数**，但按 claim ladder 只到 **CANDIDATE**：⛔ 不是「它们进化出了 lurker」（form ≠ function）。
还差两级，各自需要新的预注册：**birth**（同一 detector 扫世代，找 0 号这个模式什么时候出现的）·
**intervention**（把 slot one-hot 在 0 号和队友之间对调 —— 五个身体共用一个脑子，「lurker」只可能住在 slot 输入里，
看它跟着 **slot** 走还是跟着**身体**走）。

## [2026-09-15 13:50] lurk 的 birth（VISION §12.1 层①）：**slot 0 的角色在第 72 代锁定，一路守到 299 代**  #measure

预注册 `runs/g2-lurk-birth-predictions.txt`（跑之前冻结）。⭐ 选 birth 而不是干预，是拿 VISION §12.1 过了一遍：
层① Detection 本来就包含「第一次出现 / 怎样逐代稳定 / 能回放到具体 match·player·lineage」——上一轮只答了前半句。
⛔ detector 的定义、半径、时长、零假设、门槛**一个字没动**，只加了 `--gens` 与 `--opponent-gen`。

两条臂分别问不同的问题：**A = red@G vs blue@299 固定对手**（动的只可能是被观测这条谱系）·
**B = red@G vs blue@G 实际共演化的那一对**。⚠ 预注册写死：两臂不一致就报「对手依赖」，⛔ 不许平均。

| 代 | 0 | 25 | 50 | 75 | 100 | 150 | 200 | 250 | 299 |
|---|---|---|---|---|---|---|---|---|---|
| A：slot0 占自己 tick | 1% | 0% | 0% | **65%** | 71% | 85% | 34% | 21% | 47% |
| B：slot0 | 0% | 0% | 6% | **61%** | 69% | 83% | 50% | 30% | 47% |
| top slot（A） | #4 | #4 | #4 | **#0** | #0 | #0 | #0 | #0 | #0 |

细扫 52–72（每 4 代，两臂）：slot0 = 0/0/**15–29%**/0/**13–14%**/**62–64%**，集中度 = 1.85/0.97/2.86/1.24/2.78/**3.22**。

⇒ **两臂逐代一致（P2 ✅）**，读法：
- **第 0 代是预注册过的退化混淆，⛔ 不能当形状读**：13% 孤立、中位 episode 17.3 s，但 episode 内只有 1.25–1.61 m/s、
  全行 0–26 伤害 —— 那是**谁都不接敌**（坑 #13），于是谁都离谁很远；而且它在 slot 4 上。
- 25–56 代：没有角色（1.4–2.9× 在 slot 4 上晃，slot 0 是 0%）。
- **60–68 代：前兆**。slot 0 冒到 13–29%、2.5–2.9×，但**身份在闪** —— 64 代又没了（换成 slot 2/4）。
  按我自己冻结的分支，这段算 **drift 不算 birth**。
- **72 代锁定**：slot 0 拿下自己 62–64% 的可计 tick、**3.22×**、24 次 episode，此后**每一个采样代、两条臂**都在 slot 0，
  一直到 299 —— **227 代**。
⇒ **层① 完成**（第一次出现 ≈ 60 代、稳定自 72 代、每行都带可回放锚点，如 72 代 B 臂 `seed 1130297674 atk0 t44`）。

⚠ **P1 我猜错了一半**：我押「共享大脑一次挪一点偏置 ⇒ 渐变」，实际是**台阶**（60→72 代，0% → 62%，12 代完成），只是前面有一段闪烁的前兆。
⭐ **两臂分歧本身是结论**：150–200 代在**匹配代**臂上会出现**第二个**脱离支援的 slot（150 代 slot 2 占 54%、200 代 slot 0 和 4 各 50%），
集中度被拉到 1.75–2.38×；而同一批冠军对**固定的 299 代对手**仍然只派一个（94%、64%）。
⇒ **派几个人去游猎是对手依赖的，派谁不是**。

⚠ 限制（写进预注册，别忘）：每一代只有**一个** hall-of-fame 冠军基因组 ⇒ 64 代那次闪烁可能只是一个不具代表性的冠军，
⛔ 不等于「种群那一代丢了这个性状」；「72 代锁定」说的是**冠军线**，不是种群。
⛔ 仍然只到层①：它说的是**何时**与**多稳**，不是**为什么**与**干什么用**。层③ 的 slot one-hot 对调仍未跑，仍需单独预注册。

## [2026-09-15 14:30] lurk 的干预（VISION §12.1 层③）：**角色不跟身份输入走，跟出生位置走 —— 而且它要的是「训过的那一对」**  #measure

预注册 `runs/g2-lurk-swap-predictions.txt`（实现之前冻结）。新脚本 `npm run lurkswap`，detector 定义**一个字没动**，
只干预**被观测那一队**，对手在每条臂里都正常打。五个身体共用一个基因组，区分它们的只有 `self.slot` one-hot，
而 `map.spawns[team][slot]` 把同一个下标也发给了出生点 ⇒ 角色只可能住在这两个地方，三条臂把它们拆开：
**A 对照** · **B 换 one-hot**（身体不动，读到的身份对调）· **C 换出生点**（身份不动，身体换个地方开局）。

**先记两件方法上的事**：
① **我自己的读数有 bug，在当成结果读之前抓到**：第一版两个标签都按 agent 下标索引，而 C 臂里那个下标**就是** one-hot
⇒ 「by spawn」那列静默地把 carrier 归因印了两遍，A/C 两行和汇总全都读成「spawn == carrier」。
**两列完全一致就是坑 #26 的那个信号**。改成显式维护 `carrier[]` 与 `spawnOf[]` 两张表。
② **事后补了一条 placebo 臂**（⚠ 不在冻结设计里，写清楚了）：把**不承载形状**的两个 slot 的 one-hot 对调（1↔2 / 2↔3 / 3↔4）。
B/C 单独说不清「动 0 号的身份会打散形状」和「任何越界的 one-hot 编辑都会打散一切」—— placebo 只可能削弱结论，所以补得起。

| 臂 | 孤立占比 | slot0 按**出生点** | slot0 按**身份** |
|---|---|---|---|
| A 对照 | 10% | 100% | 100%（同一个身体） |
| **P placebo**（换两个非 lurker 的身份） | **10%**（与对照相同） | **90% ± 9.9pp** | 90% |
| B 换 one-hot（k=1..4） | 2–8% | **29% ± 19.3pp** | **6% ± 5.9pp** |
| C 换出生点（k=1..4） | 4–7% | **54% ± 22.3pp** | **8% ± 4.7pp** |

⇒ 三条结论，按冻结分支读：
1. ⛔ **角色不跟身份输入走**。两条臂里「读着 one-hot 0 的那个身体」都几乎什么都没扛（6%、8%），
   而同样喂了没训过的身份配对、只是没喂给 lurker 的 **placebo 完全不掉**（90%，占比仍是 10%）
   ⇒ **OOD 这条反驳被答掉了**：打散形状的是动了 0 号**自己那一对**，不是「编辑」这件事。
2. **但也不是纯几何**。若角色只是位置，C 臂（身体仍从 0 号点出生，只是身份不同）应该读得和对照一样，
   实际**两条臂的总孤立都腰斩**（10% → 2–8%）⇒ **这个形状要的是训过的那一对 (spawn 0, one-hot 0)**，拆掉任一半就少一半。
3. **拆完之后剩下的那部分挂在出生位置上**（29%/54% vs 6%/8%），两条臂方向一致 —— 这正是预注册要求的「两臂必须给同一个答案」。
⇒ **「lurker」是从那个侧翼出生的那个身体，它的 one-hot 是训练出来的配对的一半，不是一个可搬运的身份。**
⭐ 这对 SUBSTRATE 的 Club / Player-block 方向是**降温**的读数（预注册里就写明了这条分支意味着什么）：
**此处的 player block 还不是一个能转会的东西**。

⚠ 限制（引用时别丢）：只做了**一个**冠军（s1 红 @299；s2 蓝那个「只游荡不开火」是另一种形状，没做干预）；
跨 k 很不均匀（B 臂 by-spawn 在 0–86% 之间、C 臂 15–100%，k=2 两条臂都保住形状，k=1/k=4 都打散），
**± SE 是对四个 k 求的，⛔ 不是紧估计**；⛔ 意图从来不在这条阶梯上。

## [2026-09-15 15:05] G2 第六个 detector（pair）precision 过了 —— **四个冠军都有稳定互选搭档，其中两个「跳过邻居」**  #measure

预注册 `runs/g2-pair-predictions.txt`（⚠ 开头写明**先污染再预注册**：为了确认「抱团的队伍还有没有结构可量」，
我先跑了一次世界尺度探针，那次**已经看到** s1 红的最近邻矩阵 ⇒ 那一行不是先验，P2 只对**没看过的另外三个**冠军有效）。

⭐ 定义绕开了饱和问题：**距离阈值在这里没用**（冠军两两距离 p25 3.3m / p75 13.0m、32% 的 pair-tick 在 4m 内，
「在不在一起」恒真），量的是**谁是谁的最近**——这是**关系**不是距离，抱团里依然有信息。
互选对 strength = min(P(i→j), P(j→i))，零假设仍是**场内 slot 标签置换**（不碰时钟，坑 #35）。

**两处修复都在负对照**（⛔ 定义与门槛没动）：
① **循环轮转不足以拆散一个对照** —— `(i+m)%5` 下**两个 slot 的差是不变量**，几何上的那一对每场换个标签照样复现；
再叠上「站在哪个 post 决定能活多久」，每个 slot 的**分母**恰好在「它和同一个人站一起」的那些场次里被填满
⇒ 一个**按构造没有搭档**的对照读出 60%（零假设 35%）。改成**逐场随机置换**（没有不变量可藏）。
② 同一 seed 的两种攻守共用一份置换，而零假设是**逐场独立**置换 ⇒ 多出一截零假设没有的跨场对齐。置换改成也吃攻守。
⇒ 负对照 60% → 45% → **32%（零假设 34%，0.94×）✓**，正对照 100%（2.75×）不变。**P1 ✅ 3/3**。

⭐ **邻接是量出来的不是猜的**：出生点是一条直线、按 slot 序每 4m 一个，两队都核过 ⇒ 一对的出生间距 = 4m × |i−j|，那一列直接印米数。

| 冠军 | 最强互选对 | strength | ×null | 出生间距 |
|---|---|---|---|---|
| s1 红 | **1-3** | 64% | 2.14 | **8m（跳过中间那个）** |
| s1 蓝 | 0-1 | 91% | 2.55 | 4m |
| s2 红 | **0-2** | 56% | 1.72 | **8m（跳过）** |
| s2 蓝 | 0-1 | 71% | 2.05 | 4m |

⇒ P2 对了一半：「没看过的三个里至少两个 ≥1.5×」✅（三个都是）；「强对多半是出生点邻居」❌ —— 四个里两个不是，
而且**分界线是颜色**：两个红冠军都跨 8m 配对，两个蓝冠军都和邻居配对。⚠ 每种颜色只有 n=2，这是**值得命名的模式，⛔ 不是关于颜色的结论**。

⭐⭐ **这是本程序里第一个「出生几何本身解释不了」的形状**：上一轮干预刚证明 lurk 角色跟**位置**走，
那就预测搭档应该是**相邻**出生点 —— 两个红冠军偏偏跳过中间那个人。
⇒ 按冻结分支只到 **CANDIDATE**，下一步是把 `scripts/lurkswap.ts` 的三臂套到**pair 统计**上（这对跟两个 one-hot 走还是跟两个位置走），需**单独预注册**，⛔ 本轮不跑。
⛔ 命名纪律：「pair coordination」暗含共同意图，**不被授权**；测到的东西的诚实名字是**「反复出现的互选最近邻对」**。

## [2026-09-15 15:45] pair 的干预：**同一个冠军里，lurk 角色跟位置走，而搭档关系跟身份走**  #measure

预注册 `runs/g2-pair-swap-predictions.txt`（跑之前冻结，⭐ **placebo 这次一开始就在设计里**，不像 lurk 那次是事后补的）。
复用 `lurkswap` 的三臂，用 `--focus i --partner j` 重新对准一对搭档：交换只发生在**这对之外**，placebo 也只在这对之外。
pair 统计的定义**一个字没动**，只是同时按 **carrier（读到的 one-hot）** 和 **spawn（占的出生位）** 两套标签归因。

| 冠军 | 搭档 | 出生间距 | 结论 | 证据 |
|---|---|---|---|---|
| **s1 红** | 1-3 | **8m** | ⭐ **跟身份走** | C 臂 by carrier **60% ± 2.5pp** vs by spawn **1% ± 0.8pp**；**六条臂里 by-spawn 全 ≤ 3%** |
| s1 蓝 | 0-1 | 4m | **跟位置走** | B 臂 by spawn 76% ± 15.3pp vs by carrier 1% |
| s2 红 | 0-2 | 8m | ⚠ **归因不了** | 两臂都塌，**且 placebo 也把它打没了**（0%/1%/9%）⇒ 任何编辑都留不下它 |
| s2 蓝 | 0-1 | 4m | **跟位置走** | B 臂 78–81% by spawn、by carrier 9–13%，placebo 完好（78–81%）|

**中途我把假说写进了预注册再去跑最后两个**（这正是写在那儿的意义）：看完前两个后我押「**分界线是邻接不是颜色**
—— 相邻出生点的搭档是几何、跳过邻居的搭档跟身份走」，并预言 s2 红 = 身份、s2 蓝 = 位置。
**s2 蓝 ✅，s2 红 ❌**（它是「归因不了」不是「跟身份走」）⇒ 假说**只过了一半的检验**，⛔ 我不能事后改轴的名字。
能说的只有：**两个相邻搭档都干净地跟位置走（2/2，两臂一致、placebo 完好）；两个非相邻搭档里，一个跟身份走，一个任何干预都活不下来。**

⭐⭐ **真正的发现，按证据强度说**：**s1 红这一对跟身份输入走，不跟出生位置走**（by carrier 60% ± 2.5pp，
而 by spawn 在**每一条臂**上 ≤ 3%）。这是本程序里**第一个住在身份里而不是几何里的形状**，
而且它和同一个冠军的 lurk 结论**正好相反**（那个角色跟出生位置走）。
⭐⭐⭐ **同一支队、同一个基因组、两种形状、两个不同的载体** —— 这也顺手排除了「这个冠军就是纯位置/纯身份驱动」这种偷懒解释。
⚠ **复现状态：n=1 干净 + n=1 归因不了。⛔ 谁都不许在不带这两个数字的情况下引用成「搭档住在身份里」。**
下一步是**更多冠军**（别的 seed、别的世代），⛔ 不是给同一份证据换个更强的词。
⛔ 仍然不叫 "pair coordination"，也仍然和意图无关。

## [2026-09-15 16:20] ⛔ 更正上一条：**「搭档住在身份里」撤回** —— 我把「没被打散」当成了「跟着它走」  #measure #incident

复现集（d2s-pop48 s1/s2、d2s-eval15 s1/s2 @99，四条**独立训练**的谱系）跑完之后，我发现**上一条的判读有推理错误，而且是我自己的**。

**先说复现集**：八个冠军里有五个的最强搭档过了 1.5× 零假设，⚠ **五个全是相邻（4m）**；那一批里两个非相邻的都没过线
⇒ 「跟身份走」那个模式**没有拿到新的检验对象**。五个里有四个在**两条臂下都把搭档留在同一批 agent 身上**
（B 臂 by spawn 高 **且** C 臂 by carrier 高：50/35 · 44/57 · 64/63 · 35/65），一个（eval15-s1 蓝）placebo 掉得和实验臂一样多 ⇒ 归因不了。

⭐⭐ **错在哪**：**两条臂都只拆掉被动的那半边配对** —— B 臂保留出生位、换身份；C 臂保留身份、换出生位。
于是「搭档**还在同一个 agent 身上**」正是「**掉了一半配对之后行为温和退化**」会给出的结果 ——
而这个温和退化**上一轮 lurk 干预已经量到了**（总孤立腰斩但没归零）。
⇒ **「没被打散」不是归因，「搬家」才是**：要说跟某个标签走，搭档必须出现在**现在持有那个标签的另一个 agent** 身上。
- 「跟出生位走」要看 **C 臂的 by spawn 高**（搬到了接手位置的那个身体）；
- 「跟身份走」要看 **B 臂的 by carrier 高**（搬到了接手身份的那个身体）。

**按这条规则重判（⛔ 数字一个没改，改的是推理）**：

| 冠军 | 搭档 | 原判 | **重判** | 依据 |
|---|---|---|---|---|
| s1 蓝 | 0-1 | 位置 | **位置 ✅ 成立** | C 臂 by spawn 87%/92%（**搬到**接手位置的身体上） |
| s2 蓝 | 0-1 | 位置 | **位置 ✅ 成立** | C 臂 by spawn 68/76/89% 三条全中 |
| **s1 红** | 1-3 | ⭐ 身份 | ⛔ **撤回 → 未确立** | C 臂那个 60% 是**留存不是搬家**；唯一的搬家证据是 B 臂 k=2（69%），而 k=0/k=4 直接把它打散（0%、16%） |
| s2 红 | 0-2 | 归因不了 | 不变 | placebo 也打没 |
| pop48-s1R / pop48-s2B / eval15-s1R / eval15-s2B | — | — | **归因不了**（只有留存） | 两臂都留在同一批 agent 上 |
| eval15-s1B | — | — | 归因不了 | placebo 掉得一样多 |

⇒ **整个 pair 程序的诚实结论**：九个「有搭档」的冠军里，**只有两个能正向归因，而且两个都归给出生位置**。
⛔ **没有任何一个冠军在「搬家」证据上显示搭档跟身份走。上一条那句「第一个住在身份里而不是几何里的形状」——撤回。**

⭐ 真要检验它，需要**新的一臂**：把被动 agent 的**整份配对（出生位 + one-hot）一起搬走**，让它一半都不剩，
再问搭档跟不跟这个「捆」走。另外，两条臂都没动的那个按 slot 挂的通道是**感知噪声键**
（`viewer.slot * 31 + target.slot`、音频 `listener.slot * 97 + …`）；⚠ **队友排序是按距离排的、不按 slot**，所以不在其中。
两者都要各自预注册，⛔ 本轮不跑。

⭐⭐ 方法教训已进 **GOTCHAS #37**：**干预只拆掉「被测对象的一半」时，「它还在」什么也证明不了** ——
只有「它出现在现在持有那个标签的**另一个**对象身上」才是归因证据。

## [2026-09-15 17:05] ⛔⛔ 再更正：arm C 有**真 bug**（`--focus≠0` 时换错了出生点），修好后结论**更干净**  #measure #incident

上一条（16:20）的推理规则没错，但它依据的一部分数字是**错的实验**。

**bug**：`swappedSpawnMap(k, from = 0)` 的默认参数把 `from` 钉死在 slot 0 ⇒ `--focus` 不是 0 时，
arm C 换的是 **0↔k** 而不是 **FOCUS↔k**。九次 pair 运行里**有五次**中招（s1红 focus 1、pop48-s1红 1、pop48-s2蓝 1、
eval15-s1红 3、eval15-s2蓝 2）——它们的 arm C 量的是**另一个身体**上的干预。
⭐ **为什么闸没拦住**：落地断言拿来比对的期望值是**自己用同一个错误默认重新构造的那张地图**（不是真正被播的那张），
而且容差 **8m** 架在 **4m** 的出生点间距上 ⇒ **它断言的是错的对象，还断得很松，于是一路绿灯**。
现在改成断言「focus 身体开局站在**对方 slot 的原始出生点**上」，容差 2m（一 tick 只走 ~0.4m）。已进 **GOTCHAS #38**。

**九个全部重跑（按坑 #37 的搬家规则打分，留存列照印但不计分）**：

| 冠军 | B 臂 by carrier | **C 臂 by spawn** | placebo（对照） | 判定 |
|---|---|---|---|---|
| s1 红 1-3 | 28% ± 20.8 | **0% ± 0.3** | 47%（64%） | **不是位置**；身份证据弱且散 |
| s1 蓝 0-1 | 1% | **70% ± 19.4** | 63%（91%） | **位置** |
| s2 红 0-2 | 16% | 7% | **3%（56%）** | 归因不了（placebo 打没） |
| s2 蓝 0-1 | 9% | **78% ± 6.2** | 79%（71%） | **位置** |
| pop48-s1 红 1-2 | 13% | 31% ± 25.8 | 39%（66%） | 归因不了（没有干净的） |
| pop48-s2 蓝 1-2 | 5% | **38% ± 5.5** | 57%（68%） | **位置** |
| eval15-s1 红 3-4 | 3% | **41% ± 11.2** | 59%（65%） | **位置** |
| eval15-s1 蓝 0-1 | 16% | 34% | **30%（49%）** | 归因不了（placebo 掉一样多） |
| eval15-s2 蓝 2-3 | 7% | **43% ± 19.1** | 72%（64%） | **位置** |

⇒ **变了什么**：上一条里我给四个复现冠军贴的「只有留存 / 跟 agent 下标走」这一类，**是 bug 造出来的**。
arm C 对准正确的身体之后，它们显示的是**跟位置搬家**（by spawn 38–43% vs by carrier 3–8%）。
⇒ **修正后的统计：九个里五个能正向归因，全部归给出生位置；三个归因不了（两个是被自己的 placebo 判的）；
一个（s1 红）不是位置，身份证据弱（28% ± 20.8）。**
⇒ 上一条的核心结论 —— **没有任何冠军在搬家证据上跟身份走** —— **不变，而且现在有五个干净的位置归因撑着，不是两个**。

**arm D（整份配对一起搬，预注册过的自我检验）如预期**：s1 红 35/42/59% vs 对照 64%（和 placebo 的代价相当），
eval15-s1 红 61/68/52% vs 65% ⇒ **位置和身份一起换走时，搭档跟着这个「捆」走**，
说明两条臂都没动的那个按 slot 挂的通道（**感知噪声键**）**什么都没扛** ⇒ **双标签分解是成立的**，这正是 D 臂被预注册来检验的事。

⚠ 坑 #37 那条规则**不受这个 bug 影响**，而且正是它让修正后的打分站得住：
B 臂的 "by spawn" 列在这批冠军上仍读 34–81%，而它**仍然什么都不说明**。

## [2026-09-15 17:50] 收掉最后一条线索：s1 红那对**确实跟身份走，但只属于 299 代那个基因组**  #measure

预注册 `runs/g2-pair-identity-predictions.txt`。上一轮留下的唯一悬案：九个冠军里只有 s1 红的 1-3「不是位置」，
但身份证据只有三次交换里的一次。⛔ 我没有用「加大 n 再看」——三次交换互相矛盾是有原因的，原因要量出来。

**两处仪器修正（定义未动）**：
① **给「搬家」加基线**：说「搭档搬到 k 身上」只有相对于「(k, 搭档) 这一对本来就有多少」才有意义。
本冠军对照：1-3 = 64%、1-2 = 32%、2-3 = 18%。加了基线列之后，原方向的 k=2 是 69% vs 基线 **18%**（+51pp），
k=4 的 16% vs 基线 11% 则**什么都不是**。
② **对称检验**（真正的新证据）：之前动的都是 slot 1 的身份。若搭档真跟身份走，**动另一半也该一样** ⇒ `--focus 3 --partner 1`。

**对称检验 P1 ✅**（我只押了 55%）：把 one-hot 3 给谁，谁就跟「读 one-hot 1 的那个身体」配对，**三次全中且全部高于自己的基线**：
k=0 **60% vs 2%**（+58pp）· k=2 **73% vs 32%**（+41pp）· k=4 **27% vs 2%**（+25pp）。
配合原方向**六条臂 by-spawn 全 ≤ 2%** ⇒ **299 代这一对是跟身份输入走的**。
⚠ **但有一处不对称，是真限制不是噪声**：身份 **1（锚）只转得动相邻的 2 号身体**，给 0 号（lurker）或 4 号都是把这对**打散**而不是搬走；
身份 **3（搭档）给谁都转得动**。⇒ **锚那一半仍然要它的位置，只有搭档那一半是可搬运的** —— 和 lurk 那边「训过的那一对」是同一个形状。

**世代稳定性 P2 ❌，而这正是给结论划界的那一条**：1-3 这对**更早的世代根本不存在**。同一谱系逐代的最强互选对：
150 代 **3-4（83%，相邻）** · 200 代 **1-2（56%，相邻）** · 250 代 **3-4（52%，相邻）** · 299 代 **1-3（64%，跨 8m）**
⇒ **搭档大约每 50 代换一次人**，那个非相邻的一对是**299 代才发生的事**。
（⚠ 探索性、未预注册、只描述：更早那两对相邻搭档**都是跟位置走的** —— 150 代 3-4 的 C 臂 by spawn 41% ± 16.6 vs by carrier 7%；
200 代 1-2 是 31% ± 14.3 vs 2%。）

⇒ **按冻结分支能说的**：**在 s1 红 299 代这个基因组里，互选搭档是跟身份输入走的，不是跟出生位置走的；
这是那个基因组的性质，⛔ 不是这条谱系的性质** —— 五十代之前同一条谱系的搭档是另一对、相邻、且跟位置走。
⭐ **本轮真正的结论是那个对比**：**位置型的角色（lurk 的 0 号）稳了 227 代；身份型的搭档只出现过一次、很晚、而且不稳定。**
⛔ 引用身份那条时必须带上「仅 299 代」和「锚/搭档不对称」两个限定。

## [2026-09-15 18:40] G3 chunk 1：style space 建成（`npm run style`）—— 而它最先抓到的是**我自己的两次误读**  #measure

预注册 `runs/g3-style-predictions.txt`。G3 要的是「肉眼看见这队学会了什么」，最便宜的诚实版本 = 离线、复用现有 run：
逐代一条风格向量 + 二维嵌入 + **噪声底**（没有底的轨迹图只是把采样噪声画成趋势线）。

⭐ **特征不是新造的**：直接用 `src/evo/match.ts` 里已有的 **22 个 `METRIC_KEYS`**（accuracy / spread / engageDist /
coverRatio / aimUsage / moveFraction / commActivity / firstContact / attack·defendProgress …），
这样风格的坐标轴就是仓库别处已经在画的那些数。**对手固定**为另一色 @299（birth sweep 的 arm-A 教训：对手会动时，
「风格变了」和「对手变了」是同一个数）。

**两处读法错误，都是我自己的，都已修成脚本里的闸**：
① **不等间距扫描**：第一版扫了 0,20,40,60,70,72,80,100,150,200,250,299，然后把跨 2 代、10 代、50 代的「step」并排读。
跨 50 代当然更大。⇒ 现在打印 gap，且**不等间距时大声警告**；本轮结论全部来自均匀网格。
② ⭐⭐ **噪声底比我写的窄，而这是量出来的不是想出来的**：`world.rng` **只用在命中判定**（和曳光弹外观），
感知抖动是 (slot key, 时间桶, 常数) 的确定性哈希，地图和出生点又是固定的 ⇒ **一支从不开火的队伍，换种子等于同一场比赛**。
所以这个底是**「重掷命中骰子」的底**，不是「重跑世界」的底。后果：**不接敌的冠军 own-noise = 0.00，它邻居的 step 会显得巨大**。
⇒ 现在噪声底**逐代**用（取一步两端中较大的那个），且 own-noise < 0.3 的行直接标 `⚠ det — ratio inflated, do not read`。

**结果（均匀 20 代网格、四条冠军轨迹、固定对手）**：噪声底 1.65–3.04 z 单位；
清过各自局部底 2× 的步数：s1 红 3（另 2 步被判 det）· s1 蓝 4 · s2 红 6（另 2 det）· s2 蓝 7（各 14 步中）。
⭐ **四条轨迹都在最早那一步动得最凶**（0→20 或 20→40）——年轻种群本该如此，这是仪器通过的一个 sanity check。
⭐⭐ **而 s1 红看起来的「头条」是假的**：gen 160 的 PC1 = −9.61、两侧是全扫描最大的两步，但它的 own-noise 只有 **0.08**——
那个冠军**几乎不接敌**。**没有逐代噪声底的话，这会被当成这条谱系最大的时代分界线报出去。**

**评分**：P1（多数步清不过底）**对一半**，实际约一半能清。
**P2（gen 72 附近有分界线，对应 lurk 角色锁定）** ❌ **未确认** —— 5 代一格的细扫（50–100 代）显示 **65 代之后几乎每一步都清过底**
（2.3–3.1×），即那个窗口是**连续churn**而不是单一分界。⇒ style 这台仪器对 gen 72 既不背书也不否定。
⛔ 当时很容易指着 65→70 那一步说「两台仪器互相印证」——是均匀扫描把这条路堵住的。

⇒ **chunk 1 交付** = `npm run style`：逐代风格向量 + 2D 嵌入（含解释方差与载荷）+ 逐代噪声底 + 两道防我自己误读的闸。
⛔ 它还答不了「这个时代为什么克上一个时代」——那要**对过去自己的 cross-play 矩阵**，是 chunk 2（连同 lineage）的事。
⚠ 下一步要先把**噪声底做强**：现在只重掷骰子，真正该变的是**地图**（`crossplay --maps` 已有先例）。

## [2026-09-15 19:15] G3 chunk 1 的外部效度检验：**「哪几代动了」是地图特有的** —— 两条预测都错，结论要给整个 G3 划界  #measure #incident

预注册在 `runs/g3-style-predictions.txt` 末尾。⭐ **先纠正我自己的用词**：我之前写「把噪声底做强 —— 该变的是地图」，
那是**对的测试、错的名字**。地图固定、出生点固定、策略确定 ⇒ 本来就没有多少采样噪声可找；换地图买到的是**另一个问题**：
**这条风格轨迹是谱系的性质，还是这一张地图的性质？** 那不是底，是**外部效度**。叫它「底」会把这件事藏起来。

同一套均匀 20 代网格，s1 红，三张地图（7 = 训练图，11、23）。按各自的 YES 比例算出偶然一致水平后比对「这一步动没动」：

| 地图对 | 一致 | 偶然水平 |
|---|---|---|
| 7 vs 11 | 5/12 = **42%** | 50% ⇒ **不高于偶然** |
| 7 vs 23 | 10/12 = 83% | 62% ⇒ 高于偶然 |
| 11 vs 23 | 6/14 = **43%** | 50% ⇒ **不高于偶然** |

⇒ **P1 ❌**（我押 60–80%）：**哪几代动了，换张图就不一样**，三对里两对落在偶然水平上。
⚠ P1 里那条子预测倒是成立：**最早那一步（0→20）在三张图上都清过底** ——「年轻种群变化大」不是地图的事。
⇒ **P2 ❌**：被标 `det` 的冠军**换图就不 det 了** —— s1 红 gen 160 在训练图上 own-noise 0.08，在 11 / 23 号图上是 1.27 / 1.45。
**它是在自己长大的那张图上不接敌，换张图就打。** ⇒ `det` 是**「这个冠军在这张图上」**的性质，不是冠军的性质，脚本措辞已改。

⇒ 按冻结分支能说的：**chunk 1 的每一句风格结论都必须带「在训练图上」这个限定**，
⛔ **chunk 2 不得拿这条轨迹去讲「时代」**。唯一跨图成立的陈述是「最早的几代动得最凶」。
⭐⭐ **这是最容易悄悄弄错的一种错**：chunk 1 那张表一个数都没变、看起来依然权威，而且本来会被当成这条谱系的历史讲出去。
它现在被贴上限定，只是因为同一套扫描在另外两张图上又跑了一遍 —— 代价 30 秒。

## [2026-09-15 20:10] G3 chunk 2a（eras）：**蓝方的进步阶梯换图不变，红方的只存在于训练图上**  #measure

预注册 `runs/g3-eras-predictions.txt`。⛔ **没有新造矩阵**：`crossplay` 早就有 `--gens every:N`（打过去的自己）、
`--maps`、双色对打、逐格接触分母、`··` 非测量、thin 标记、决定性边与**非传递环**；
`crossplayFile.ts` 还写明「下游不得重算矩阵/边/环」。所以新脚本 `npm run eras` 只**消费** `rowMean/balanced/sight/cycles`。

跑法：`--gens every:60 --n 12 --maps 7,11,23`，12 个 entrant，每格 24 场（自带分辨率：真 50/50 的对子九成落在 ±17pp 内）。

**进步（Spearman(代数, rowMean)）**：

| | 训练图 7 | 图 11 | 图 23 |
|---|---|---|---|
| 红 | 0.83 | 0.43 | **0.03** |
| 蓝 | **0.94** | **0.94** | **0.94** |

**跨图序稳定性（Kendall tau，全部 12 个 entrant）**：7v11 **0.52** · 7v23 0.64 · 11v23 **0.39**
⇒ **P2 ❌，冻结的闸没过**，⇒ 按冻结分支：**本轮不授权任何「时代」结论**。这条判定照写不误。

⚠ **事后补的一格（标清楚了，绝不能当成过闸）**：冻结的 tau 把两色混在一张排名里，恰好把关键的事盖住了。**逐色**看：
红 0.47 / 0.20 / 0.47（全部 < 0.6）· 蓝 **0.73 / 1.00 / 0.73**（全部 ≥ 0.6，训练图 vs 图 23 是**完美一致**）。
⇒ **失败不是均匀的：一边的时代阶梯换图完好，另一边的是训练图产物。**

**P3（至少一个非传递环）✅**，但幅度没预料到：**环数按图是 0 → 1 → 22**。离开训练图，非传递性爆炸。
⭐ 和 tau 表一起读：图 23 上这批冠军**既排不出稳定的序、又满地石头剪刀布** —— 那就是「大家在这儿都很平庸」的样子。

⇒ 能说的：⛔ 本轮**没有**时代结论；✅ 但有一条现在被量到而不是假设的方法结论 ——
**本项目的「进步」主张必须逐色做，并且要在训练图之外复核**，因为这里一边的阶梯换图不变、另一边的不是。
chunk 2b（lineage）可以开工，但 ⛔ 在通过逐色的闸之前只能做**图内**比较。

## [2026-09-15 21:05] G3 chunk 2a 的逐色复测：**四条轨迹全部没过闸,「蓝方特殊」是 6 个采样点撑出来的运气**  #measure #incident

预注册 `runs/g3-eras2-predictions.txt`（冻结在跑之前）。上一轮的闸没过、事后按颜色拆才看出蓝稳红不稳——**那是事后的,不能当证据**。
这一轮把它写成**冻结的逐色闸**（tau ≥ 0.6 三对地图全过 **且** Spearman ≥ 0.5 三张图全过,PASS/FAIL 由脚本自己打印）,
并换了三样东西让它成为真检验:采样 **每 60 代 → 每 30 代**（6 点 → 11 点）· 加**独立谱系 s2** · **两色分开跑**（各自成联赛）。

| 轨迹 | Spearman（图 7 / 11 / 23） | min tau | 闸 |
|---|---|---|---|
| s1 红 | 0.12 / −0.04 / −0.25 | 0.18 | **FAIL（两半都挂）** |
| s1 蓝 | 0.85 / 0.72 / 0.59 | **0.48** | **FAIL（只挂一致性）** |
| s2 红 | 0.60 / 0.86 / 0.69 | **0.45** | **FAIL（只挂一致性）** |
| s2 蓝 | 0.17 / 0.54 / 0.75 | 0.35 | **FAIL（两半都挂）** |

⇒ **一条都没过** ⇒ 按冻结分支：**「时代」叙事从 chunk 2b 里删掉**,⛔ 不做第三次切同一批数据。

**P1 ❌**：s1 蓝在加密采样后没过——它上一轮那组 0.73/1.00/0.73 是 **6 个点**的产物,11 个点读出 0.60/0.59/0.48。
⭐ 红方从另一侧撞了同一堵墙：s1 红训练图上的 Spearman **6 点时 0.83、11 点时 0.12**。
⇒ **本轮最可搬运的一条教训：六个冠军上的秩统计量不是证据** —— 这一天里它骗了我两次,方向相反。

**P2 ❌ 而且信息量更大**：s2 **不复现**颜色分裂——s1 里是蓝方有稳定进步,**s2 里是红方**（0.60/0.86/0.69）。
⇒ **「蓝方会进步」从来不是关于颜色的事实。** ⭐ 真正重复出现的是**形状**：**每条谱系里恰好有一方在进步、另一方没有**,
像一场单边军备竞赛。⚠ n=2,这是**待检的模式,不是定律**。

**P3 部分成立**：每条轨迹 3 张图里有 1–2 张出现非传递环,仍集中在训练图之外,但上一轮那种 22 个环的爆炸没复现。

⚠ **一条会削弱标题的替代解释,我有义务写出来**：tau 挂掉可能部分是**数据质量**而非真不稳定 ——
出了训练图冠军更少碰面,被丢掉的无接触格子分布**不均**：s1 红 3/162、s2 红 4/161,而 **s1 蓝 15/150、s2 蓝 24/141**。
⭐ 丢格最多的两条恰好也是挂掉的两条 ⇒ **「次序不可迁移」和「离开训练图我们量得更少」在这里还没分开**。
修法是**离图时加大每格场次**,⛔ 不是降低 tau 门槛,而且要单独预注册。

⇒ **还站得住的**：⛔ 任何轨迹、任何地图上都**没有**时代结论；
✅ 上一轮那条方法结论**加强**了：进步要**逐色**读,而且**只有粗趋势**（越晚越强）曾经看起来跨图稳,
**逐代的细次序在四条轨迹上一条都没迁移过**；
✅ 外加新的一条：**六个冠军上的秩统计量不是证据**。

## [2026-09-15 21:50] G3 chunk 2b（lineage）：**没有任何一次「换人」—— 冠军基因组是一条受约束的随机游走**  #measure

预注册 `runs/g3-lineage-predictions.txt`。⛔ **trainer 不记录亲缘**（hof 条目只有 `{gen, fitness, genome}`），
所以真实的 parent→child 边**在任何存档里都不存在**，加它要重训（训练侧正在「不要动工」）⇒ 本轮的血统**全部是从基因组推断的**，
写的时候必须带「推断」两个字。好处是：**一次仿真都不用跑**，纯基因组算术，确定性、瞬间出结果。

| 轨迹 | 完全保留（d=0） | 3× 中位数的「换人」 | max/中位 | lag128/lag1 |
|---|---|---|---|---|
| s1 红 | 42/299 = 14% | **0** | 2.51× | 4.34× |
| s1 蓝 | 44/299 = 15% | **0** | 2.68× | 4.60× |
| s2 红 | 34/299 = 11% | **0** | — | 4.62× |
| s2 蓝 | 61/299 = 20% | **0** | — | 4.25× |

**P1 ❌**（我押保留 >30%）：实际 11–20%，精英只把冠军带过大约六代里的一代。
**P2 ❌**（我押 5–25 次换人）：**零**，四条轨迹全是零，而且不是差一点 —— **一条 run 里最大的一步只有它自己中位数的约 2.5 倍**，
第一代之后**根本没有尾巴**。⚠ 门槛冻在 3× 就保持 3×；诚实的说法不是「门槛不巧」，而是「没有尾巴」。**P3 ❌/空**：没有换人，gen 72 自然谈不上靠近换人。

⭐ **但「零次换人」不等于字面意思，而这需要一个诊断来分开**：两种完全不同的世界都会给出「没有换人」——
一条线在连续漂移，或者**种群已经收敛**（每个个体都是彼此的近似副本，于是任何替换都看不出大）。冻结的设计分不开它们。
**距离随 lag 的曲线**可以：收敛云团从 lag 1 起就**平**；随机游走按 √lag 长（lag 128 时 11.3×）；定向行军线性长（128×）。
**实测四条轨迹全是 4.3–4.6×** ⇒ **都不是**。冠军线是一条**受约束的随机游走**：持续在动（远高于平），但**被关在一个有界区域里**（远低于 √）。

⚠ **还顺手纠正了我自己的心智模型**：`mutRate 0.02 × mutSigma 0.05` ⇒ **一次突变**的 RMS 是 0.0071，
而相邻冠军的中位步长是 **0.063**（是它的九倍，动了 15% 的权重而不是 2%）
⇒ **相邻代的冠军不是亲子关系**，而是「一个已经收敛到看不出替换的种群里的不同个体」。

⭐⭐ **最该留下的一条（预注册里写明不许埋掉）**：**lurk 那个角色在第 72 代锁定、守了 227 代，而冠军基因组在那里没有任何不连续**——
不是「那儿没有换人」，是**哪儿都没有**，而且那 227 代里基因组移动了好几个单步的距离。
⇒ **一个稳定的行为角色，跨越了可观的基因改变而持续存在。** ⛔ 这是描述不是机制，也**不**等于「这个角色对基因改变鲁棒」，
只是说在这条谱系里这两件事没有一起动。

⇒ 「谁改变了这支队」在这里有答案，而答案是**没有某一个谁**：没有任何个体在一步之内夺走 hall of fame，冠军基因组在一个有界区域里连续游走。
这就是 chunk 3 可以画的描述性骨架。⛔ 不带时代叙事，⛔「血统」二字必须永远挨着「推断」。

## [2026-09-15 22:40] ⛔⛔ 收口检验：**lurk 角色不可跨图搬运** —— 本程序的头条结论要加限定  #measure #incident

预注册 `runs/g2-lurk-maps-predictions.txt`。做这件事的理由：G3 教会了「一张图上量出来的东西不一定迁移」，
而 lurk 是整个程序**唯一一条实质性的正面发现**，却**只在训练图上量过**。⛔ 在没查它之前继续堆展示面，是在没验过的地基上盖楼。

⭐ 这不是钓鱼，是**从两个旧结果推出来的可证伪预言**：实测 `generateMap` 在 7/11/23 三张图上把**出生点与点位放在完全相同的坐标**，
只有 33 个掩体盒不同；而层③ 干预已证明**角色跟出生位置走**。⇒ 两者合起来预言：**角色应该能跨图存活**（而风格轨迹不能）。

| s1 红 @299 | 逐 slot 孤立% | top slot | ×null | 占比 |
|---|---|---|---|---|
| 图 7（训练） | **47/0/0/0/0** | 100% #0 | **3.17** | 10% |
| 图 11 | **60**/14/2/18/12 | 60% #0 | **2.20** | 23% |
| 图 23 | **0**/6/14/25/7 | 36% #2 | **1.10** | 10% |

⇒ **P1 ❌**：图 11 上**还在**（仍是 0 号、仍明显高于零假设），图 23 上**整个塌掉**（0 号读 **0%**，孤立跑到 2 号身上、1.10× 等于随机）。
按冻结分支**照实报、⛔ 不许跨图平均**。**P2 也 ❌**：占比没有下降，反而在图 11 上**升到 23%**。

⭐⭐ **这改的是本程序的头条**：**lurk 角色不可跨图搬运。** 三张图的出生点与点位**逐位相同**、只有掩体不同，
所以「角色跟出生位置走」这条**不足以**让角色迁移 —— **从那个出生点出发的路线是掩体几何决定的**。
诚实的说法现在是个联合命题：这个角色是 **(这个基因组, 那个出生位, 这张图)** 的性质。
⇒ **G2 里每一条 lurk 结论都要带上和 G3 数字一样的「在训练图上」限定**，包括那个「守了 227 代」——它只在一张图上量过。

⚠ **而且反方向同样成立**（只测我在意的那个冠军就会漏掉）：**s2 红在自己训练图上没有 lurk 形状**（1 次 episode、1.13×、在 2 号身上），
**换到图 11 就长出来一个**：0 号占自己 tick 的 **64%**、**2.76×**、32 次 episode。
⇒ 这个形状不只是「离图就脆」，它会随掩体变化**出现、消失、换人**。⛔「这支队有个 lurker」**不是这支队的性质**。

⇒ **没有被推翻的**：detector 的 precision 对照、场内零假设、birth 扫描的机制、干预的归因，**全都成立** ——
它们只是**都跑在同一张图上**。⛔ 这里说的不是测错了，是**结论的适用范围比写法窄**。

## [2026-09-15 23:10] pair 的跨图复核：**「有搭档」跨图成立，「搭档是谁」不成立** —— 并给出全天的统一规律  #measure

预注册 `runs/g2-pair-maps-predictions.txt`（⛔ 刚在 CHECKPOINT 里写下「pair 只在训练图量过」这个缺口，
**同一个 session 里指出缺口又不补，比不指出更糟**，而这只要一条命令）。

| 冠军 @299 | 图 7（训练） | 图 11 | 图 23 |
|---|---|---|---|
| s1 红 | **1-3** 64% 2.14× 8m | **2-3** 47% 1.49× 4m | **1-4** 58% 1.75× 12m |
| s1 蓝 | **0-1** 91% 2.55× 4m | **0-1** 57% 1.77× 4m | **2-4** 64% 1.92× 8m |
| s2 红 | **0-2** 56% 1.72× 8m | **3-4** 60% 1.88× 4m | **0-1** 94% 2.56× 4m |
| s2 蓝 | **0-1** 71% 2.05× 4m | **2-3** 59% 1.75× 4m | **0-1** 78% 2.28× 4m |

⇒ **P1 对了一半，而它裂开的那一半正是有用的那半**：**十二格里每一格都存在互选搭档**，且**全部高于各自的零假设**（1.49–2.56×）；
⛔ 但**是哪一对**，四个冠军里有三个换图就变（s1 红 1-3 → 2-3 → 1-4）。
⇒ **P2 ❌**：出生间距的「种类」也不保：s1 红的非相邻 8m 在图 11 变成相邻 4m、在图 23 变成 12m。

⭐ **可以说的**：**「这支队会保持一对互选搭档」跨图成立** —— 这是 G2 里第一条在**每个**被测冠军上都扛住换图的陈述。
而**「这支队的搭档是 1 号和 3 号」不跨图**，要和现在的 lurk 结论一样带地图限定。

⭐⭐ **全天的统一规律（本程序最可搬运的一条）：在这个世界里，结构的「总量层」跨图稳，「身份层」不稳。**
- **style**：「有几代在动、且最早那几代动得最凶」稳；**「哪几代动」不稳**（42/83/43%）。
- **eras**：「越晚越强」这个趋势部分稳；**逐代的次序不稳**（四条轨迹 tau 全 < 0.6）。
- **pair**：「存在互选搭档」稳（12/12）；**「是哪一对」不稳**（四个冠军里三个换）。
- **lurk**：连**存在性**都不稳（图 23 上塌掉）—— 它恰好是**单个身体的路线**，而路线正是掩体决定的。
⇒ 本项目该带着的经验法则：⛔ **不带地图限定就不要点名任何个体**（哪个 slot、哪一对、哪一代）；
✅ **关于「结构有多少」的陈述可以搬运**。

## [2026-09-15 23:30] 补上最后一个缺口：pair 的**归因**跨图成立（机制迁移，身份不迁移）  #measure

给 `lurkswap` 加了 `--map`，按预注册在每张图上**取那张图自己的最强互选对**再做三臂归因（定义/臂/搬家规则均未改）。

| s1 蓝 | 那张图的最强对 | C 臂 by spawn | by carrier | placebo（对照） |
|---|---|---|---|---|
| 图 7（训练） | 0-1 | **70% ± 19.4** | 4% | 63%（91%） |
| 图 11 | 0-1 | **46% ± 7.9** | 9% | 57%（57%） |
| 图 23 | **2-4** | **20% ± 6.5** | 1% | 48%（64%） |

⇒ **预测成立**：**归因在两张离图上都复现** —— 搭档搬到**接手位置**的那个身体上，从不搬到接手身份的那个；
而且在**图 23 上依然成立**，尽管那里的搭档**换成了另一对（2-4）**、而且 lurk 角色在那张图上**整个塌掉**。
⚠ 效应量离图后变弱（70% → 46% → 20%），placebo 也一起降 ⇒ ⛔ 跨图不比大小，**复现的是方向**。

⇒ 这把全天的规律补完了，补上的正是缺的那块：**机制会迁移，身份不会。**
「这支队的搭档是哪一对」是地图特有的；「搭档是被**位置**而不是被**身份输入**扛着的」不是。

## [2026-09-15 23:55] G3「tactic frequency over generations」交付：**pair 是一条平线，lurk 是一个台阶；量的大小不跨图**  #measure

预注册 `runs/g3-tacticfreq-predictions.txt`。⛔ 没定义任何新东西 —— `detect` 已经有 `--gens` 和 `--map`，
这一项就是把现成的仪器对准 roadmap 的那个问题。⚠ 而且从一开始就按今天的教训跑**两张图**，因为单图答不了。

**pair（最强互选对相对自己零假设的倍数）**：图 7 = 2.35/2.53/1.59/1.77/1.88/1.78/1.86/2.14；
图 11 = 1.77/2.31/1.48/2.33/2.41/1.72/1.61/1.49。
⇒ **P2 ✅**：**每一个采样代、两张图上都有一对互选搭档高于零假设（1.48–2.53×），而且没有趋势。**
⇒ 「pair 随世代的频率」**是一条平线，结构从第 0 代就在**。⛔ 它不是故事，别画成故事。

**lurk（孤立占比 / 0 号占自己 tick 的比例）**：图 7 = 4/2/14/18/35/25/8/10%（slot0 0·0·58·77·94·50·35·47）；
图 11 = 0/7/16/15/8/13/14/23%（slot0 0·0·31·59·19·44·46·60）。
⇒ **P3 ✅（定性）**：**两张图上 0/40 代都没有（slot0 都是 0%），80 代起都出现**。
⇒ **P1 ❌/答不了**：**量的大小不跟着走** —— 图 7 在 160 代冲到 35%，图 11 恰好在那一代跌到 8%。
八个采样点的 Spearman 是 0.29，而⚠ 按我自己的坑 #39，**八个点太少，两个方向都不算证据** ⇒
诚实的说法是：这一轮**既没证明也没否证**量级上的一致，而**定性的台阶（60 代前没有、80 代后有）是一致的**。

⭐ **一个计划外的跨仪器互检，而且通过了**：图 7 的 **160 代**是全扫描里 lurk 占比最高的一代（35%、0 号 94%），
而它**正是 style 那台仪器在图 7 上标 `det` 的那一代**（own-noise 0.08 = 那个冠军几乎不接敌）。
两台为不同问题造的仪器，**因为同一个底层事实点了同一代**：不打架的队伍，队员当然常常落单。⛔ 这是一致性检查，不是新发现。

⇒ G3 的「tactic frequency over generations」**按今天的规律所允许的层级回答完了**：
**总量层的台阶（这个形状出现了没有）跨图一致；量级轨迹、以及由哪个 slot／哪一对承载，都是地图特有的。**

## [2026-09-16 00:20] G3「communication vocabulary evolution」交付：**早期一个台阶，之后 259 代平线；内容始终为零**  #measure

预注册 `runs/g3-vocab-predictions.txt`。⛔ 没定义新东西 —— `radio` 已经在量发送端（符号熵 / 静默率 /
对候选指称的 MI，含**按比赛阶段条件化**的那个，坑 #30）。今天给它加了 `--map`，理由和别的仪器一样：
**说话者看见什么是掩体决定的**（坑 #40）。

⭐ **比画一条线更值钱的问题**：D2 的结论是发送端没有内容（最高原始 MI 9%、按阶段条件化后 0.9%），
但那是在**特定世代**上量的。扫历史问的是 D2 从没问过的一件事：**曾经有过内容吗？**
一条「短暂有过内容、后来丢了」的电台，在 299 代看起来会一模一样。

| 代 | 0 | 40 | 80 | 120 | 160 | 200 | 240 | 299 |
|---|---|---|---|---|---|---|---|---|
| 熵（满 2.32 bits）图 7 | 1.25 | 2.28 | 2.17 | 1.81 | 2.07 | 2.17 | 2.05 | 2.22 |
| 熵 图 11 | 1.12 | 2.24 | 2.14 | 1.84 | 2.09 | 2.20 | 2.01 | 2.27 |
| 静默率 图 7 / 图 11 | 61/63% | 17/18% | 23/23% | 12/13% | 11/11% | 21/22% | 13/12% | 14/16% |
| 最大 MI\|phase 图 7 / 图 11 | 0.8/— | 2.7/2.6% | **3.4/3.7%** | 1.6/1.6% | 1.8/2.7% | 3.0/2.3% | 1.6/1.8% | 1.7/2.3% |

⇒ **P1 ✅，而且强得超出预期**：两张图在**每一代**上熵差 ≤ 0.04 bit、静默率差 ≤ 2pp。
⭐ **这是全天最紧的跨图一致**，而且有道理：**说话者「发出」什么是策略输出的性质**，
而今天所有不迁移的东西（单个身体的路线、哪个 slot 孤立、哪一对成对）都依赖**掩体允许身体去哪**。
⇒ 轨迹本身：**第 0 代近乎哑巴**（静默 61–63%、熵 ~1.2），**到第 40 代信道就饱和了**（静默 17–18%、熵 ~2.26），
**然后 259 代一直平**。⛔ 这条谱系的「词汇演化」= **早期一个台阶 + 之后一条平线**。

⇒ **P2 ✅（实质），⚠ 但我写死的阈值没守住**：我押「最大 MI|phase 处处 < 约 3%」，实际读到 3.4%（图 7）和 3.7%（图 11）。
十六格全部落在 0.8–3.7% 之间，**没有任何一处像内容**。
⇒ **可以说的、而且比 D2 的端点结论更强**：**这条谱系从来没有演化出带内容的词汇** ——
在这个定义下、两张图上都是。**没有内容可丢；「没内容」是关于整段历史的陈述，不是关于最后一个冠军的。**

**P3 —— 唯一够得上「候选」的一格，只报不推**：**第 80 代在两张图上都是 MI|phase 最高的一代，而且是同一个指称**
（"enemy visible"，3.4% 与 3.7%）。两张图在**代数和指称上同时**一致，不像单格巧合，
⚠ 但 3–4% 的熵占比仍然很小，且按坑 #39，**从十六格里挑出来的最大值**必须配自己的预注册和更多 seed 才谈得上「意义」。
⭐ 另外记一句：**第 80 代正是 lurk 形状开始出现的地方** —— 这已经是**第三台仪器**指向同一个邻域了，
这是**去看一眼的理由，⛔ 不是一个发现**。

## [2026-09-16 00:45] G3「player identity cards」交付：**一张诚实的卡片上，大多数行写的是 VARIES**  #measure

预注册 `runs/g3-cards-predictions.txt`。⭐ **设计被今天的结论反过来约束，而这正是重点**：
一张写着「0 号是那个落单的人」的卡片，在训练图上是真的、在**图 23 上是假的**（那儿它读 0%）。
卡片是整个项目里**看起来最笃定的界面**，所以它是**最不该印单图事实的地方**。
⇒ 卡片**按构造是地图感知的**：每条事实在每张图上都测，带 **TRAVELS / VARIES** 标记，
**只有每张图都同意时才平铺直叙地写出来**。⛔ 卡上不出现任何角色名（lurker/anchor/entry）——
VISION 不许战术名进产品面，而今天的证据说连底层事实都不稳。

⛔ **没有第三个 play loop**：`detect` 和 `lurkswap` 各有一个，第三份拷贝就是这个仓库一直在付账的那种漂移。
改成 `detect --out` 把**它本来就算出来的**逐 slot 事实写成文件，`cards` 只做**跨图合并**。

| s1 红 @299 | 图 7 | 图 11 | 图 23 | 判定 |
|---|---|---|---|---|
| slot 0 | 47% | 60% | **0%** | **VARIES**（只在 7、11 上是最孤立的那个） |
| slot 1 / 3 / 4 | — | — | — | **travels（负向）：哪张图上都不是最孤立的** |
| slot 2 | 0% | 2% | 14% | VARIES（只在图 23 上） |
| 搭档 | 1-3 2.1× | 2-3 1.5× | 1-4 1.8× | **VARIES：每张图都是另一对** |

⇒ **P1 ✅：三张图上「既是稳定的最孤立者、又有稳定搭档」的玩家 —— 一个都没有。**
⇒ **P2 ✅：能 travel 的全是负向或总量** ——「3 号哪张图上都不是最孤立的」处处成立；蓝方那张卡上
「**总有某一对**搭档高于 1.5× 零假设」处处成立。⭐ 这正是今天一路 travel 的那两层：**不存在**与**有多少**，⛔ 从来不是**是谁**。

⇒ **卡片格式本身就是交付物**：在这里唯一诚实的卡片，就是**大多数行写着 VARIES、并把逐图数字留在原地**的那种。

## [2026-09-16 01:10] G3「club/team clusters」：**做不了** —— 风格空间连「共同进化的那一对」都认不出来  #measure

预注册 `runs/g3-clusters-predictions.txt`。设计上先解决了一个真问题：`style.ts` 在**自己的扫描内**做 z-score、
又拿**各自的最终对手**当尺子 —— 两把不同的尺，坐标**跨 run 不可比**。聚类需要**一个空间、一把尺**，
而这个仓库自己就有那把尺：**手写 reference bot**（无基因组、不读 obs、不共演化，`yardstick` 的立论）。
⇒ 16 个冠军全部打**同一个 bot**，22 个 metric 一起 z-score。⛔ bot 只作尺子，C1 exit 明禁它进训练池。

⛔ **不做 k-means**：挑 k、跑到图好看为止是典型的 forking paths。改成**预注册的最近邻检验**，而且**偶然水平能事先说出来**：
16 个冠军里每个有 15 个候选、其中恰好 1 个是它的**同 run 搭档** ⇒ 偶然 = 1/15 = **6.7%**。

| | 图 7 | 图 11 |
|---|---|---|
| 最近邻是同 run 搭档 | **0/16 = 0%** | **2/16 = 13%** |
| 最近邻关系的**跨图一致** | colspan: **1/16 = 6%** | **（= 偶然 6.7%）** |

⇒ **P1 ❌ 而且是彻底的**（我押 40–80%）：**共同进化了 300 代、互相塑造了整段历史的那一对，在风格空间里认不出彼此。**
⇒ **P2 ❌**：最近邻关系跨图只对上 1/16，就是偶然水平。
⇒ **P3 ✅（唯一存活的预测）**：没有干净的家族。⚠ 全场最紧的一对（pop48-s1:B 与 pop48-s2:B，d=1.36，图 7）
是**同训练配置的两个 run、同颜色** —— 本来是个好故事，但它**在图 11 上不复现**，⛔ 所以不是。

⇒ **按冻结分支可以说的：club/team clusters 在这个风格向量上做不了。**
它认不出一个**我们确知存在**的分组，而且它的邻域结构在跨图上只是偶然。
⛔ 拿这 22 个 metric 画出来的聚类图，是**把噪声画得很自信**。
⭐ 这是学到这件事最便宜的地方：一个**预注册 + 事先说明偶然水平**的最近邻检验，而不是一个 k-means ——
**k-means 永远能给出聚类**。

## [2026-09-19 12:20] 接手一份重新 clone 的树：`runs/` 与 `node_modules/` 都不在，冠军类仪器全是死的  #incident

工作树是 2026-09-17 重新 clone 的（全部文件同一个 mtime）。`.gitignore` 里有 `runs/`，于是：
**G2 / G3 / D2 / D2s 引用的每一份 `runs/*-predictions.txt` 预注册、每一个 run artifact，全没了。**
文档里的结论还在，判它们用的冻结预测不在。⚠ **什么都不会报错** —— 这是这条的要害。

⇒ 记成 **GOTCHAS #41**，预注册改住 `predictions/`（进版本库），配 `tests/predictions.test.ts`
（仓库里引用的每个 `predictions/*.txt` 必须真实存在 + `predictions/` 不得进 `.gitignore`）。
⛔ 09-19 之前那些引用**救不回来，也不去伪造重建**；引用它们的结论按「没有独立 backstop」对待。

环境复原并复核：`npm ci` → **105 tests 全绿**（Windows 本机）· `npm run leaks` 矩阵与登记表一致 ·
`mapprobe --self-test` OK。重训 `d2-long-s{1,2}`（配置抄自 d2-radio：`--sites 2 --mode capture --tokens 2
--comm-interval 5 --comm-delay 3 --gens 300 --pop 16 --snap-every 10`），obs=109 / genome=5684 与记录一致；
本机 6472 s 与 6770 s（21.6 / 22.6 s/gen，两条并行 ⇒ 比当年 11.5 s/gen 慢是机器占用，不是配置差异）。

## [2026-09-19 12:20] E2b：世界为「混合」付钱，而这个底座**表达得出混合，却生不出混合**  #measure

**为什么是这根杠杆**（按 CLAUDE.md「拿 VISION 过一遍再决定」，没弹回给用户）：G3 清单已全部结案，
下一步在「G4 发现 feed」与「回训练侧」之间。⛔ 不开 G4 —— 它是为「把新东西浮出来」造的观战仪器，
而 G2/G3 的证据说这条谱系里**没有可发现的东西**（电台 300 代零内容、身份层不跨图、风格空间连
共演化的那一对都认不出）。反过来，本仓四条 demand 探针里**只有一条读正**：E2 把自己那张表当零和博弈解，
守方均衡是**混合**（压点 68 / 换计划 32），比任何单一合法打法的最坏情况高 **14.5pp**。
⇒ 问题掉头：**这 14.5pp，进化收得到吗？** 这是 VISION §14.1 第一步「缺的是腿还是没选」的正面提问。

**动手前先读源码，把前提和发现分开**：`World` 的 `rng` 只被两处消费 —— 命中骰子与曳光弹端点；
出生点取自 `map.spawns`；感知抖动 `jitter(key, bucket, const)` 是**纯哈希**，不读世界种子。
⇒ 「同一对基因组在第一枪前与 match seed 无关」是**源码给的前提**，⛔ 不是本次的发现。
本次要量的是它在**决策变量**上值多少，以及**换对手**能不能接住。

**仪器** `npm run mixing`（预注册 `predictions/e2b-mixing-predictions.txt` + 三段 addendum）：
计划 = t=T 时该队五个 slot 各自**最近的点位**构成的 5 元组；熵只在单个 (row, arm, map, role) 格内算
⇒ 跨色的点位顺序（坑 #17）与坑 #40 都进不了这个数。三条臂各带分母：
**A own dice**（同图同对手，32 个 match seed）· **B opponents**（同图同种子，对方 hof 跨史 6 个大脑）·
**C maps**。闸：`ctl fixed 3/2` = 0.000 且 `ctl alternating` = 1.000。

**闸自己改过一次，改的是构造不是阈值（坑 #42）**：原闸是「手写硬币守方读 0.90–1.00 bit」，
n=32 下读 0.896 就红了 —— 追下去是 **22/10** 的硬币窗口，而 `new Rng(hashSeed(...)).int(2)` 在 5000 个
种子上 49.1%（同流连抽 5000 次 49.5%）⇒ **种子首抽没偏，是闸把二项噪声算进了检测性断言**。
⇒ 换成按场次下标**交替**，构造上 1.000。两个硬币行保留照印，⛔ 不判闸。

**arm B 先自证不是空分母（坑 #26）**：6 个对手在 5684 个权重里差 5635–5681 个，六场比赛
winner / 298–600 ticks / sight / damage **全都不同** —— 换的是真对手，打的是真不同的比赛。

### 读数（两条谱系 × 四个冠军 × 两图 × 攻防，t=6 headline）

| | arm A own dice | arm B opponents |
|---|---|---|
| s1:R@299 · s1:B@299 · s2:B@299 | **0.000（全部格子）** | **0.000** |
| s2:R@299 | **0.000** | **1.792（图 7）/ 1.459（图 11）** |
| 对照 fixed 3/2 / alternating / team coin / own coins | 0.000 / **1.000** / 0.896 / 3.938 | — |

⭐ **P1 ✅ 而且没有例外**：四个冠军 × 两图 × 攻防的**每一个受保护格子**，own dice 都读 **0.000 bit**。
同一个对手下，32 场比赛的计划**逐字相同**。⛔ 这不是「熵很小」，是恰好零。

⛔ **P3 两个方向都错了**（我押「>0 且 <0.5」）：三个冠军读**恰好 0**，一个读 **1.79**。
⇒ **「计划随对手变不变」是谱系的性质，不是这个世界的性质** —— 与 09-15 那条「身份层不跨图」同形。

⭐⭐ **时间扫描把「计划」和「反应」分开了**（addendum 3，只作描述）：
**t=3 s 时，四个冠军、两条臂、两张图，全部 0.000。** s2:R 的对手依赖是 **t=4 s 才出现**（图 11，0.650）、
t=6 s 长到 1.79 —— 而它的第一枪在 **5.7 s**。⇒ 那是**接敌信息到达之前**就开始的条件化（合法的听/看），
⛔ 不是中弹后的反应。反过来，s1:R 在图 11 上 **t=12/20/30 s** 仍是 **0.000 / 0.000**，而那一格
alive5 还有 75–91%、首枪在 4.0 s ⇒ **接敌二十多秒之后，对着六个完全不同的对手，承诺仍逐字不变**。

### 结论（按冻结许可收窄）

⭐⭐ **这个世界允许一支队「反应」，但不允许它「不可预测」。**
给定同一个对手，计划是一个**确定函数**：0 bit，每个冠军、每个时刻、两张图都如此。
变化只以「换了对手」的形式存在。而 E2 的 14.5pp 要的恰恰是**同一个对手下猜不到** —— 就是那条恒为 0 的轴。

⇒ **P4：世界挂 0.904 bit，进化收到 0.000 bit，而且不是没学会，是底座里没有这条自由度。**
⭐ 关键的一半由对照给出：`ctl alternating` 在**同一个世界、同一把仪器**上读 **1.000** ——
**世界装得下混合的计划，只是不会为一个每回合输入都相同的策略生出一个。**
⇒ 这是本仓第一次把一条 demand 读数判成**收不到**，而不是**没找到**（VISION §14.1 的「底座缺腿」）。

### ⛔ 适用范围（先写下来再谈下一步）

⛔ 只约束**这一个统计量**（空间承诺）· 这两张图 · 这两条谱系 · 末代。
⛔ 0 bit **不说明冠军弱**：对手搜不出 best response 时纯策略可以是好策略（`exploit` 四打一穿）。
⛔ **本次不授权任何施工**。「给私有骰子 / 换出生点 / 多回合历史」都是各自独立的杠杆，
而且 E2 自己的读数已经警告过：混合只对**会适应的**对手兑现 —— 加了自由度不等于进化会收。
⚠ 下一根杠杆要用户定，或按 ROADMAP 另开 probe 先问「加了之后世界还付不付这 14.5pp」。

## [2026-09-20 00:50] E2c：骰子被捡起来了，而且是**它**造成的 —— 但长出来的是「每人各自掷」，不是「队的混合计划」  #measure

用户在 E2b 的闸门上选了**私有随机输入通道**（三个候选里 VISION 冲突最小的那个）。
通道已 ship（`privateDieDim`，默认 0），A/B 两条 300 代跑完（`runs/e2c-die-s{1,2}`，
对照臂 = 已有的 `runs/d2-long-s{1,2}`）。预注册 + 两段 addendum 在 `predictions/e2c-die-ab-predictions.txt`。

### 施工里最别扭的一处，值得单独记一笔

镜像测试断言红蓝**逐位相同**的 observation；而一颗诚实的私有骰子**必须**打破它 ——
如果逐位镜像仍然成立，那就是两队掷出了同一个数，谁都没有私有的东西。
而按坑 #17，「把通道关掉再跑镜像测试」恰恰是禁止的。
⇒ 改成**带骰子跑**，断言**不同的字段恰好是骰子、且是全部骰子**：别处不对称会红，骰子不变也会红。
side fairness 降回它本来的形状（分布式）：400 个种子两色各自 |mean| < 0.05、sd ∈ [0.54, 0.61]。
⭐ 一般化的那句：**一个断言的形状必须配得上被断言的量** —— 逐点等式配不了随机量，它要的是分布。

### ⚠ 冒烟在读长跑之前作废了我自己写的 exit

原 P1：「arm A > 0.1 bit 就算进化捡起了骰子」。一个**只训了 2 代**的随机网络读到 1.703 bit / 4 plans ——
随机权重本来就把骰子透传出去。⇒ **零假设不是零**，那条 exit 量的是通道存在，不是进化用了它。
同一次冒烟把仪器那一半钉死了：新增 `ctl die-reader`（手写守方，从**自己 observation 的尾巴**读骰子）
读 3.890 / 17 plans ⇒ **通道确实送达策略**，于是冠军读 0 会是进化的结论而不是管道坏了。
⇒ P1 换成**同一条 run 的 gen 0 vs gen 299 比 (熵, distinct)**，写在读长跑之前。

### 读数

| 冠军（守方） | gen 0 图 7 | gen 299 图 7 | gen 0 图 11 | gen 299 图 11 |
|---|---|---|---|---|
| s1:R | 0.000 / 1 | **2.151 / 5** | 0.000 / 1 | **1.531 / 4** |
| s1:B | 0.625 / 2 | **1.428 / 3** | 0.000 / 1 | **2.550 / 7** |
| s2:R | 0.000 / 1 | **1.873 / 4** | 0.625 / 2 | **0.857 / 2** |
| s2:B | 0.974 / 2 | **2.096 / 6** | 0.997 / 2 | 0.758 / 2 |

⭐ **8 个格子 7 个上升**（均值 0.40 → 1.66 bit），会动的 slot 从 ≤1 变成 1–4，
distinct 只有 2–7（`ctl die-reader` 是 17）⇒ **捡起来了，而且用得比透传克制。**
⇒ 我押的「压制」和「当噪声用」**两个都错**。

⭐⭐ **干预层过了**：`--freeze-die`（每场都用第 0 场那轮的**合法**骰值，通道仍在只是不再变；
⛔ 不清零 —— 清零是 OOD，坑 #24）⇒ **八个格子全部塌回 0.000 / 1 plan。**
⇒ 变化**是骰子造成的**，⛔ 不是「末代权重更大所以对什么都更敏感」。三层证据齐了。

⭐ **但它长的不是队级混合**：计划集合按独立逐 slot 翻转**几乎精确可分解**
（s2:R 图 7 观测 14/7/6/5 vs 独立模型 13.1/7.9/6.9/4.1；s1:R 图 7 11/8/6/5 vs 10.8/8.2/6.2/4.8）。
⇒ **五颗私有骰子没有共同抽样，本来就只能各掷各的。** 队级混合需要一次**共同的抽**，
而共同的抽只能靠**说出来** —— 正好是这条谱系 300 代从没长出内容的那条通道（D2 / G3）。
⭐⭐ **两条本来互不相干的线在这里接上了**：要队级的不可预测，就得先有带内容的通信。

**`yardstick`（跨边界唯一合法尺子）**：无骰子臂均值 85.5%、有骰子臂 79% ⇒ **没变好，方向上略差**
（⚠ 单格 ±21pp、2 个 seed，⛔ 只读方向）。与 E2 的警告一致：混合只对会 best-respond 的对手兑现，
而 `exploit` 说这个预算下四个目标只打穿一个。

**坑 #40 复核**：**存在性跨图**（gen 299 八格全 > 0），**量级不跨图**（s2 图 7 读 1.87–2.10、图 11 只有 0.76–0.86）。

### 结论（按冻结许可收窄）

⭐ **底座这条腿补上了，而且证明是被用上的**：E2b 的「同一对手下恒为 0.000 bit」在有骰子的臂上不再成立，
且干预证明因果。⛔ **但没有证据说它值钱** —— yardstick 方向上略差。
⇒ 正确的一句话是：**这个预算、这个对手分布下，不可预测性不受惩罚也不受奖励**，
⛔ 不是「私有骰子有用」，也 ⛔ 不是「私有骰子没用」。
⇒ 通道保留、**默认继续关**（VISION §14.2：回退实现不回退现象）。下一个该问的是 **E6 对手生态**：
给一个会 best-respond 的对手，这 14.5pp 才有人来收。
