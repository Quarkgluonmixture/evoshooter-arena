# GOTCHAS — evoshooter-arena

> **现役危险清单，不是事故史。** 动手前按你正在哪一步挑着读。
> 编号只追加、**永不重排、不复用**——LOG / CHECKPOINT / commit message 都按号引用；正文可以随时改写。
> 一次性的现场状态不进这里（那是 `CHECKPOINT.md`）。判据见文末「什么进得来」。
>
> ⚠ **格式是给 kit 的 `gotchas-contract` hook 读的，别乱动**：条目头 = `N. **标题**（日期）`
> （数字后紧跟粗体，中间**不能**塞日期——`[^\w*\n]{0,12}` 匹配不上），`闸:` 必须**独占一行**，
> 小节标题**不能带编号**（`## 3. xxx` 会被当成条目头）。不合形状 ⇒ hook 解析出 0 条 ⇒ **静默失守**。

## 动手前按你正在哪一步挑着读

| 你正要做什么 | 先读 |
|---|---|
| 改 observation / 加感知通道 | **19**（每个 contact 字段都乘 confidence）· **17**（抖动 key 用 slot、格建在自我相对量上）· **11**（对照只能差一个不可感知量） |
| 写预测 / 读 A/B 结果 | **26**（判 FAIL 前先证明工具到达了断言）· **24**（消融必须配 in-distribution 对照）· **23**（内部指标全绿 ≠ 打得过外面）· **20**（ladder 的 50% 可能是没发生的比赛）· **21**（训练 fitness 不跨 run）· **14**（零和指标不是独立 cell）· **12**（改了"谁算已知"= 换了尺子）· **10**（观感达标 ≠ 可以 ship） |
| 写探测 / 度量脚本 | **13**（随机基因组根本不接敌）· **18**（同一 run 的两个冠军可能互相不接触） |
| 下性能结论 | **16**（必须交错配对量）· **15**（忙机器读数虚高 35%）· **4**（训练抢 CPU 会让测试假失败） |
| 动 UI / 用页面验证 | **5**（端口撞车）· **7**（重叠是宽度的函数）· **8**（Sprite 数是假信号）· **9**（渲染读插值位姿） |
| 写地图 / 空间类度量 | **25**（网格自己必须先对称）· **11**（对照只能差一个不可感知量） |
| 改 network / genome 尺度 | **22**（旋钮是 resetProb 不是 σ；看 fan-in 不看参数总量）· **6**（旧 mutation σ 不可沿用）· **1**（先查优化器再改规则） |

## 元规律：三个反复出现的失败族

（本节是**导读**，不是陷阱条目——每一族的正文和闸都在它点名的那几条里。）

**族 A —「只在 t=0 测」**：开局那一帧看不见比赛中才成立的偏差。坑 **17** 是它的现役形态，
与已退休的坑 2 同形（那次是战斗结算顺序的先手优势，开局对称测试同样抓不到）。
⇒ 任何对称性 / 公平性断言都要有一个**中局**版本。
坑 **25** 是它的另一面：**测对称性的仪器自己不对称**，于是报出一个只存在于测量里的偏差。

**族 B —「分母是空的或换了」**：输出看起来在讲行为，其实在讲测量。坑 **12**（定义变了）、
**13**（样本根本不接敌）、**18**（这一对冠军互相不接触）、**20**（连常驻的 ladder 都会这样）都是它。
坑 **23** 是它的极端形态：**整个对手集合都是自己人**，于是所有内部读数都在讲同一个盲区。
⇒ 下行为结论前先打印**样本量本身**（被威胁 tick 数 / visible-pair-tick / 开火数），为 0 或口径变了就不比。

**族 C —「机器状态污染读数」**：坑 **15**、**16**、**4**。
⇒ 性能与超时类结论一律**同轮交错配对**，并先看一眼 `uptime`。

## 改 observation / 加感知通道之前

19. **contact 槽里的每个字段都必须乘 confidence**（2026-09-12）
否则视距 / 记忆边界一定会出现满幅断崖。V6a 把 `staleness` 换成 `recency` 时让它在「看得见」时恒为 **1** ——
敌人跨过视距线，这个字段从 1 掉到 0，`A1-P8`（三刀前写的）立刻红。A3.2a 就写下过这条规矩，V6a 还是漏了一个字段。
⭐ 推论：**登记式探针矩阵守的不是当期这一刀，是前面每一刀的结论**。
闸: src/probe/leak.ts — `A1-P8` / `A1-P15` 用 max |Δ| > 0.05 守住视距与记忆两个边界；⛔ 不覆盖**新增**字段，新字段要自己进探针登记表

17. **只在开局测对称性 = 有半条通道从没测过**（2026-09-12，族 A）
唯一的镜像测试跑在 kickoff，而那时谁也看不见谁，observation 里整个 enemy-contact 半边是零 ⇒ A3.2b 在那半边引入的
对称性破坏（抖动 key 用 agent id、bearing 量化用世界绝对角）全绿推上 main，直到 A3.3 改了每 tick 都有读数的 lidar 才被撞出来。
⭐ 两条硬规矩：① **任何 per-agent 的随机 / 抖动 key 必须用队伍内 slot，不能用 agent id**；
② **任何量化格必须建在自我相对量上**（绝对角度格差 π 就落进别的格子）。
⭐ 加新通道时**同笔**给中局镜像测试补上该通道的断言（A4 加听觉时补了速度，否则 audio 半边又是全 0）。
闸: tests/world.test.ts — `keeps the mirror symmetry once contacts exist`（中局、带同龄记忆、逐特征比）；⛔ 不覆盖「新通道在该场景里是否真的非零」，那半要人写断言

11. **counterfactual 的对照只能差一个观察者感知不到的量**（2026-09-11）
第一版 A1-P3 用「队友在角落 vs 队友在能看见的位置」做对照，结果 diff 里混进 `mate3.dx/dz/dist`（队友位置本身就是 HUD 通道），
敌情泄漏被 HUD 噪声盖住。改成**同一位置只改朝向**（朝向不在任何 obs 里）后 diff 只剩敌情字段。
⭐ 修一条泄漏时**同笔加正向护栏**（`A1-P22` / `A1-P17` 那种）——把字段改成恒 0 也能让泄漏探针变绿，那是空过。
闸: tests/leak.test.ts — 对每条 leak probe 断言**精确字段集**（不是「有变化」），多出一个字段就红；⛔ 不覆盖「护栏写了没有」

## 写预测 / 读 A/B 结果之前

26. **一条 exit 读 FAIL 时，先证明工具真的到达了那个断言**（2026-09-12，族 B）
C1c 的 exit 脚本连着**四版**给出自信的 FAIL，四次都是工具而不是世界：
① reference bot 走直线，被 C1b 新加的 screen 顶住，**从没走到过任何一个点位**（守方卡在 (13,14)，点位在 z=0）——
于是三条 exit 全 FAIL，而且「hold」和「eager rotate」读数**逐位相同**，因为两个 bot 都卡在同一面墙上；
② 「假打」的 bot 走进了假点位并**把它 arm 了** —— 那不是假打，是改主意；
③ 「过早 rotate」设在 t=8，而点位通常在那之前就 armed，bot 于是**从没偏离过** holder；
④ E3 的基线本身就 88% 攻方胜率，**天花板效应**把 27pp 的真实差压成 0pp。
修完之后同一个世界读 **3/3 PASS**。
⭐ 判据：读到 FAIL（或 0 差异）先问**这次测量到达断言了吗** —— 打印中途状态（谁在哪、什么时候 armed、
基线离天花板多远），⛔ 别先改世界。⚠ 「两组读数逐位相同」几乎永远是工具没生效，不是世界没反应。
闸: 无 — 机器分不出「没效果」和「没跑到」；规矩是任何 FAIL/零差异的结论都要先出示一条中途轨迹

24. **「消融它会变差」只证明那块输入有用，不证明它存的是你以为的东西**（2026-09-12）
D1 把 recurrent state 每 tick 清零，行为明显变差（kills 2.58→0.42、shots 50.7→19.6），看上去就是「记忆在起作用」。
但清零一口气抹掉了 140 个输入里的 **40 个**，任何训练过的网络都会变差。加两个 **in-distribution 对照**之后结论反转：
**frozen**（本场早期快照、之后恒定）和 **alien**（从**另一场比赛**抓来的常数，`var` 实测 **0.000**）
**与 live 打平**，两个 seed、三个对手、每项指标都是。
⇒ 那条通道承重，但承的不是历史；进化把它用成了**学出来的常数偏置**。
⭐ 规矩：任何「消融 X ⇒ X 承载 Y」的推断，都要有一个**保住 X 的分布与幅度、只抽掉 Y**的对照。
⛔ 只跑「有 / 无」两档会系统性地把「这块输入有用」误读成「它编码了我要的那个东西」。
闸: scripts/recprobe.ts（`npm run recprobe`）四档 live/wiped/frozen/alien 并排，且只消融被观察的那一队（对手也有脑子时两边一起废会把「都变弱」读成 null）；⛔ 不覆盖别的通道——新通道要自己设计它的 in-distribution 对照

23. **共演化的内部指标全绿，不代表打得过任何一个外部对手**（2026-09-12，族 B）
两个 population 只跟对方比，就可能一起滑进同一个盲区，而**每一个内部读数都还是正常的**：
best/mean fitness 在涨、redWinShare 贴 50%、ladder 有台阶。实测：v6a 三个 seed 的 **12 个冠军**
（含 gen 0）对手写 rusher **11 个 0%、1 个 6%**，逐场看是 zoneShare **0.000**、比分 0:36.5、团灭 0–5。
机制：`winner` 只看占区分，而 fitness 里有 0.5×伤害差；两边都不进区 ⇒ 占区差恒 0 ⇒ 梯度只剩打架，
**而且谁都不吃亏，因为对手也不占点**。
⭐ ⇒ 任何「进步了」的结论至少要有一个**不参与共演化**的对手作证。
闸: scripts/yardstick.ts（`npm run yardstick`）+ tests/world.test.ts 机械校验 `BRAIN_ONLY_FIELDS`（表里字段改动必须不影响 scripted-bot 的比赛哈希，否则跨规则集比较就是假的）；⛔ 不覆盖「有没有人去跑它」

20. **ladder 印出来的 50% / 100% 可能是一场从未发生的比赛**（2026-09-12，族 B）
共演化会稳定在「互相回避」上：两个冠军整场 **0 个 sighting tick**，各自空转到时间结束判平 ⇒ ladder 读 50%，
长得和「势均力敌」一模一样。`runs/v6a-fade-s1` 的红方 `ladder0` 连续五代读 0.5，cross-play 一查是 16 场 0 接触；
加上标记后重跑一个玩具训练，**36 个 ladder 格里 5 个（14%）零接触，其中 4 个印的是 100%**。
⭐ 这条比 #18 更硬：#18 说的是一次性脚本，这条说的是 `train.ts` **每代都在打印**的那把常驻尺子。
⭐ 跨血统的强弱判断一律走 `npm run crossplay`（每对双色打、格子自带分母、零接触印 `··`）。
闸: src/evo/trainer.ts + scripts/train.ts — `ladderSight`/`ladder0Sight` 与胜率同生同灭，零接触的格子印 `·` 而不是 `%`；`tests/trainer.test.ts` 断言这个配对关系；⛔ 不覆盖「低但非零接触」，那要人去看 crossplay 的第二张表

21. **训练 fitness 是 run 内货币，跨 run 排名会倒挂**（2026-09-12）
六个末代冠军放进同一张 cross-play 矩阵：训练 fitness 排第 2 的 `s1:B@39`（0.817）实战均值 25%（排 5）；
训练 fitness **垫底**的 `s3:B@39`（−0.023）实战均值 57%（排 3）。分低是因为它那一次 run 的对面强，不是因为它弱。
⇒ ⛔ 不要跨 run 比 `best`/`mean` fitness，也不要用它挑「哪个 seed 学得好」。
闸: 无 — 两个数字都是合法的 float，机器分不出它们来自不同的对手分布；规矩是任何跨 run 的强弱判断只认 `npm run crossplay` 的侧平衡胜率

14. **对抗里按边计数的指标不能当独立 cell**（2026-09-11）
`kills`（我方击杀 = 对方阵亡）、`deaths`、`damageDealt/Taken` 在一场比赛里近似零和，红蓝两个 cell 结构上很难同向移动。
A3.1 预注册写成「kills 在 ≥4/6 个 cell 下降」⇒ 这个预测**几乎不可能成立**，除非总杀伤率下降。
闸: 无 — 预测写在自然语言里，机器判不了「这两个 cell 独立吗」；改用**双方合计**或比率，并在落笔前自问一句

12. **改了「谁算已知」就等于换了尺子**（2026-09-11，族 B）
A2 把威胁判定从团队共享改成私有接触后，`coverRatio` 在 4 个 cell 全部暴跌 0.23–0.46，看上去像「学会不躲了」。
同一批 12 条轨迹并排算两种定义：团队口径 0.580（11374 被威胁 tick）vs 私有口径 0.450（7581 tick）
⇒ **光定义就吃掉 0.13、分母掉 33%**。
⭐ 更一般的做法：行为指标变了先做 **same-genome 对照**（同一批冠军在两版代码下重放），
A3.2b 的 accuracy 下降就是这样被证明**不是**机制后果的。
闸: 无 — 指标口径的可比性是人的判断；规矩是任何依赖「已知 / 可见」的指标在改感知层后**不可跨 phase 直接比**

10. **观感参数达标 ≠ 可以 ship**（2026-09-11）
扫视限速 1.6 与 2.6 rad/s 的抖动指标完全相同，但 1.6 让 seed 2 红方冠军打不过第 0 代。
闸: 无 — 「手感」没有机器判据；任何改仿真机制的手感调整都要过 same-seed 阶梯 A/B，挑对学习代价最小的那档

## 写探测 / 度量脚本之前

13. **随机基因组根本不接敌**（2026-09-11，族 B）
一整场 600 tick、10 个 agent，`visible` 配对 tick 数 = **0**，`threatTicks` = 0。任何需要「有人看见人」的探测 / 度量脚本
用随机基因组会得到全 0 的**假阴性**（第一版 coverRatio 对照就栽在这）。
闸: 无 — 一次性脚本不进测试套件；规矩是这类脚本一律从 `runs/*.json` 的 `hof` 里取**进化后的冠军**基因组

18. **同一次 run 里的两个冠军可能根本不接触**（2026-09-12，族 B，与坑 13 同形）
A4 的 audio ablation 在 seed 3 上读到 16 场 0 枪 0 杀，看着像 harness 坏了；实测是红蓝冠军**共演化出互相回避的均衡**
（8 场 **0** 个 visible-pair-tick，两边 100% 时间在跑），交叉配对（红 s3 vs 蓝 s1）立刻有 435 发。
⇒ 拿「同一 run 的冠军对冠军」做 ablation / 行为度量会得到**全 0 的假信号**。
闸: 无 — 同上，一次性脚本；规矩是先打印 `visible-pair-ticks` / shots 做 sanity check，为 0 就跨 seed 换对手

## 下性能结论之前

16. **bench 的绝对值不可信，要配对交错量**（2026-09-11，族 C）
同一份代码在 10 分钟内读到 43 / 66 / 77 / 91 ms/match，而且**单调上升**——`uptime` 的 1 分钟值当时只有 3.80（看着很闲），
5 分钟值却是 70，隔壁 build 在一波波地压机器。正确做法：`git stash` 做 **A,B,A,B 交错配对**（严格前台、期间不跑别的任务），
比较**同一轮内**的相对差。A3.2a 就是这样量出真实的 +6%（45.3 vs 42.7），而不是单看一次读数得出的「慢了一倍」。
闸: 无 — CI 里没有稳定的性能基线可比；规矩是性能结论必须来自同轮配对，⛔ 别拿跨时间的绝对值比

15. **bench 在忙机器上读数虚高 35%**（2026-09-11）
A3.1 删了三个输入、网络小了 7%，第一次 `npm run bench` 却读到 58–59 ms/match（基线 43.8）。真因是隔壁 session 的
xcodebuild 把 load average 顶到 32（swift helper 930% CPU）；机器安静后重测是 41–42 ms。
闸: 无 — hook 拦不住「机器忙」；规矩是 `npm run bench` 前后各看一次 `uptime`，load 高就不下性能结论

4. **vitest 默认 5 s 超时会被后台训练拖成假失败**（2026-09-11）
不是断言错，是 CPU 被抢。
闸: 无 — 已把逐元素 expect 改成聚合断言减负；跑测试时别并行开训练

## 动 UI / 用页面验证之前

5. **端口撞车会让验证全是假的**（2026-09-11）
端口被别的项目占时 `curl` 会命中别人的页面。
闸: 无 — 启动后先 grep 页面 title 确认是本项目

7. **浏览器里重叠是宽度的函数**（2026-09-11）
两条各自 absolute、靠固定间距错开的 HUD 条，1600 px 宽下完全正常，1100 px 下有 14 个控件点不到。
闸: 无 — 布局验证至少量 3 档宽度，判据用 `elementFromPoint` 的命中者，不是肉眼

8. **拿「可见 Sprite 数」证明特效触发是假信号**（2026-09-11）
血条也是 Sprite，29 个里全是血条。
闸: 无 — 数特效池本身 `scene.fxStats()`；要在没人开枪时看特效，`#pause-view` 冻住仿真后直接 `scene.addShot(...)` 注入事件

9. **渲染必须读插值位姿**（2026-09-11）
`scene.pose(i)` 而不是 `agent.x/z/yaw`：仿真 15 Hz、屏幕 60 Hz，直接贴仿真位姿会让人和相机都一卡一卡
（实测 150 个动画帧里只有 28 帧在动）。
闸: 无 — 视觉流畅度没有机器判据；规矩是新增跟随类视觉都走 `scene.pose`

## 本项目特有的硬规则

25. **用来证明对称的尺子，自己必须先对称**（2026-09-12，族 A）
C1 的地图探针第一版用边对齐网格（`floor((x + half) / cell)`），于是 x = 8 落在中心 8.25 的格子、
x = −8 落在中心 −7.75 的格子 —— **一个 180° 旋转对称的地图被读成红蓝不对称**：
距离 27.0 vs 28.0 m、通路 2 条 vs 3 条、choke 17 vs 14。改成**奇数格数、格心关于 0 对称**之后
三个 seed 上红蓝**逐项相同**。⇒ 这个偏差从头到尾只存在于测量里。
⭐ 规矩：任何要给「side-fair / 镜像 / 对称」背书的离散化度量，先证明**离散化本身**在那个对称操作下闭合。
闸: scripts/mapprobe.ts `--self-test`（断言 `idx(x,z) + idx(-x,-z) == n²-1`、旋转两端可走性一致、对称地图上两侧读数相同，失败 exit 2）；⛔ 不覆盖别的脚本的网格——各自要写自己的 self-test

22. **继承保真度的旋钮是 `resetProb`，不是 `mutSigma`；而且它看 fan-in，不看 genome 大小**（2026-09-12）
`mutate()` 的 reset 是**替换**（`out[i] = gauss*0.5`），每权重贡献 `0.25 + s²`；σ 只贡献 `sigma²`。
默认设置下 reset 项 **5.5e-4** 对 sigma 项 **5.0e-5 = 11 倍**。实测 sweep：σ 从 0.02 拉到 0.15（7.5×）
layer-1 扰动只 +18%，而 resetProb 从 0 到 0.02 让它涨 **11 倍**（`resetProb=0` 时突变几乎行为惰性）。
⇒ ⛔ 调了 σ 却看不到变化时，别下「突变尺度不重要」的结论——你动的不是主导项。
⭐ 另一半同样反直觉：genome 从 3028 涨到 32780（**10.8×**），layer-1 扰动纹丝不动（0.210–0.236）。
决定它的是**那一层的 fan-in 与权重 std**（`D = √v / s`，init 尺度下 `D ≈ √(fan_in · v)`），不是参数总量。
⚠ 逐层量 D 时只有**第 1 层**能校验公式：更深的层测到的 Δ 里混着上游传下来的扰动，会系统性偏高（实测 1.4–7.1×）。
闸: tests/brain.test.ts — 实测每权重方差 / `mutationVariance()` ∈ [0.93, 1.07]（改 RESET_SCALE 或把 reset 改成加性都会红）；⛔ 不覆盖「这个 D 值对进化好不好」，那要 `npm run inherit` + A/B

6. **改 genome 尺度会让旧 mutation σ/rate 失效**（2026-09-11）
未来增加 recurrent brain / individual player parameters 都会改尺度；任何 D1/E1 改动先重新做 inheritance/mutation sensitivity。
⚠ V6b（记忆搬进 brain）就是这一条的第一个真正触发点。
闸: 无 — 超参是否仍然合适要靠 A/B 判断；规矩是改尺度前先做敏感性实验

1. **突变 σ 相对权重尺度过大会让种群塌成全员躲藏**（2026-09-11）
首层权重 ≈ 0.1 而 σ 取 0.15 ⇒ 孩子继承不到父代行为，best 每代 ±0.5 跳、mean 贴 0。**先查优化器再改规则。**
闸: 无 — 超参判断；证据在 README Evidence

## 已退休（闸已完全覆盖，留号防复用）

| 号 | 当年的坑 | 退休于 | 现在由谁守 |
|---|---|---|---|
| 2 | 战斗按 agent 顺序结算给先手方永久优势 | 2026-09-12 | `tests/world.test.ts` fairness（镜像对打红胜率须在 33–67%）。机制那半活在**族 A** |
| 3 | 出生点直视目标区让蹲点压过占区 | 2026-09-12 | `tests/map.test.ts`（>75% 出生点→区视线被挡） |

## 什么进得来

两个闸门**同时**满足才写进这里：**未来还会再遇到**（复发性）∧ **发生时不会报错**（静默性）。
不满足的去别处，别堆在这里：

| 这种东西 | 去哪 |
|---|---|
| 会报错、或现有测试抓得到 | 写成**测试 / 探针**（本仓的自然去处 = `src/probe/leak.ts` 的登记表） |
| 只发生过一次、后果可逆 | `LOG.md` 的 `#incident` |
| 现场状态 / 进度 / git 状态 | `CHECKPOINT.md` 或 `TODO.md` |
| 闸**完全覆盖**之后 | 退休成上面表里的一行墓碑，正文留在 `LOG.md` |
